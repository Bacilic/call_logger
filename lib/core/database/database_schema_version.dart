/// Η έκδοση σχήματος της βάσης — **μόνη της, χωρίς κανένα import**.
///
/// Ζει σε δικό της αρχείο για έναν πρακτικό λόγο: το εργαλείο δημοσίευσης
/// (`tool/publish.dart`) τρέχει με σκέτο `dart run`, όχι μέσα από την
/// εφαρμογή, και το μόνο που χρειάζεται από τη βάση είναι **αυτός ο
/// αριθμός** — για να γράψει στο πακέτο ενημέρωσης ποια έκδοση σχήματος
/// διαβάζει.
///
/// Όσο ο αριθμός ζούσε μέσα στα αρχεία του σχήματος, το εργαλείο έσερνε
/// ολόκληρη την αλυσίδα `database_schema_migrations` → `app_config` →
/// `path_provider` → `package:flutter` → `dart:ui`. Και το `dart:ui` δεν
/// υπάρχει έξω από τη μηχανή Flutter: η δημοσίευση σταματούσε με εκατοντάδες
/// σφάλματα μέσα στο ίδιο το Flutter («'Size' isn't a type»), που δεν έδειχναν
/// πουθενά προς την πραγματική αιτία.
///
/// **Μην προσθέσεις import εδώ.** Ένα και μόνο θα ξανάφερνε το πρόβλημα, και
/// το μήνυμα σφάλματος δεν θα οδηγούσε ξανά εδώ.
///
/// Το ιστορικό των εκδόσεων ζει στο `database_v1_schema.dart`, δίπλα στο
/// σχήμα που περιγράφει.
const int kDatabaseSchemaVersion = 56;
