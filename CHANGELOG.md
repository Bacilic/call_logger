# Ιστορικό Αλλαγών

Όλες οι σημαντικές αλλαγές σε αυτό το έργο θα καταγράφονται σε αυτό το αρχείο.

Η μορφή βασίζεται στο [Keep a Changelog](https://keepachangelog.com/el/1.1.0/) και το έργο ακολουθεί [Semantic Versioning](https://semver.org/lang/el/).

**Σημείωση:** Μπορείτε να εμπλουτίσετε τις περιγραφές με λεπτομέρειες από μνήμη· το JSON `[assets/changelog.json](assets/changelog.json)` τροφοδοτεί την εφαρμογή και πρέπει να παραμένει συγχρονισμένο με αυτό το αρχείο.

## [0.21.0] - 2026-06-01

### Προστέθηκε

- **CLI προφίλ (`main.dart`, `AppConfig`, `SettingsService`):** παράμετρος `--profile <όνομα>` ή `--profile=<όνομα>` (μόνο `a-z`, `A-Z`, `0-9`, `_`, `-`)· χωρίς παράμετρο η συμπεριφορά παραγωγής παραμένει αμετάβλητη.
- **CLI προφίλ · απομόνωση ρυθμίσεων:** πρόθεμα κλειδιών SharedPreferences (`profile_<όνομα>_…`) σε `SettingsService`, `LampSettingsStore` και `DepartmentPaletteStore`.
- **CLI προφίλ · βάση:** προεπιλεγμένο `call_logger.db` στον φάκελο `profiles/<όνομα>/` κάτω από Application Support (`path_provider`) — όχι η portable διαδρομή `..\\Data Base\\` δίπλα στο εκτελέσιμο.
- **CLI προφίλ · UI:** τίτλος παραθύρου `Καταγραφή Κλήσεων [Profile: …]` (`windowTitleWithProfileLabel`)· providers `activeProfileProvider` / `hasActiveProfileProvider`· unit tests `test/app_config_profile_test.dart`.
- **Πλοήγηση (`main_shell.dart`, `_CallsNavigationIcon`):** ένδειξη ανοιχτής κλήσης στο εικονίδιο «Κλήσεις» — `Badge` με λευκό `Icons.phone` σε κόκκινο κύκλο (οπτικά όπως badge εκκρεμοτήτων)· `callEntryProvider.select(_hasActiveCallSession)` ώστε να μην γίνεται rebuild κάθε δευτερόλεπτο· κρύβεται στην οθόνη Κλήσεων.
- **Κατάλογος · χρώμα τμήματος (`department_color_picker_dialog`, `department_form_dialog`):** διάλογος HSV (κορεσμός/φωτεινότητα, hue, HEX, αντιγραφή)· κλικ στην προεπισκόπηση ανοίγει επιλογέα.
- **Κατάλογος · παλέτα (`DepartmentPaletteStore`, `department_color_palette.dart`):** 8 τετράγωνες θέσεις χρήστη μετά το λευκό (κύκλοι = προκαθορισμένα)· κενή θέση → επιλογέας· γεμάτη → επιλογή χρώματος τμήματος· αποθήκευση `department_custom_palette_slots_v2`.

### Άλλαξε

- **Windows · metadata (`Runner.rc`):** `CompanyName` = **Bacilic** — τα τοπικά prefs και οι κατάλογοι εφαρμογής στο Roaming AppData είναι πλέον `Bacilic\Καταγραφή Κλήσεων\` (παλιά εγκατάσταση κάτω από `gr.itsupport\` δεν διαβάζεται αυτόματα).
- **Κλήσεις · κεφαλίδα (`smart_entity_selector_*_field.dart`, `text_layout_utils.dart`):** τα Χ καθαρισμού ανά πεδίο εμφανίζονται δυναμικά όταν `trim().isNotEmpty` (≥1 χαρακτήρας)· χρώμα γκρι όπως πριν.
- **Κατάλογος · παλέτα (`department_palette_actions.dart`):** διάλογος όταν καθαρίζεται χρώμα που χρησιμοποιείται σε τμήμα· διάλογος πλήρους παλέτας (8/8)· απαγόρευση διπλότυπου hex· προεπισκόπηση γεμίζει πρώτη κενή θέση· παρατεταμένο πάτημα → μενού «Διαγραφή χρώματος» (όχι άμεση διαγραφή).

### Διορθώθηκε

- **Εκκίνηση (`main.dart`):** σφάλμα «Zone mismatch» — `WidgetsFlutterBinding.ensureInitialized()`, `AppConfig.configureFromCliArguments()` και `await _bootstrapAndRunApp()` εκτελούνται στο ίδιο async closure του `runZonedGuarded` με το `runApp`.
- **Κλήσεις · κεφαλίδα (`smart_entity_selector_widget.dart`):** ευθυγράμμιση κόκκινου Χ «καθαρισμός όλων» με τα Χ των πεδίων· διόρθωση `RenderFlex overflow` (placeholder πλάτους).
- **Κλήσεις · τμήμα (`smart_entity_selector_provider.dart`):** κουμπί «προσθήκη» για άγνωστο τμήμα (π.χ. «μαγειρία») — `selectedDepartmentId` μηδενίζεται όταν δεν υπάρχει αντιστοιχία στη λίστα.
- **Κλήσεις · επιστροφή στην οθόνη (`smart_entity_selector_widget.dart`):** το πεδίο τμήμα δεν μένει κενό μετά αλλαγή tab και επιστροφή σε ανοιχτή κλήση — σύγχρονισμός controllers από provider και διόρθωση `checkContent` στο focus-out τηλεφώνου.

## [0.20.2] - 2026-05-27

### Προστέθηκε

- **Εκκρεμότητες · αναφορές (`task_analytics_bottom_sheet`, `task_analytics_date_provider`):** επιλογή εύρους ημερομηνιών με κουμπιά Σήμερα / 7 / 30 ημέρες, πλήρες εύρος δημιουργίας (παλαιότερη–νεότερη εκκρεμότητα) και προσαρμοσμένο ημερολόγιο· αποθήκευση τελευταίας επιλογής σε `SharedPreferences`· οπτική ένδειξη ενεργού preset.
- **Ρυθμίσεις (Windows) · παράθυρο (`settings_screen`, `desktop_window_service`):** ραδιοεπιλογή «Πάντα στο κέντρο της οθόνης» ή «Τελευταία θέση κατά τον τερματισμό»· αποθήκευση θέσης/μεγέθους· προεπιλογή κεντράρισμα.
- **Ιστορικό κλήσεων · πίνακας (`history_screen`, `EllipsisTooltipText`):** tooltip με πλήρες κείμενο σε Καλούντα, Τμήμα, Σημειώσεις (και συναφή κελιά μέσω `DeletedCatalogEntityText`) **μόνο** όταν το κείμενο περικόπτεται· κοινό widget `lib/core/widgets/ellipsis_tooltip_text.dart`.

### Άλλαξε

- **Εκκρεμότητες · φίλτρα (`task_filter_bar`, `filterChipLabelEl`):** chip «Ανοικτές (Ν)» και «Αναβληθείσες (Ν)» αντί «ανοικτή (Ν)» / «Αναβολές» χωρίς μετρητή.
- **Εκκρεμότητες · καθαρισμός ημερομηνιών:** `IconButton` με `assets/ic_calendar_broom_clear.png` και tooltip αντί `ActionChip` «Καθαρισμός ημερομηνιών».
- **Ιστορικό κλήσεων · επεξεργασία (`call_edit_dialog`):** αφαιρέθηκαν το πεδίο «Λύση» και το dropdown κατάστασης (η κατάσταση κλήσης διατηρείται ως έχει)· ορθογραφικός έλεγχος στις σημειώσεις (`LexiconSpellTextFormField`).
- **Βάση δεδομένων · κλήσεις:** αφαίρεση στήλης `calls.solution` με `ensureCallsNoSolutionColumn` σε κάθε άνοιγμα βάσης — **χωρίς** bump `databaseSchemaVersionV1`· νέες βάσεις χωρίς τη στήλη στο CREATE TABLE.

### Διορθώθηκε

- **Ημερολόγιο εύρους (`DateParserUtil.parseSmartInput`):** το split του `"ΗΗ/ΜΜ/ΕΕΕΕ - ΗΗ/ΜΜ/ΕΕΕΕ"` έδινε μεσαίο token `"-"` ως δεύτερη ημερομηνία — χρήση πρώτου/τελευταίου cluster με `/` ή `\`· διόρθωση ψευδοσφάλματος μετά επεξεργασία ψηφίου ημέρας/μήνα/έτους.

## [0.20.1] - 2026-05-25

### Προστέθηκε

- **Ιστορικό κλήσεων · επεξεργασία (`call_edit_dialog`):** διάλογος με `SmartEntitySelectorWidget` (`historyEditSmartEntityProvider`, `loadFromCall`), ημ/νία/ώρα, κατηγορία (`historyCategoryEntriesProvider`), κατάσταση, διάρκεια, σημειώσεις/λύση· προειδοποίηση όταν υπάρχει Lansweeper ticket/sent· κλωνοποίηση ως νέα κλήση· αποθήκευση μέσω `HistoryCallActionsService.saveEditedCall`.
- **Ιστορικό κλήσεων · διαγραφή (`call_delete_dialog`):** soft delete ή hard delete· αν υπάρχουν συνδεδεμένες εκκρεμότητες — cascade ή αποσύνδεση (`nullify`)· επαναφόρτωση providers μετά τη μετάλλαξη.
- **Ιστορικό κλήσεων · μαζική διαγραφή:** `historySelectedCallIdsProvider`, γραμμή ενεργειών «Επιλεγμένες κλήσεις» και `showCallBulkDeleteDialog`.

### Άλλαξε

- **Ιστορικό κλήσεων · πίνακας (`history_screen`):** αντικατάσταση `DataTable` με εικονικοποιημένη δομή (`ListView.builder`, οριζόντιο scroll, σταθερά πλάτη στηλών × zoom) — σημαντικά καλύτερη απόδοση σε ~180+ γραμμές και κατά το resize του desktop παραθύρου· διατήρηση ταξινόμησης, multi-select και ενέργειες ανά γραμμή.

### Διορθώθηκε

- **Ιστορικό · αποθήκευση τροποποίησης:** κλείδωμα βάσης ~10s και freeze — μέσα σε `db.transaction` καλούνταν `AuditService.performingUser(db)` αντί `performingUser(txn)`· διόρθωση σε `insertCall` / `updateCall` και `performingUser(DatabaseExecutor)`.
- **Ιστορικό · Riverpod:** «Cannot use the Ref … after it has been disposed» μετά αποθήκευση — το `historyCallActionsServiceProvider` ήταν `autoDispose` ενώ τα dialogs κάνουν μόνο `ref.read`· μετατροπή σε κανονικό `Provider` και `ref.mounted` πριν τα `invalidate`.
- **Εκκρεμότητες · audit σε transaction:** `_auditTaskCreate` / `_auditTaskUpdate` χρησιμοποιούν πλέον τον ίδιο `executor` για `performingUser` (ίδιο anti-deadlock pattern).

## [0.20.0] - 2026-05-24

### Προστέθηκε

- **Χάρτης κτιρίου · φορητότητα κατόψεων (`BuildingMapStorage`):** αποθήκευση εικόνων στο `maps_images/` δίπλα στο αρχείο βάσης· σχετικές διαδρομές στον πίνακα `building_map_floors`· διάλογος μεταφοράς όταν η επιλεγμένη εικόνα είναι εκτός φακέλου εφαρμογής.
- **Βάση δεδομένων · αντιγράφα:** διακόπτης **«Συμπερίληψη εικόνων χαρτών»** — το αντίγραφο γίνεται zip με `call_logger.db` και τον φάκελο `maps_images` (όχι μόνο `.db`).
- **Βάση δεδομένων · επαναφορά:** κουμπί **«Επαναφορά από zip (βάση + χάρτες)»**· αποσυμπίεση βάσης και εικόνων στον portable κατάλογο· relink διαδρομών μετά την επαναφορά.
- **Βάση δεδομένων · διαδρομή:** επιλογή αρχείου `.zip` στον επιλογέα βάσης — αποσυμπίεση σε `call_logger.db` δίπλα στο zip.
- **Ρυθμίσεις · Λάμπα:** διακόπτης **«Απόκρυψη παλιάς Βάσης Δεδομένων (Λάμπα)»** — εμφάνιση/απόκρυψη του στοιχείου πλοήγησης «Λάμπα» (`showLampNavProvider`, `SettingsService`)· ίδια λογική με τους διακόπτες Βάσης Δεδομένων και Λεξικού· αν είναι ενεργή η Λάμπα και την κρύψεις, επιστροφή στις Κλήσεις.

### Άλλαξε

- **Χάρτης κτιρίου · ονομασία αρχείων:** το όνομα της κατόψης προκύπτει από την ετικέτα ορόφου (`safeFloorImageBaseName`, π.χ. `1st_floor.png`)· σε συνωνυμία προστίθεται επίθημα `yyyy-MM-dd_HH-mm-ss`.
- **Πλοήγηση (`MainShell`):** το στοιχείο **Λάμπα** στο NavigationRail μετακινήθηκε αμέσως κάτω από **Ιστορικό** (πριν Βάση Δεδομένων / Λεξικό).
- **Κατάλογος · εισαγωγή Master Excel:** αφαιρέθηκε η ροή από το κύριο κέλυφος — floating κουμπί (`_onImportExcel`), ρύθμιση **«Εμφάνιση κουμπιού Import Excel»**, live κονσόλα (`ImportConsoleWidget`), διάλογος αντικατάστασης καταλόγου και υπηρεσίες `ImportService` / `ExcelParser` / `importPreparedData`· **δεν** επηρεάζεται η εισαγωγή Excel στην οθόνη **Λάμπα** (`old_excel_importer`).

### Διορθώθηκε

- **Χάρτης κτιρίου · λείπουσα εικόνα:** το **«Επανεπιλογή εικόνας»** καλεί `replaceFloorSheetImageById` — ενημέρωση `image_path` στο **ίδιο** φύλλο, χωρίς νέο `building_map_floors`· διατήρηση `map_*` γεωμετρίας τμημάτων.

## [0.19.2] - 2026-05-22

### Προστέθηκε

- **Κλήσεις · MiniMapCard:** εικονίδιο `assets/department_not_found.png` όταν το τμήμα δεν έχει αποθηκευμένη θέση στον χάρτη κτιρίου.
- **Κατάλογος · εισαγωγή Excel:** διάλογος **«Αντικατάσταση καταλόγου»** πριν το `clearImportedData` — εξηγεί ότι κλήσεις, εκκρεμότητες και ιστορικό **δεν** διαγράφονται (F2-03).
- **Κατάλογος · διαγραφή χρήστη:** διάλογος για αποκλειστικό τηλέφωνο 1↔1 — **«Μένει στο τμήμα»** ή **«Αφαίρεση τηλεφώνου»** (`user_delete_exclusive_phone_dialog`, F2-02).
- **Ιστορικό κλήσεων:** στήλη **Κατηγορία**· ένδειξη **(διαγραμμένο)** (πλαγιά) για χρήστη, εξοπλισμό και κατηγορία όταν η συνδεδεμένη εγγραφή καταλόγου είναι soft-deleted (F3-04/05).

### Άλλαξε

- **Κλήσεις · κεφαλίδα φόρμας (`SmartEntitySelectorWidget`):** οι βοηθητικές λίστες προτάσεων για **Τηλέφωνο**, **Καλούντα** και **Εξοπλισμό** εμφανίζονται πλέον σε **OverlayPortal** με `CompositedTransformFollower` — επικάλυπτουν τα κάτω πεδία χωρίς να τα σπρώχνουν (αντί inline `Column`).
- **Κλήσεις · MiniMapCard (`_buildSnapshot`):** η μικρογραφία κατόψης (zoom + overlay) εμφανίζεται μόνο όταν `DepartmentModel.isMapped`· αλλιώς εικονίδιο + κείμενο **«Δεν υπάρχει το τμήμα στο χάρτη»** — αφαιρέθηκε η προβολή ολόκληρης κατόψης για μη χαρτογραφημένα τμήματα.
- **Κατάλογος · τμήματα (F3-02):** `getActiveDepartments()` για lookup, πίνακα ελέγχου και MiniMapCard· `getDepartments()` μόνο στην οθόνη Κατάλογος (συμπεριλαμβάνει soft-deleted).
- **Πίνακας ελέγχου · φίλτρο χρήστη (F4-05):** κανονικοποίηση κειμένου και αναζήτηση στο `search_index` (`_appendDashboardUserFilter`).
- **Ιστορικό / Κλήσεις / Εκκρεμότητες:** ενοποιημένο επίθημα **(διαγραμμένο)** μέσω `DeletedCatalogEntityText` και flags από JOIN (`caller_is_deleted`, `equipment_is_deleted`, κ.λπ.) — συμπεριλαμβανομένων «Τελευταίων 7 κλήσεων» και καρτών εκκρεμοτήτων.

### Διορθώθηκε

- **Κλήσεις · κεφαλίδα:** πλοήγηση με **↑↓** και **Enter** στις overlay λίστες — ζωντανή ενημέρωση κειμένου πεδίου (`_isKeyboardPreview` + `_setControllerText`), `highlightedIndex` στις λίστες και σωστό commit με Enter.
- **Κλήσεις · `ref.listen`:** guards ώστε το sync state→controller να μην σβήνει preview πληκτρολογίου όταν το πεδίο έχει focus (τηλέφωνο: κενό `selectedPhone`· καλούντας/εξοπλισμός: ασύγχρονο `callerDisplayText`/`equipmentText`).
- **Κλήσεις · τηλέφωνο:** όταν υπάρχουν `phoneCandidates` και κενό πεδίο, το `optionsBuilder` του Autocomplete επιστρέφει κενή λίστα — αποφυγή διπλής framework λίστας παράλληλα με την overlay.
- **Κλήσεις · αποθήκευση (F6-01):** `insertCall` / `updateCall` σε transaction (κλήση + `search_index` + audit)· `CallSaveException` και SnackBar στο UI.
- **Εκκρεμότητες (F6-02):** `createTask` / `updateTask` / `createFromCall` σε transaction· `TaskSaveException` και μήνυμα στο UI.
- **Κατάλογος · διαγραφή χρήστη (F2-02):** καθαρισμός `user_phones` στο `deleteUsers`· πολιτική αποκλειστικού τηλεφώνου.
- **Αναζήτηση κλήσεων (F4-01):** επαναδόμηση `search_index` μετά `updateUser` / `updateEquipment` / `createFromCall`.

## [0.19.1] - 2026-05-21

### Διορθώθηκε

- **Κλήσεις · εξοπλισμός (`SmartEntitySelectorWidget` / `SmartEntitySelectorNotifier`):** μετά επιλογή κωδικού από αναδυόμενη λίστα (π.χ. 2990) ο εξοπλισμός δεν εξαλείφεται πλέον όταν το focus πάει στις σημειώσεις — συγχρονισμός `equipmentText` στο `setEquipment`, προστασία χειροκίνητης επιλογής σε lookups, `updatePhone` χωρίς καθαρισμό εξοπλισμού κατά lookup/autofill, guard στο `ref.listen` και `didUpdateWidget`, παράλειψη επανάληψης blur lookup όταν η επιλογή είναι ήδη έγκυρη.

## [0.19.0] - 2026-05-19

### Προστέθηκε

- **Πίνακας ελέγχου · KPI:** tooltips στα mini bar sparklines όταν το φίλτρο είναι «Όλες οι ημερομηνίες» — `KpiBarSparklinePoint` με κείμενο ανά ράδο (μήνας, ημέρα Δευ–Παρ, μεγαλύτερες/μικρότερες κλήσεις, θέσεις 2–6 κορυφαίου entity)· hover με `BarSparklineChart` και `OverlayPortal`.
- **Πίνακας ελέγχου · KPI:** tooltips στα line sparklines με φίλτρο ημερομηνίας — συνολικές κλήσεις, συνολική διάρκεια και μέσος όρος ανά κλήση ανά ημέρα (`dd/MM` + τιμή).
- **Lansweeper · αναφορά:** προσυμπλήρωση Ticket ID με **μέγιστο αριθμητικό id + 1** (`CallsRepository.suggestedNextLansweeperTicketId`) στο διάλογο καταχώρησης κλήσης και χειροκίνητης σήμανσης· διατηρείται το υπάρχον id της κλήσης αν υπάρχει.
- **Ιστορικό κλήσεων:** πλήθος κλήσεων με φίλτρα **κατηγορίας** και **ημερομηνίας** αριστερά στη γραμμή ζουμ (`historyCategoryDateCallCountProvider`, `CallsRepository.getHistoryCallCount`) — χωρίς keyword αναζήτησης.
- **Εφαρμογή (Windows) · μέγεθος παραθύρου:** μόνιμη αποθήκευση τελευταίου πλάτους/ύψους που όρισε ο χρήστης (`SettingsService.getSavedWindowSize` / `setSavedWindowSize` στο `SharedPreferences`)· επαναφορά στην εκκίνηση (`main.dart`)· αποθήκευση μετά το resize (`onWindowResized` στο `AppShortcuts`) και στο κλείσιμο· δεν αντικαθιστά το μέγεθος όταν το παράθυρο είναι maximized.

### Άλλαξε

- **Πίνακας ελέγχου · KPI tooltips διάρκειας:** `formatKpiAggregateDurationSeconds` / `formatKpiCallDurationSeconds` — μορφή `ώω:λλ` ή `λλ:δδ` αντί ωμών δευτερολέπτων (`δ`).
- **Ιστορικό κλήσεων · γραμμή ζουμ:** «Μέγεθος πίνακα» και ποσοστό **μετά** το κουμπί μεγέθυνσης· επαναφορά 100% με `IconButton` (`Icons.restart_alt`) αντί `TextButton`.
- **Κλήσεις · sticky σημειώσεις:** ο μετρητής χαρακτήρων εμφανίζεται **μέσα** στο πεδίο (κάτω δεξιά, `Stack` + `counterText: ''`) — ομοιόμορφο περιθώριο στο κίτρινο πλαίσιο.
- **Κλήσεις · κεφαλίδα φόρμας (`CallHeaderForm`):** αναλογική κατανομή πλάτους πεδίων (18:34:24:20, κανονικοποίηση στο 100% του `available`) χωρίς ανώτατα `clamp` — τα πεδία μεγαλώνουν αναλογικά σε ευρύτερα παράθυρα· σταθερά κενά/trailing (`_kHeaderGapsAndIcons`)· ενημέρωση `equipmentColumnOffset` για το κουμπί «Προσθήκη».

### Διορθώθηκε

- **Πίνακας ελέγχου · KPI:** τα tooltips δεν κόβονται πλέον από τη διπλανή κάρτα — εμφάνιση στο overlay layer (`OverlayPortal` αντί τοπικού `Stack`).
- **Πίνακας ελέγχου · KPI μέσος όρος (Όλες οι ημερομηνίες):** SQL φίλτρο `duration > 0` για τις 3 μικρότερες κλήσεις — οι ράβδοι 00:00 δεν εμφανίζονται πλέον ως κενές.
- **Κλήσεις · κεφαλίδα φόρμας:** διόρθωση overflow στη γραμμή πεδίων σε στενά παράθυρα — κατάργηση `available.clamp(200)` που ξεπερνούσε το `maxWidth`· ασφαλής κλιμάκωση αν το άθροισμα πεδίων υπερβαίνει το διαθέσιμο πλάτος.
- **Κλήσεις · ετικέτα Εξοπλισμός (`_EquipmentField`):** η ετικέτα «Κωδικός Εξοπλισμού» μένει σε μία γραμμή (`FittedBox` + `scaleDown`) στο ελάχιστο πλάτος — δεν μετατοπίζεται πλέον το πεδίο εισαγωγής προς τα κάτω.

## [0.18.0] - 2026-05-18

### Προστέθηκε

- **Πίνακας ελέγχου · KPI (Όλες οι ημερομηνίες):** macro στατιστικά χωρίς σύγκριση προηγούμενης περιόδου — ημερήσιος μέσος όρος κλήσεων (στρογγυλοποίηση σε ακέραιο), μέσος χρόνος απασχόλησης ανά ενεργή ημέρα, διάμεσος χρόνος κλήσης, ποσοστό κορυφαίου τμήματος/καλούντα/βλάβης επί του συνόλου· απόκρυψη βέλους τάσης στις τέσσερις KPI κάρτες.
- **Πίνακας ελέγχου · KPI:** τίτλος «Συνολικές κλήσεις» με αριθμό ενεργών ημερών και εύρος ιστορικού (`Από dd/MM/yyyy έως Σήμερα` όταν η τελευταία κλήση είναι σήμερα).
- **Πίνακας ελέγχου · δεδομένα:** επέκταση `DashboardSummaryModel` (`isAllDatesMode`, `totalActiveDays`, `medianDurationSeconds`, `historyDateFrom`/`historyDateTo`)· υπολογισμός στο `CallsRepository.getDashboardStatistics` (ενεργές ημέρες, min/max ημερομηνία, διάμεσος)· παράλειψη ερωτήματος προηγούμενης περιόδου όταν δεν υπάρχει φίλτρο ημερομηνιών.
- **Πίνακας ελέγχου · KPI (Όλες οι ημερομηνίες):** mini bar sparklines στις 4 κάρτες — κλήσεις ανά μήνα, συνολική διάρκεια Δευ–Παρ, 3 μεγαλύτερες + 3 μικρότερες κλήσεις, θέσεις 2–6 κορυφαίου entity (τμήμα/καλών/βλάβη)· νέο `_BarSparklineChart` (fl_chart).
- **Πίνακας ελέγχου · δεδομένα:** `KpiAllDatesBarSparklines` στο μοντέλο και συγκεντρωτικά SQL (μήνας, `%w` 1–5, άκρα διάρκειας, runner-ups) στο repository.

### Άλλαξε

- **Πίνακας ελέγχου · διάρκεια:** `_formatAggregateDurationSeconds` για συνολικές/ημερήσιες τιμές (`10ω:23λ` = ώρες:λεπτά) και `_formatCallDurationSeconds` για μέσο/διάμεσο ανά κλήση (`03:15` = λεπτά:δευτερόλεπτα)· τίτλοι καρτών `(ώρ:λεπ)` και `(λεπ:δευτ)`.
- **Πίνακας ελέγχου · KPI sparkline:** σε «Όλες οι ημερομηνίες» αντικατάσταση `LineChart` με minimal `BarChart` — χωρίς άξονες, πλέγμα, borders και tooltips· λεπτές ράβδοι στα χρώματα παλέτας κάρτας.

## [0.17.0] - 2026-05-18

### Προστέθηκε

- **Πίνακας ελέγχου · φίλτρα ημερομηνίας:** προεπιλογή **Σήμερα**· μόνιμη αποθήκευση τελευταίας επιλογής (Σήμερα / 7 ημέρες / 30 ημέρες / Όλα) στο `SettingsService` — διατηρείται μετά την έξοδο από την οθόνη στατιστικών και επανεκκίνηση· τα presets 7/30/σήμερα ξαναυπολογίζονται από την τρέχουσα ημερομηνία.
- **Πίνακας ελέγχου · φίλτρα ημερομηνίας:** οπτική ένδειξη ενεργού preset στα κουμπιά (`FilledButton` έναντι `FilledButton.tonal`).
- **Lansweeper · αναφορά:** το `LansweeperStateBadge` λειτουργεί ως κουμπί εναλλαγής **καταχωρημένη ↔ ακαταχώρητη**· προαιρετικό ticket id (π.χ. `17132`) αποθηκεύεται στο `calls.lansweeper_main_ticket_id` (χωρίς νέο πεδίο schema).
- **Lansweeper · αναφορά:** φίλτρο λίστας κλήσεων με `FilterChip` — προεπιλογή **Ακαταχώρητες**· επιλογές Καταχωρημένες, Εξαιρεμένες, Αποτυχημένες, Όλες.
- **Lansweeper · αναφορά:** διάλογος κατά απο-καταχώρηση όταν υπάρχει ticket id (**μηδενισμός** ή **διατήρηση** για επαναφορά)· προειδοποίηση πριν την καταχώρηση όταν το ίδιο ticket id υπάρχει σε άλλες καταχωρημένες κλήσεις (`countCallsWithLansweeperTicketId`) — επιτρέπεται για ομοδοποιημένες κλήσεις (ίδιος καλών / ένα περιστατικό).

### Άλλαξε

- **Πίνακας ελέγχου · KPI:** υπότιτλος σύγκρισης **«εχθές»** αντί «προηγ. ημέρα» στο `DashboardFilterModel.kpiComparisonRangeHint()`· νέο `_EllipsisTooltipText` — tooltip με πλήρες κείμενο όταν η υποσημείωση κόβεται.

## [0.16.1] - 2026-05-14

### Διορθώθηκε

- **Κλήσεις · κατηγορία:** στο `CategoryAutocompleteField`, μετά από επιλογή από τη λίστα και κένωμα (Χ ή πληκτρολόγιο), η λίστα εμφανίζεται ξανά με κλικ στο πεδίο· διορθώθηκε το `_suppressOptionsUntilTyping` σε συνδυασμό με συνθήκη `blocked` που δεν εφαρμόζεται σε κενό κείμενο (race `optionsBuilder` / `onChanged`).
- **Κλήσεις · εκκρεμότητα:** όταν αδειάζουν οι σημειώσεις, το checkbox «Εκκρεμότητα» αποεπιλέγεται αυτόματα· η καταχώρηση κλήσης δεν δημιουργεί πλέον task χωρίς μη κενό κείμενο σημειώσεων (και η κατάσταση κλήσης δεν μένει `pending` χωρίς issue).

### Προστέθηκε

- **Πίνακας ελέγχου (dashboard):** δύο νέες χρωματικές παλέτες «Δάσος» και «Νυχτερινό ίντιγκο» (σύνολο πέντε)· ελληνικές ετικέτες στο μενού για όλες τις παλέτες (Κλασικό, Ωκεανός, Ανατολή, Δάσος, Νυχτερινό ίντιγκο).

### Άλλαξε

- **Πίνακας ελέγχου:** η ενεργή παλέτα δένει πλέον φόντο οθόνης, διαβάθμιση (gradient), γυάλινες κάρτες, τίτλους και υπότιτλους στις κάρτες KPI, κεφαλίδες ενοτήτων, πίνακα πιο χρονοβόρων κλήσεων, χρώματα θέσεων κατάταξης, ράβδους προόδου, κάρτες ενσωματωμένων γραφημάτων και χρώματα τομών στην πίτα βλαβών· η μπάρα κορυφής και τα skeletons φόρτωσης ακολουθούν την παλέτα· το εικονίδιο του κουμπιού «Χρώματα» είναι πολύχρωμο (`ShaderMask` με διαβάθμιση στους τόνους των KPI).

## [0.16.0] - 2026-05-11

### Προστέθηκε

- **Lansweeper · ρυθμίσεις αναφοράς:** ξεχωριστά αποθηκευμένα URL για **API** (`…/api.aspx`, κλειδί `lansweeper_api_url`) και για **φόρμα εισιτηρίου στον browser** (κλειδί `lansweeper_url`), με providers `lansweeperApiUrlProvider` και `lansweeperTicketFormUrlProvider`.
- **Lansweeper · επικύρωση:** νέο `LansweeperUrlRules` και παράδειγμα `kExampleLansweeperApiUrl` για υποδείξεις· έλεγχος ώστε η Άμεση καταχώρηση να ενεργοποιείται μόνο με έγκυρο API URL και το «Αντιγραφή & άνοιγμα» μόνο με έγκυρο http(s) URL φόρμας.
- **Lansweeper · διάλογος σύνδεσης:** `LansweeperConnectionSettingsDialog` από εικονίδιο ρυθμίσεων στην αναφορά· κουμπιά **«Έλεγχος συνδέσμου»** που ανοίγουν τον αντίστοιχο σύνδεσμο στον φυλλομετρητή (χωρίς αντιγραφή στο πρόχειρο).
- **Lansweeper · πράκτορας:** μόνιμη αποθήκευση `lansweeper_agent_username` στο `app_settings` και provider `lansweeperAgentUsernameProvider` (το πεδίο μεταφέρθηκε από τη φόρμα ανά κλήση στις ρυθμίσεις).
- **Λάμπα · Επιλεκτική μεταφορά:** νέο κουμπί «Μεταφορά» στις κάρτες αποτελεσμάτων (μόνο για Εξοπλισμό/Ιδιοκτήτη/Τμήμα), που ανοίγει οδηγό μεταφοράς διπλού πίνακα (Dual-Pane Wizard).
- **Λάμπα · οδηγός μεταφοράς:** νέο `LampTransferWizardDialog` με αριστερή προβολή μόνο ανάγνωσης από `old_equipment.db` και δεξιά επεξεργάσιμη φόρμα προορισμού στη `call_logger.db`.
- **Λάμπα · υπηρεσία μεταφοράς:** νέο `LampMigrationService` για cross-referencing παλιών προς νέες εγγραφές, με pre-fill πεδίων και fallback Top-3 υποψηφίων αντιστοιχίσεων με confidence score.

### Διορθώθηκε

- **Lansweeper · Riverpod:** αποφυγή τροποποίησης providers κατά το κλείσιμο διαλόγου (lifecycle)· η αποθήκευση ρυθμίσεων από πεδία εκτελείται μετά το frame (`addPostFrameCallback`) ώστε να μην εμφανίζεται σφάλμα «Tried to modify a provider while the widget tree was building».

### Άλλαξε

- **Lansweeper · dashboard:** οι ρυθμίσεις URL/API/πράκτορα μεταφέρθηκαν από το panel φίλτρων στον διάλογο αναφοράς Lansweeper (εικονίδιο ρυθμίσεων).
- **Lansweeper · SettingsService:** το `getLansweeperApiUrl()` δεν επιστρέφει πλέον αυθαίρετα το URL φόρμας· το legacy `lansweeper_url` χρησιμοποιείται ως API URL μόνο αν περιέχει `api.aspx`.
- **Λάμπα · scoring αντιστοίχισης:** προστέθηκε επαναχρησιμοποιήσιμη ρουτίνα similarity/confidence score στο `LampIssueResolutionService` και αξιοποιείται από τον migration οδηγό (χωρίς νέο scoring αλγόριθμο από το μηδέν).
- **Λάμπα · ροή αποθήκευσης:** ο wizard γράφει αποκλειστικά μέσω υπαρχόντων methods του `DirectoryRepository` (`insertUser`, `insertEquipment`, `insertDepartment`, `update`*) με ασφαλή defaults schema (`is_deleted=0`, `map_hidden=1`).
- **Λάμπα · συμβατότητα UI:** αντικατάσταση deprecated χειρισμού `RadioListTile` με `RadioGroup` στο wizard, ώστε να αφαιρεθούν προειδοποιήσεις `deprecated_member_use`.

## [0.15.0] - 2026-05-03

### Προστέθηκε

- **Λάμπα · ρυθμίσεις:** κουμπί «Έλεγχος Προβλημάτων» στο διάλογο ρυθμίσεων — ίδια ροή ελέγχου ακεραιότητας με την καρτέλα «Προβλήματα ETL» πάνω στη βάση `.db` ανάγνωσης (π.χ. `old_equipment.db`), ώστε να είναι δυνατός ο έλεγχος όταν η καρτέλα δεν εμφανίζεται ακόμα.

### Άλλαξε

- **Λάμπα · Προβλήματα ETL:** ελληνικές ετικέτες τύπων εγγραφών στο `data_issues` (κλειδιά αναφοράς, διπλότυπα, κύριος εξοπλισμός, κύκλοι ιεραρχίας, κ.λπ.) σε λίστες, στην αναφορά του ελέγχου ακεραιότητας και στα μηνύματα επίλυσης· τα εσωτερικά κλειδιά `issue_type` στη SQLite παραμένουν αμετάβλητα.
- **Λάμπα · Προβλήματα ETL:** αναδιπλούμενες ομάδες ανά κατηγορία· κουμπιά επίλυσης μεταφέρθηκαν ως εικονίδια στην κεφαλίδα κάθε ομάδας (με tooltip)· ταξινόμηση ομάδων και της αντιγραφής «για ΤΝ» κατά προτεινόμενη σειρά επίλυσης.
- **Λάμπα:** κουμπί πληροφοριών (ι) δίπλα στο «Έλεγχος για προβλήματα» με κείμενο συνιστώμενης σειράς επίλυσης· ενημέρωση των ελληνικών περιγραφών των βημάτων ελέγχου ακεραιότητας στο `OldEquipmentRepository`.
- **Λάμπα · επίλυση:** ελληνική διατύπωση στην προεπισκόπηση εφαρμογής και στο διάλογο χειροκίνητης επισκόπησης (είδος ενέργειας, γραμμή/στήλη/βεβαιότητα, φιλικά ονόματα στηλών)· ελληνικά μηνύματα προτάσεων στο `LampIssueResolutionService` όπου εμφανίζονταν αγγλικοί όροι.
- **Λάμπα · Ασύμβατο Αναγνωριστικό:** η διάγνωση/παρουσίαση προβάλλει πλέον ως κύρια στοιχεία τον τύπο οντότητας (`entity_type`) και την προέλευση (`origin`) αντί για το legacy `sheet`, ώστε η ερμηνεία να βασίζεται στη φύση του σφάλματος και όχι στην πηγή φύλλου.
- **Λάμπα · οδηγός unknown_id:** για `Ασύμβατο Αναγνωριστικό` παράγονται πλέον μόνο χειροκίνητες προτάσεις επανασύνδεσης (χωρίς αυτόματη εφαρμογή/δημιουργία), για `owner/contract/office/model`, με απλή προτεραιοποίηση από συσχετίσεις FK στον πίνακα `equipment`.
- **Λάμπα · χειροκίνητος έλεγχος unknown_id:** στις κάρτες απόφασης εμφανίζονται πλέον συνοπτικά στοιχεία της ίδιας εγγραφής εξοπλισμού (κωδικός/περιγραφή, κατάσταση, asset/serial, τμήμα, υπάλληλος, μοντέλο, συμβόλαιο) για τεκμηριωμένη επιλογή επανασύνδεσης.
- **Λάμπα · labels αντιστοιχίσεων unknown_id:** στα συμφραζόμενα γραμμής προτιμώνται πλέον ονομαστικές τιμές (π.χ. όνομα τμήματος/μοντέλου) αντί για σκέτους κωδικούς όταν υπάρχουν διαθέσιμα στοιχεία στους πίνακες αναφοράς.

## [0.14.0] - 2026-04-28

### Προστέθηκε

- **Λάμπα · επεξεργασία legacy βάσης:** πλήρης επεξεργασία όλων των πεδίων στους επιχειρησιακούς πίνακες `offices`, `owners`, `model`, `contracts` και `equipment`, μαζί με PK/FK πεδία και `*_original_text` τιμές για χειροκίνητο καθαρισμό δεδομένων Excel.
- **Λάμπα · ακεραιότητα δεδομένων:** εφαρμογή partial μοναδικών ευρετηρίων και triggers για `asset_no`, `(model, serial_no)`, `set_master` self-reference/cycles, πολιτικές cascade/restrict/set-null και συμφωνία γραφείου εξοπλισμού-κατόχου.
- **Λάμπα · αλλαγή γραφείου ιδιοκτήτη:** νέα ροή επιβεβαίωσης για μηδενισμό ή αντιγραφή τηλεφώνου νέου γραφείου και επιλογή μαζικής μεταφοράς εξοπλισμού ή αποσύνδεσης κατόχου.
- **Λάμπα · Προβλήματα ETL:** νέο κουμπί «Έλεγχος για προβλήματα» που εκτελεί έλεγχο ακεραιότητας βάσει κανόνων (μοναδικότητες, owner-office consistency, `set_master`, κύκλοι) και εμφανίζει σύντομη αναφορά πριν από προαιρετική καταχώρηση στο `data_issues`.
- **Λάμπα · εξυγίανση προβλημάτων:** νέα κουμπιά επίλυσης για `non_numeric_fk`, `unknown_id`, διπλότυπα `asset_no`, διπλότυπα `(model, serial_no)`, ασυμφωνίες owner-office και προβλήματα `set_master`, με προεπισκόπηση, `manual_review` επιλογές και ασφαλή batch εφαρμογή διορθώσεων.

### Άλλαξε

- **Λάμπα · validation:** χαλάρωση μορφολογικών ελέγχων σε email/τηλέφωνα/ημερομηνίες/κείμενα ώστε να επιτρέπεται καθαρισμός αλλοιωμένων τιμών από Excel, με διατήρηση μόνο των ελέγχων ακεραιότητας.
- **Λάμπα · Προβλήματα ETL:** η φόρτωση της καρτέλας προβλημάτων εμφανίζει πλέον έως 10.000 ανοικτά issues ώστε οι μαζικές ροές επίλυσης να βλέπουν όλες τις κατηγορίες που παρήγαγε ο έλεγχος.

## [0.13.0] - 2026-04-27

### Προστέθηκε

- **Λάμπα:** συγκεντρωτικό ευρετήριο αναζήτησης `search_index` (όπου η βάση επιτρέπει εγγραφή), προφόρτωση in-memory cache και αποτέλεσμα με `**totalCount`** και εμφάνιση έως **Ν** γραμμών (ρύθμιση **Ν** στο διάλογο ρυθμίσεων· δυναμικό μήνυμα «Εμφάνιση των πρώτων Ν…» ή «Βρέθηκαν Χ…»).
- **Λάμπα:** ζωντανή αναζήτηση με **debounce**, Enter, αμοιβαίος καθαρισμός καθολικής/ανά πεδίο, εκκαθαρίσεις ανά πεδίο και «Καθαρισμός όλων».
- **Λάμπα · αναζήτηση:** κάρτα αποτελεσμάτων τύπου **dashboard** πέντε ενοτήτων (Εξοπλισμός, Μοντέλο, Σύμβαση, Ιδιοκτήτης, Τμήμα) με **χρωματική κωδικοποίηση**, **responsive** διάταξη και κάθετους διαχωριστές.
- **Αντιγραφή τιμών:** `SelectableText` σε όλα τα values, `CopyableField` με εικονίδιο αντιγραφής και SnackBar «Αντιγράφηκε {ετικέτα}: {τιμή}» με **truncate** πάνω από 80 χαρακτήρες.
- **Επεξεργασία ανά ενότητα** (όχι ανά πεδίο): κουμπί επεξεργασίας ανά στήλη, διάλογος φόρμας με **validators** (email, ημερομηνίες, τηλέφωνα, αριθμοί), παρακολούθηση αλλαγών (**original vs current**), επιβεβαίωση απόρριψης, **Escape** για κλείσιμο.
- **Αποθήκευση στη SQLite** μέσω `OldEquipmentRepository.updateSection` με **transaction** και allowlist πεδίων ανά ενότητα· **τοπική ενημέρωση** της κάρτας μετά από επιτυχία.
- Πεδίο **«Συνδεδεμένο σε»** (`set_master`): **απόκρυψη** της γραμμής όταν ο κωδικός εξοπλισμού ταυτίζεται με τον κύριο κωδικό.

### Άλλαξε

- **Λάμπα:** ενοποίηση των καρτελών «Ανά πεδίο» και «Καθολική» σε μία καρτέλα «Αναζήτηση» (καθολικό πεδίο πάνω, φίλτρα κάτω).
- **Λάμπα · Πίνακες:** αφαίρεση του κουμπιού «Ανανέωση»· όταν ανοίγει η αναδιπλούμενη κάρτα στατιστικών αρχείου, ο υπότιτλος συνοψίζεται για ελευθέρωση χώρου.
- Επέκταση της **κανονικοποιημένης** αναζήτησης σε επιπλέον πεδία εξοπλισμού/γραφείων/μοντέλων/συμβάσεων· ενημέρωση έκδοσης cache για ανακατασκευή ευρετηρίου.
- Επέκταση `**SELECT`** ερωτήματος αναζήτησης για εμφάνιση της κάρτας (ημερομηνίες, κόστος, identifiers μοντέλου/σύμβασης/ιδιοκτήτη/γραφείου).

---

## [0.12.0] - 2026-04-26

### Προστέθηκε

- Νέο module **Λάμπα (Lamp)** (legacy περιήγηση βάνας): ξεχωριστές διαδρομές **ανάγνωσης** (αναζήτηση, πίνακες) και **εξόδου/ETL** (δημιουργία/ενημέρωση), migration από την παλιά μοναδική ρύθμιση `lamp_database_path`, **έλεγχος εγκυρότητας** αρχείου στο διάλογο ρυθμίσεων (χωρίς μόνιμο ενοχλητικό banner).
- Στην Λάμπα, καρτέλες **Αναζήτηση**, **Προβλήματα ETL** (όταν εφαρμόζεται) και **Πίνακες** (όταν το αρχείο ανάγνωσης είναι έγκυρο): λίστα πινάκων με φιλικούς/τεχνικούς τίτλους, απλή προεπισκόπηση (preview) σειρών, οριζόντιο/κάθετο κύλισμα με σωστούς `ScrollController`/`Scrollbar`, **μεταβλητό πλάτος** λίστας/προεπισκόπησης με **απομνημόνευση** πλάτους, **αναδιπλούμενη** κάρτα στατιστικών αρχείου (κλειστή **προεπιλογή**) με **συνοπτικό υπότιτλο** όταν είναι μαζεμένη.

### Άλλαξε

- Αναβαθμίστηκε η μορφοποίηση του ιστορικού εφαρμογής (audit) με ενιαίο, πιο σαφές ύφος σύνοψης: `ΕΝΕΡΓΕΙΑ · οντότητα - αλλαγή`.
- Προστέθηκε φιλική περιγραφή αλλαγών πεδίων ανά οντότητα (κλήσεις, εκκρεμότητες, χρήστες, τμήματα, εξοπλισμός, τηλέφωνα, κατηγορίες), με fallback για μη εξειδικευμένα πεδία.
- Η ενότητα του side panel αναδομήθηκε σε «Τι άλλαξε» με ξεχωριστές αναδιπλούμενες «Τεχνικές λεπτομέρειες (JSON)», ώστε να παραμένει η πηγή αλήθειας χωρίς περιττή επανάληψη.
- Οι μαζικές ενέργειες (bulk) εμφανίζονται πλέον με πιο κατανοητή σύνοψη (επηρεαζόμενο πλήθος και φιλικά ονόματα πεδίων).
- Το φίλτρο «Τύπος οντότητας» στο ιστορικό εμφανίζει πλέον φιλικές ελληνικές ετικέτες αντί για raw τεχνικές τιμές.
- Το φίλτρο «Ενέργεια» στο ιστορικό αντικαταστάθηκε από δυναμική λίστα επιλογών που ενημερώνεται με βάση τον επιλεγμένο τύπο οντότητας και το εύρος ημερομηνιών.
- Η αναζήτηση audit στηρίζεται πλέον σε κανονικοποιημένο πεδίο `audit_log.search_text` (χωρίς action), με συνδυαστική αναζήτηση ανά token και ανεκτικότητα σε μορφολογικές παραλλαγές (`η/ι`, περικοπή καταλήξεων).
- Προστέθηκε ροή μαζικής επιλογής/διαγραφής εγγραφών audit (checkbox ανά γραμμή, επιλογή όλων βάσει φίλτρων, κουμπί `Icons.cleaning_services`, επιβεβαίωση και αναίρεση 5").

### Διορθώθηκε

- Στην οθόνη Κλήσεων, μετά από «Νέα Κλήση», τα κουμπιά ενεργειών της «Κατηγορίας προβλήματος» δεν παραμένουν πλέον ορατά όταν το πεδίο έχει εκκαθαριστεί.
- Διορθώθηκε το παραπλανητικό μήνυμα «Η οντότητα δεν βρέθηκε στη βάση (πιθανώς διαγράφηκε)» για εγγραφές audit τύπου τμήματος/κατηγορίας, με προσθήκη υποστηριζόμενης προεπισκόπησης και καθαρό διαχωρισμό από μη υποστηριζόμενο τύπο.
- Διορθώθηκε η σύνθεση του `search_text` ώστε να περιλαμβάνει πλήρεις υποενέργειες (π.χ. «αλλαγή χρώματος από … σε …», «αφαίρεση …», «προσθήκη …») για ακριβέστερο φιλτράρισμα.
- Διορθώθηκε η διπλή εμφάνιση μηνύματος μετά από μαζική διαγραφή audit: παραμένει μόνο το `SnackBar` με «Αναίρεση» και δεν εμφανίζεται δεύτερο μήνυμα στη λήξη του χρόνου.

---

## [0.11.0] - 2026-04-23

### Προστέθηκε

- Πλήρες module αναλύσεων εκκρεμοτήτων σε modal panel από το `TaskFilterBar`, με διάταξη Bento και οπτικό ύφος Modern Soft-UI.
- Νέες μετρικές για εκκρεμότητες: ανοικτές τώρα, ανοικτές που δημιουργήθηκαν στο εύρος, ολοκληρωμένες στο εύρος, διπλό overdue rate, μέσος χρόνος ολοκλήρωσης, μέσο πλήθος αναβολών.
- Νέα οπτικά στοιχεία αναφοράς: κάρτες KPI με sparkline 7 ημερών, area chart backlog growth, donut chart προέλευσης και insight card φυσικής γλώσσας.
- Νέα μοντέλα/πάροχοι αναλύσεων (`task_analytics_summary`, `task_analytics_filter`, `taskAnalyticsProvider`).
- Modal αναφοράς Lansweeper από την κάρτα «Συνολικές κλήσεις» στο dashboard, με ομαδοποίηση κλήσεων ανά καλούντα.
- Πολλαπλή επιλογή κλήσεων με checkboxes (σε επίπεδο ομάδας και μεμονωμένης εγγραφής), με δυναμικό υπολογισμό συνολικής διάρκειας.
- Ενέργεια «Αντιγραφή & Άνοιγμα Lansweeper» που αντιγράφει μορφοποιημένο κείμενο επιλεγμένων κλήσεων στο πρόχειρο και ανοίγει τον σύνδεσμο.
- Νέο `SettingsRepository` για μόνιμη αποθήκευση ρυθμίσεων στο `app_settings` (`getSetting`/`saveSetting`).
- Νέοι providers στο dashboard για αναφορά κλήσεων και μόνιμη ρύθμιση `lansweeperUrl`.

### Άλλαξε

- Επεκτάθηκε το schema της βάσης με στήλη `tasks.origin` και migration σε έκδοση v23, με backfill παλιών εγγραφών σε `legacy`.
- Οι νέες εκκρεμότητες ταξινομούνται πλέον με ρητή προέλευση (`manual_fab`, `call_linked`, `quick_add`) για ακριβέστερη ανάλυση καναλιού δημιουργίας.
- Η ροή δημιουργίας εκκρεμοτήτων από το FAB της οθόνης εργασιών μαρκάρεται πλέον ως `manual_fab`.
- Το πεδίο URL του Lansweeper προστέθηκε στο panel φίλτρων dashboard και πλέον αποθηκεύεται μόνιμα στη βάση (`app_settings`) ως Single Source of Truth.
- Η κάρτα KPI «Συνολικές κλήσεις» έγινε clickable και ανοίγει πλέον τον διάλογο αναφοράς Lansweeper.
- Προστέθηκε νέα μέθοδος repository για φόρτωση λίστας κλήσεων dashboard με την ίδια λογική φιλτραρίσματος που χρησιμοποιούν τα στατιστικά KPI.
- Στα εργαλεία απομακρυσμένης σύνδεσης με «Αρχείο προτύπου» και ενεργό `{FILE}`/`{file}`, η παράμετρος εξοπλισμού δέχεται πλέον διαδρομή αρχείου (όχι μόνο hostname/IP).
- Το πεδίο «Παράμετρος · <εργαλείο>» εμφανίζει δυναμικό hint «Αρχείο παραμέτρων πχ .rdp» όταν το εργαλείο περιμένει αρχείο.
- Η επίλυση στόχου και η εκκίνηση απομακρυσμένης σύνδεσης χρησιμοποιούν την παράμετρο αρχείου από το `remote_params` του εξοπλισμού, με διατήρηση fallback στη legacy συμπεριφορά.

---

## [0.10.0] - 2026-04-22

### Προστέθηκε

- Εξαγωγή χάρτη ορόφου ως PNG ή JPG (επεξεργασία): κουμπί λήψης δεξιά στη μπάρα, ορατότητα τμημάτων όπως στον χάρτη, προεπιλογή Λήψεις Windows και όνομα αρχείου = ορόφος
- Διάλογος «Τμήματα ορόφου» για προβολή/απόκρυψη τμημάτων στον χάρτη
- Μαζική αφαίρεση τμημάτων από το ενεργό φύλλο κατόψης (κατά την επεξεργασία)
- Γρήγορη αλλαγή επωνυμίας και χρώματος τμήματος μέσα από τον διάλογο
- Ένδειξη (badge) στο κουμπί όταν υπάρχουν κρυμμένα τμήματα σε όροφο
- Προσωρινή αποκάλυψη κρυμμένου τμήματος όταν εντοπίζεται από την αναζήτηση

### Διορθώθηκε

- Μετατροπή εξαγωγής χάρτη σε JPG: τα διαφανή σημεία αποδίδονται σε λευκό φόντο αντί για μαύρο (ώστε να παραμένουν ευανάγνωστες οι ετικέτες)
- Κατάρρευση στο Windows κατά το άνοιγμα του διαλόγου «Τμήματα ορόφου»: κοινός `ScrollController` για `Scrollbar` και `ListView` (το primary controller σε desktop δεν συνδεόταν με τη λίστα)

### Άλλαξε

- Διάλογος αποθήκευσης εξαγωγής χάρτη (Windows): δύο ξεχωριστές γραμμές φίλτρου PNG και JPEG αντί για μία συγχωνευμένη λίστα επεκτάσεων
- Ο διάλογος «Τμήματα ορόφου»: ξεχωριστά στοιχεία για επιλογή όλων (checkbox, λειτουργία επεξεργασίας) και μαζική ορατότητα χάρτη (εικονίδιο ματιού)
- Ο διάλογος «Τμήματα ορόφου» εμφανίζει αναζήτηση μόνο όταν η λίστα θα χρειαζόταν κύλιση (εκτίμηση ύψους γραμμών)
- Αναδιάταξη γραμμής εργαλείων προβολής χάρτη: στενότερη λίστα φύλλων ώστε να χωρά το νέο κουμπί
- Ο περιηγητής χάρτη και ο σχεδιαστής ορόφων αγνοούν τα κρυμμένα τμήματα σε σχεδίαση/hit-test
- Εσωτερικές βελτιώσεις: λιγότερα περιττά επανασχεδιάσματα χάρτη και πιο σταθερή ροή αναζήτησης

---

## [0.9.0] - 2026-04-21

### Προστέθηκε

- Χάρτης ορόφων κτιρίου για επισκόπηση τμημάτων στον Κατάλογο
- Μικρός χάρτης επισκόπησης (mini-map) στην οθόνη χάρτη
- Ολοκληρωμένη διάταξη ετικετών στον χάρτη και βελτιώσεις αναζήτησης

### Άλλαξε

- Αναβάθμιση εξαρτήσεων σε πιο πρόσφατες συμβατές εκδόσεις
- Βελτιώσεις στο συγχρονισμό δεδομένων Καταλόγου και Τμημάτων

---

## [0.8.0] - 2026-04-17

### Προστέθηκε

- Κάρτες στην οθόνη Κλήσεων για ταχύτερη επισκόπηση
- Γρήγορη πρόσβαση σε εργαλεία απομακρυσμένης διαχείρισης από την οθόνη Κλήσεων
- Αναφορές ιστορικού εφαρμογής και εργαλείων απομακρυσμένης σύνδεσης

### Άλλαξε

- Αναδιάταξη και καθαρισμός της οθόνης Κλήσεων
- Διάταξη καρτελών και ιστορικού για ευκολότερη πλοήγηση

### Διορθώθηκε

- Διορθώσεις εμφάνισης και σταθερότητας μετά από αλλαγές διάταξης

---

## [0.7.0] - 2026-04-07

### Προστέθηκε

- Επεξεργασία κατηγοριών και καταχωρήσεων στον Κατάλογο
- Βελτιωμένη διαχείριση λεξικού και ορολογίας

### Άλλαξε

- Διαχωρισμός και αναδιοργάνωση του database helper για ευκολότερη συντήρηση
- Ρυθμίσεις και συμπεριφορά εκκρεμοτήτων

### Διορθώθηκε

- Διορθώσεις ορθογραφίας σε ονόματα τμημάτων και καταχωρήσεις

---

## [0.6.0] - 2026-03-31

### Προστέθηκε

- Δημιουργία αντιγράφου ασφαλείας (backup) της βάσης δεδομένων
- Εργαλεία συντήρησης βάσης και έλεγχοι ακεραιότητας
- Ενημέρωση του εκτελέσιμου παραθύρου για desktop

### Άλλαξε

- Βελτίωση ροής αρχικοποίησης και χειρισμού σφαλμάτων βάσης

---

## [0.5.0] - 2026-03-27

### Προστέθηκε

- Ορθογραφικός έλεγχος και προτάσεις διόρθωσης σε ελληνικά κείμενα
- Εντοπισμός ορφανών τηλεφώνων και εξοπλισμού που δεν αντιστοιχούν σε καταχωρήσεις

### Άλλαξε

- Ενοποίηση κανόνων επικύρωσης για τηλεφωνία και εξοπλισμό

---

## [0.4.0] - 2026-03-23

### Προστέθηκε

- Νέος πίνακας και λογική για Τμήματα στον Κατάλογο
- Ξεχωριστή καρτέλα Τμημάτων με διαχείριση και προβολή δεδομένων
- Βελτιώσεις στην καρτέλα Ιστορικού και στην καρτέλα Εκκρεμοτήτων

### Άλλαξε

- Προσαρμογή κώδικα υπαρχόντων οθονών για ενσωμάτωση τμημάτων

### Διορθώθηκε

- Ενίσχυση χειρισμού σφαλμάτων και ευστάθειας πλοήγησης

---

## [0.3.0] - 2026-03-16

### Προστέθηκε

- Καρτέλα Ιστορικού κλήσεων με φίλτρα και λεπτομέρειες
- Καρτέλα Εκκρεμοτήτων για παρακολούθηση ανοιχτών θεμάτων
- Αυτόματη συμπλήρωση πεδίων στην οθόνη Κλήσεων

### Άλλαξε

- Διάταξη και συμπεριφορά των καρτελών Κλήσεων και Εκκρεμοτήτων

---

## [0.2.0] - 2026-03-14

### Προστέθηκε

- Καρτέλα Εξοπλισμού στον Κατάλογο με αναζήτηση και σύνδεση με κλήσεις
- Σύντομες ενέργειες απομακρυσμένης σύνδεσης (AnyDesk, VNC) όπου εφαρμόζεται

### Άλλαξε

- Επεξεργασία ονομάτων χρηστών και εξοπλισμού για συνέπεια στον Κατάλογο

---

## [0.1.0] - 2026-03-12

### Προστέθηκε

- Αρχικοποίηση τοπικής βάσης SQLite και ροή πρώτης εκκίνησης
- Αυτόματη συμπλήρωση για ταχύτερη καταγραφή κλήσεων
- Βασική οθόνη Κλήσεων και ρουτίνα καταχώρησης

