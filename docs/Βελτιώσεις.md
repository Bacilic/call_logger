Το κεφάλαιο αυτό λειτουργεί ως οδηγός μελλοντικής υλοποίησης και σημειωματάριο του Διευθυντή Έργου. Υπάρχουν **δύο κωδικοί**: **Δ** = Διόρθωση (κάτι είναι ήδη λάθος), **Π** = Προσθήκη (κάτι δεν υπάρχει ακόμη). *(Οι κωδικοί «Ε», «Σ» και «Κ» καταργήθηκαν — δες παρακάτω.)*

> **Ο κωδικός δηλώνει ΘΕΣΗ ΠΡΟΤΕΡΑΙΟΤΗΤΑΣ, όχι σταθερή ταυτότητα.** Το Δ3 είναι «αυτό που πρέπει να λυθεί τρίτο», όχι πάντα το ίδιο θέμα. Σε κάθε αναδιάταξη οι κωδικοί ξαναδίνονται 1→Ν χωρίς κενά μέσα σε κάθε κατηγορία. Γι' αυτό οι καταχωρήσεις **παραπέμπουν η μία στην άλλη με περιγραφή, όχι με αριθμό** (ο αριθμός μετακινείται). Ροή εργασίας: ο Διευθυντής γράφει πρόχειρα στο «📝 Πρόχειρο» παρακάτω· ο βοηθός τα σουλουπώνει σε κωδικοποιημένες καταχωρήσεις.

> **Κάθε ενότητα ξεκινά με το ΝΟΣΟΚΟΜΕΙΟ.** Εκεί μπαίνει ό,τι δεν μπορεί να **επαληθευτεί** στο σπίτι: δίκτυο, Lansweeper, διακομιστές και εκτυπωτές, κοινόχρηστη βάση με συναδέλφους, πραγματικός εξοπλισμός, βάση Λάμπας. Ο κώδικας μπορεί κάλλιστα να γραφτεί σπίτι — η δοκιμή γίνεται επί τόπου. Ακολουθεί το **Σπίτι** με όλα τα υπόλοιπα. Έτσι ο χρόνος στη βάρδια πάει μόνο σε ό,τι απαιτεί το πραγματικό περιβάλλον. **Η αρίθμηση είναι συνεχής σε όλη την ενότητα**: το Νοσοκομείο παίρνει τους πρώτους αριθμούς, το Σπίτι συνεχίζει από εκεί.

> **Δύο σημάδια μέσα στις καταχωρήσεις, αντί για ξεχωριστές ενότητες** (17/09/2026):
> 1. **«Απόφαση που λείπει:»** — η καταχώρηση δεν μπορεί να ξεκινήσει σήμερα, γιατί δεν έχει αποφασιστεί ακόμη *τι* θέλουμε. Το `/fix` ανοίγει πρώτα συζήτηση. **Καμία τέτοια καταχώρηση δεν παίρνει τη θέση 1** — ο πρώτος αριθμός κάθε κατηγορίας πρέπει να είναι κάτι εκτελέσιμο σήμερα.
> 2. **«Δεν το βλέπει ο χρήστης:»** — διόρθωση στον κώδικα, όχι στην οθόνη. Τίποτα δεν χαλάει σήμερα για όποιον χρησιμοποιεί την εφαρμογή· διορθώνεται για να μη γεννήσει αυριανό σφάλμα. Τέτοιες καταχωρήσεις είναι ιδανικές για **πληρωμή εν κινήσει**: όταν μια άλλη δουλειά ανοίγει το ίδιο αρχείο, γίνονται μαζί.

> **Δεν υπάρχουν υποενότητες προτεραιότητας** («Υψηλή», «Μεσαία», «Χαμηλή») — καταργήθηκαν 06/09/2026. Τη σειρά τη λέει η αρίθμηση, οπότε ένα θέμα που γίνεται πιο επείγον αλλάζει αριθμό, όχι κεφαλίδα.

> **Η σειρά μέσα σε κάθε κατηγορία είναι κανόνας, όχι αίσθημα.**
> - **Διορθώσεις:** σιωπηλή απώλεια ή αλλοίωση δεδομένων → κατάρρευση ή μπλοκάρισμα → λάθος ορατό αποτέλεσμα → η οθόνη λέει ψέματα → ενόχληση → «δεν το βλέπει ο χρήστης».
> - **Προσθήκες:** ό,τι ξεκλειδώνει άλλα → καθημερινή χρήση → περιστασιακή → μεγάλα έργα.

> **Στο τέλος του αρχείου ζει ό,τι ΔΕΝ παίρνει αριθμό προτεραιότητας:** είτε επειδή επαναλαμβάνεται και δεν αφαιρείται ποτέ (Συντήρηση κώδικα), είτε επειδή περιμένει γεγονός που δεν ελέγχουμε πότε θα συμβεί (Μηδενισμός έκδοσης βάσης). Καθεμιά δηλώνει ρητά **πότε τρέχει**.

> Κεντρική ιδέα: Ένα σφάλμα που βρίσκεις είναι σύμπτωμα, όχι ο στόχος. Αντί να το μπαλώσω, το χρησιμοποιώ ως σημείο εισόδου για μια οριοθετημένη (στοχευμένη, όχι καθολική) ιχνηλάτηση.

> Έτσι φτιάχτηκε η Ικανότητα Fix. Σύνταξη: `/fix` όνομα σφάλματος

> **Αναδόμηση 17/09/2026 — τέσσερις ενότητες έγιναν δύο, και το Πρόχειρο άδειασε.** Οι «Προς συζήτηση» και οι «Προτάσεις Claude» καταργήθηκαν: ήταν αποθήκες αναβολής, όχι είδη δουλειάς. Ό,τι έχαναν έγινε **γραμμή μέσα στην καταχώρηση** — «Απόφαση που λείπει» και «Δεν το βλέπει ο χρήστης». **Η ερώτηση για τα δικαιώματα αποδείχθηκε το σοβαρότερο εύρημα της ημέρας** και μπήκε πρώτη: η αλλαγή δεν φτάνει ποτέ στον άλλο σταθμό, ούτε όταν **αφαιρείς** πρόσβαση. **Μία ιδέα εγκαταλείφθηκε από τον Διευθυντή:** ο έλεγχος του ιστορικού εκδόσεων από δεύτερη ΤΝ — το changelog είναι σωρευτικό ιστορικό, δεν λέει τι ισχύει σήμερα. **Η σημείωση που είχε μείνει αναπάντητη** (η λέξη «Επώνυμο» που κρύβεται) λύθηκε αυθημερόν με στιγμιότυπο: έφταιγε η κύλιση του διαλόγου, όχι η ετικέτα.

> **Αναδόμηση 13/09/2026 — οι Διορθώσεις ξαναγέμισαν από τον έλεγχο πεδίου των αντιγράφων.** Πέντε από τις έξι αφορούσαν την επαναφορά από αντίγραφο, με κοινή ρίζα: ο κριτής επαναφοράς δεν κοιτά καθόλου την έκδοση του αρχείου. **Μία σημείωση αποδείχθηκε λανθασμένη και σβήστηκε:** ο έλεγχος επικοινωνίας πριν από την επανεκκίνηση διακομιστή **υπάρχει ήδη** — ο διάλογος σαρώνει συνεδρίες μόλις ανοίξει και δείχνει σφάλμα αν ο διακομιστής δεν απαντά. Η ένδειξη πλήθους επιλεγμένων αποδείχθηκε **έξι οθόνες** με το ίδιο μοτίβο, όχι μία.

> **Αναδόμηση 06/09/2026 — η διαίρεση Νοσοκομείο / Σπίτι.** Και οι τέσσερις τότε ενότητες χωρίστηκαν με κριτήριο την **επαλήθευση**, όχι τον τόπο συγγραφής: 10 από τις 34 καταχωρήσεις απαιτούν το πραγματικό περιβάλλον. **Ό,τι αφορά τη Λάμπα πήγε στο Σπίτι** — η `lampa.db` είναι πλέον πλήρης και τοπική. **Ένα εύρημα της ίδιας μέρας:** ο κανόνας «εξοπλισμός χωρίς τμήμα» έβγαζε **55 ψευδή ευρήματα και μηδέν αληθινά**, μετρημένο στη ζωντανή βάση. Καταργήθηκαν οι υποενότητες προτεραιότητας.

## Πρόχειρο (νέες σημειώσεις εδώ — προς σουλούπωμα) 📝

- **Δύο φόρμες κρατούν ακόμη χειροκίνητο το περιθώριο της πρώτης ετικέτας.**
  *Εύρημα 17/09, μετά την ενοποίηση των υπόλοιπων διαλόγων.*

  **Δεν το βλέπει ο χρήστης:** και οι δύο δείχνουν σωστά σήμερα — έχουν το σωστό κενό, απλώς γραμμένο με το χέρι αντί για το κοινό `DialogScrollableContent` που το κουβαλά μαζί με τον λόγο του.

  **Γιατί αξίζει:** ακριβώς αυτή η χειροκίνητη αντιγραφή ήταν η αιτία που το περιθώριο ξεχάστηκε σε τρεις άλλους διαλόγους. Όσο υπάρχουν δύο τρόποι, ο επόμενος διάλογος θα αντιγράψει τον λάθος.

  **Πού:** `department_form_dialog.dart:844-848`, `equipment_form_dialog.dart:760-764`

  **Ζητούμενο:** αντικατάσταση του `SingleChildScrollView` + εσωτερικού `Padding(top: 6)` με το κοινό widget. Θέλει ξαναστοίχιση ολόκληρης της στήλης πεδίων, γι' αυτό δεν έγινε μαζί — το diff θα γινόταν αδιάβαστο.

- **Η αναζήτηση αδειάζει αν γράψεις ολόκληρη λέξη που τελειώνει σε «ς».**
  *Εύρημα 17/09 κατά την ιχνηλάτηση του φίλτρου καλούντα· μετρημένο στη ζωντανή βάση του σπιτιού.*

  **Σενάριο:** στην αναζήτηση του Ιστορικού γράφεις «Δρόσος» → **0 αποτελέσματα**. Γράφεις «Δρόσο», χωρίς το τελικό γράμμα → **14**.

  **Τι πάει στραβά:**
  1. **Ο κανόνας άλλαξε, το αποθηκευμένο κείμενο όχι.** Στις 30/06/2026 η κανονικοποίηση αναζήτησης άρχισε να μετατρέπει το τελικό «ς» σε «σ». Το ευρετήριο κάθε κλήσης χτίζεται **μία φορά**, τη στιγμή της καταγραφής — όσες γράφτηκαν πριν από εκείνη την ημερομηνία κρατούν «δροσος» ενώ η πληκτρολόγηση ψάχνει πλέον «δροσοσ». — `search_text_normalizer.dart:11`, `calls_search_index.dart:86`
  2. **Δεν είναι λίγες.** Στη δοκιμαστική βάση: **21 από 38** κλήσεις και **36 από 52** εκκρεμότητες κρατούν ευρετήριο παλιάς μορφής.
  3. **Ο Έλεγχος δεδομένων δεν το βλέπει.** Ελέγχει μόνο αν το ευρετήριο είναι **κενό**, όχι αν είναι χτισμένο με παλιούς κανόνες. — `database_integrity_diagnostics.dart:459-490`

  **Συμβόλαιο:** «όταν αλλάζει ο κανόνας κανονικοποίησης, κάθε αποθηκευμένο ευρετήριο που χτίστηκε με τον παλιό ξαναχτίζεται — αλλιώς η αναζήτηση ψάχνει με άλλη γλώσσα από αυτήν που έγραψε».

  **Ζητούμενο:** ο Έλεγχος δεδομένων να συγκρίνει το αποθηκευμένο ευρετήριο με αυτό που θα έχτιζε σήμερα (ο μηχανισμός επαναδόμησης υπάρχει ήδη: `rebuildSearchIndexForCallId`) και να προσφέρει διόρθωση. Ίδιο και για τις εκκρεμότητες.

- **Ο Εξοπλισμός του Ιστορικού έμεινε ελεύθερο κείμενο, ενώ τα δύο διπλανά φίλτρα είναι λίστες.**
  *Ανοιχτό ερώτημα 17/09, μετά τη μετατροπή του «Καλούντας» σε κλειστή λίστα.*

  **Απόφαση που λείπει:** ομοιομορφία ή μερική αναζήτηση; Σήμερα το πεδίο ταιριάζει **μερικό** κωδικό, ώστε το «501» να βρίσκει το «5010» — στα Στατιστικά το ίδιο φίλτρο είναι κλειστή λίστα. **Δεν είναι σφάλμα:** ο εξοπλισμός δένει σωστά στον κωδικό του, όχι στο ευρετήριο. — `history_screen.dart:458`, `dashboard_filter_bar.dart:186`

## Α. Διορθώσεις

### Νοσοκομείο

- **Δ1 — Η αλλαγή δικαιωμάτων δεν φτάνει ποτέ στον άλλο σταθμό.**
  *Ερώτημα Διευθυντή 17/09/2026· ιχνηλατήθηκε στον κώδικα αυθημερόν και αποδείχθηκε σφάλμα, όχι απορία.*

  **Το σενάριο του Διευθυντή:** ο διαχειριστής ανοίγει στον Βλάση το «Ιστορικό Εφαρμογής». Ο Βλάσης δουλεύει στον διπλανό υπολογιστή με την εφαρμογή ανοιχτή. **Δεν βλέπει τίποτα** — ούτε τώρα, ούτε σε δέκα λεπτά, ούτε ποτέ, όσο δεν κλείσει την εφαρμογή.

  **Γιατί:** ο σταθμός κρατά **στιγμιότυπο** του προφίλ στη μνήμη (`CurrentOperator.active`), γεμισμένο τη στιγμή που ο χρήστης συστήθηκε. Τα δικαιώματα ζουν πάνω σε αυτό το στιγμιότυπο (`permissionOverrides`), και το μόνο σημείο που το ανανεώνει είναι η **αναγνώριση ταυτότητας** — δηλαδή εκκίνηση εφαρμογής ή «Αλλαγή χρήστη». — `current_operator.dart:35`, `operator_identity.dart`, `permission_service.dart:39`

  **Η αντίστροφη φορά είναι χειρότερη:** όταν ο διαχειριστής **αφαιρεί** πρόσβαση, ο Βλάσης συνεχίζει να βλέπει το Ιστορικό Εφαρμογής μέχρι να κλείσει την εφαρμογή. Η προστασία υπάρχει και δεν ισχύει.

  **Δεν είναι θεωρητικό — δύο δικαιώματα είναι ήδη κλειστά από προεπιλογή:** «Ιστορικό Εφαρμογής» και «Πλήρες αντίγραφο ασφαλείας». Ο διαχειριστής **πρέπει** να τα ανοίξει ρητά, άρα το σενάριο συμβαίνει σε κάθε νέο προφίλ. — `app_permission.dart:127`, `app_permission.dart:101`

  **Ο φύλακας φρεσκάδας ΔΕΝ το καλύπτει.** Ο κύκλος των 12 δευτερολέπτων ανανεώνει εκκρεμότητες, τμήματα, κατάλογο, κατηγορίες και κλήσεις — η ταυτότητα **δεν είναι στη λίστα**. — `shared_database_refresh.dart:40`

  **Συμβόλαιο:** «ό,τι αλλάζει ο διαχειριστής για κάποιον άλλον, το μαθαίνει εκείνος χωρίς να κλείσει την εφαρμογή.»

  **Ζητούμενο:** η ταυτότητα του συνδεδεμένου χρήστη να ξαναδιαβάζεται στον ίδιο κύκλο φρεσκάδας, και η αλλαγή να ακυρώνει τις όψεις που κρίνονται από δικαίωμα — με τον ίδιο μηχανισμό που ήδη υπάρχει για την «Αλλαγή χρήστη». **Κανένα νέο χρονόμετρο**: το σημείο είναι ένα και υπάρχει.

  **Γιατί εδώ:** η επαλήθευση θέλει **δύο σταθμούς** στην κοινόχρηστη βάση — έναν που αλλάζει και έναν που περιμένει.

- **Δ2 — Η αλλαγή κύριου αιτήματος Lansweeper είναι λειτουργία που δεν καλεί κανείς.**
  *Εύρημα 25/08 κατά τη σάρωση αδελφών ροών των φρουρών κοινόχρηστης βάσης, επιβεβαιωμένο στον κώδικα.*

  **Τι συμβαίνει:** η μέθοδος που ορίζει σκέτο το κύριο αίτημα μιας κλήσης δεν καλείται από πουθενά στην εφαρμογή — μόνο από δύο αρχεία ελέγχων, που έτσι φυλάνε νεκρό κώδικα. — `calls_lansweeper_repository.dart:287`, `lansweeper_state_audit_test.dart:145`, `lansweeper_ticket_search_test.dart:99`

  **Δεν το βλέπει ο χρήστης:** καμία οθόνη δεν αλλάζει. Είναι εκκαθάριση νεκρού κώδικα και των ελέγχων που τον κρατούν ζωντανό.

  **Ζητούμενο:** αφαίρεση της μεθόδου και των ελέγχων της (ο έλεγχος ζει και πεθαίνει με τη λειτουργία του), ή —αν κριθεί χρήσιμη— σύνδεσή της με πραγματική ενέργεια της διεπαφής.

- **Δ3 — Η ροή αποστολής ticket είναι μία μέθοδος 231 γραμμών.**
  *Παρατήρηση ποιότητας 03/09/2026, κατά τον έλεγχο της ταχύτητας καταχώρησης.*

  **Τι ισχύει:** η `submitTicketWorkflow` κρατά μέσα της και τα πέντε βήματα του API — επαλήθευση αιτούντα, δημιουργία, σύνδεση εξοπλισμού, σημείωση με την εναλλακτική της, αλλαγή κατάστασης — μαζί με τους χειρισμούς αποτυχίας του καθενός.
  — `lansweeper_sync_service.dart:188-418`

  **Γιατί ενοχλεί:** κάθε νέο βήμα ή νέος χειρισμός σφάλματος μεγαλώνει την ίδια μέθοδο, και η σειρά των βημάτων δεν φαίνεται με μια ματιά — φαίνεται μόνο διαβάζοντας 231 γραμμές.

  **Δεν το βλέπει ο χρήστης:** η ροή δουλεύει σωστά και στέλνει πραγματικά αιτήματα σε ζωντανό σύστημα. Η διάσπαση σε μέθοδο ανά βήμα είναι καθαρά δουλειά συντήρησης, με υπαρκτό ρίσκο παλινδρόμησης — γίνεται μόνο κατόπιν ρητής απόφασης, όχι παρεμπιπτόντως.

  **Γιατί εδώ:** η επαλήθευση απαιτεί ζωντανό Lansweeper — μια διάσπαση που «φαίνεται σωστή» δεν αποδεικνύεται χωρίς πραγματική αποστολή.

### Σπίτι

- **Δ4 — Η οθόνη λέει «τοπικές ρυθμίσεις» για ρύθμιση που είναι ΚΟΙΝΗ — και σβήνει το Ιστορικό όλων.**
  *Εύρημα 17/09 κατά την απογραφή της υπογραφής χρήστη, επιβεβαιωμένο στον κώδικα.*

  **Σενάριο:** ανοίγετε «Βάση Δεδομένων → Αυτόματη εκκαθάριση audit». Από κάτω διαβάζετε «**τοπικές ρυθμίσεις**· όχι στο λεξικό», βάζετε όριο 90 ημερών και το ενεργοποιείτε νομίζοντας ότι αφορά τον δικό σας υπολογιστή. Στην πραγματικότητα η ρύθμιση είναι κοινή, και η επόμενη εκκίνηση **οποιουδήποτε** σταθμού σβήνει το Ιστορικό **όλων** — οριστικά.

  **Τι πάει στραβά:** το βοηθητικό κείμενο έμεινε πίσω από τη μετάπτωση των ρυθμίσεων σε τρία στρώματα. Το κλειδί δηλώνεται ρητά **κοινό**, με γραμμένο τον λόγο: «καθαρίζει το ΚΟΙΝΟ Ιστορικό· όσο ζούσε ανά μηχάνημα, ο χρήστης με την πιο σφιχτή ρύθμιση έσβηνε τις εγγραφές όλων». — `database_maintenance_sections.dart:472`, `shared_settings.dart:30-35`

  **Συμβόλαιο:** «όταν αλλάζει η εμβέλεια μιας ρύθμισης, αλλάζει και το κείμενο που την περιγράφει στην οθόνη.»

  **Ζητούμενο:** διόρθωση του βοηθητικού κειμένου ώστε να λέει ότι η πολιτική είναι κοινή για όλους τους σταθμούς και ότι η εκκαθάριση αγγίζει το Ιστορικό όλων. Καμία αλλαγή στη συμπεριφορά — μόνο στο τι υπόσχεται η οθόνη.

  Δένει με τη διαβάθμιση της εκκαθάρισης ανά είδος εγγραφής στις Προσθήκες: όσο το κείμενο λέει ψέματα, ο διακόπτης δεν πρέπει να πατηθεί από κανέναν.

- **Δ5 — Το κοινό τηλέφωνο ακολουθεί σιωπηλά, όταν η μεταφορά γίνεται ΜΕΣΑ στο νοσοκομείο.**
  *Εύρημα 16/09, μετρημένο στη ζωντανή βάση. Το σκέλος «προς εταιρεία» διορθώθηκε αυθημερόν.*

  **Σενάριο:** ο υπάλληλος κρατά το **2534**, που το κρατούν άλλοι **δύο** συνάδελφοι. Μετακινείται σε **άλλο τμήμα του νοσοκομείου** και ο χρήστης απαντά «τα τηλέφωνα μένουν στο παλιό τμήμα».

  **Τι γίνεται:** το 2534 **δεν** μένει — ο κανόνας το εμποδίζει σωστά («το χρησιμοποιεί και ο Χ»), οπότε ακολουθεί τον υπάλληλο. Η απάντηση αγνοείται **σιωπηλά** για αυτόν τον αριθμό. — `bulk_user_actions.dart:101-108`

  **Τι καλύπτεται ήδη — μη το ξαναζητήσεις:** προς **εταιρεία ή εξωτερική μονάδα** το εσωτερικό αποδεσμεύεται πλέον πάντα και αναγγέλλεται ονομαστικά. Το κενό αφορά μόνο τη μεταφορά **μέσα** στο νοσοκομείο.

  **Μέγεθος σήμερα:** 9 τηλέφωνα μοιράζονται σε 2–3 υπαλλήλους.

  **Συμβόλαιο:** «Ό,τι δεν μπόρεσε να γίνει όπως ζητήθηκε, λέγεται.»

  **Ζητούμενο:** ο λόγος της εξαίρεσης να φαίνεται στον χρήστη, όπως ήδη φαίνεται στη μαζική μεταφορά με την προεπισκόπηση.

- **Δ6 — Ο φρουρός διένεξης κατηγορεί τον ίδιο τον χρήστη για «ξένη» αλλαγή.**
  *Εύρημα 16/09, αναπαράχθηκε ΔΥΟ φορές στη ζωντανή εφαρμογή — σταθερό, όχι τυχαίο.*

  **Σενάριο:** μετακινείτε τη **Βασιλική Κίτσιου** από το Γραφείο Κίνησης στη DataMed. Απαντάτε ότι το τηλέφωνο μένει πίσω, και αμέσως μετά εμφανίζεται «**Κάποιος πρόλαβε** — κάποιος άλλος άλλαξε αυτή την εγγραφή· τι άγγιξε: τηλέφωνα».

  **Κανείς άλλος δεν την άγγιξε.** Η αποδέσμευση του τηλεφώνου γίνεται από την **ίδια τη ροή αποθήκευσης**, πριν γραφτεί η καρτέλα· ο φρουρός διαβάζει τη φρέσκια γραμμή και τη βλέπει αλλαγμένη σε σχέση με την αφετηρία που κρατά η οθόνη.

  **Τι κοστίζει:** ο χρήστης καλείται να διαλέξει ανάμεσα σε «Κράτα τη δική μου αλλαγή» και «Ακύρωσε την αλλαγή μου» για μια διένεξη που δεν υπάρχει — με προεπιλεγμένη την **ακύρωση της δικής του δουλειάς**.

  **Συμβόλαιο:** «Ο φρουρός συγκρίνει με ό,τι έγραψαν ΑΛΛΟΙ — οι δικές μας εγγραφές του ίδιου κύκλου ανανεώνουν την αφετηρία.»

  **Ζητούμενο:** η αφετηρία της σύγκρισης να ανανεώνεται μετά τις εγγραφές που κάνει η ίδια η αποθήκευση (αποδέσμευση τηλεφώνων, εξοπλισμού), ώστε ο φρουρός να μένει για τις πραγματικές ξένες αλλαγές.

- **Δ8 — Η λίστα «απαγορευμένοι φάκελοι σε πακέτο ενημέρωσης» υπάρχει δύο φορές.**
  *Παρατήρηση ποιότητας 13/09/2026, κατά την ιχνηλάτηση της διαφυγής φακέλου.*

  **Τι συμβαίνει:** η ίδια πεντάδα (`Data Base/`, `images/`, `maps_images/`, `dictionaries/`, `logs/`) και σχεδόν ο ίδιος βρόχος ελέγχου είναι γραμμένα αυτούσια σε δύο αρχεία:
  1. **Πριν τη δημοσίευση** — `features/database/debug/release_publisher_service.dart:119`
  2. **Πριν την εγκατάσταση** — `core/updates/update_installer_service.dart:70`

  **Δεν το βλέπει ο χρήστης:** οι δύο λίστες είναι σήμερα ταυτόσημες και οι δύο πύλες δουλεύουν σωστά.

  **Γιατί αξίζει:** είναι δύο αντίγραφα ενός κανόνα. Αν κάποτε προστεθεί φάκελος δεδομένων χρήστη, θα ενημερωθεί το ένα και όχι το άλλο — και η ασυμφωνία θα φανεί μόνο όταν ένα πακέτο περάσει τη δημοσίευση και κοπεί στην εγκατάσταση, ή το αντίστροφο.

  **Ζητούμενο:** μία λίστα με έναν κάτοχο, που τη διαβάζουν και οι δύο πύλες.

  Ίδια οικογένεια με την επόμενη καταχώρηση («γραμμένο τρεις φορές»): ένας κανόνας χωρίς σπίτι, αντιγραμμένος σε κάθε καλούντα.

- **Δ9 — Το «ποιος πρόλαβε και πότε» είναι γραμμένο τρεις φορές.**
  *Παρατήρηση ποιότητας 25/08 κατά την υλοποίηση του φρουρού Lansweeper.*

  **Τι συμβαίνει:** κάθε φρουρός μπαγιάτικης εγγραφής ανοίγει την ίδια πρόταση — «Ο χρήστης «Χ» <κάτι> στις 13:10» — και ο μορφοποιητής της ώρας είναι αντιγραμμένος αυτούσιος σε τρία σημεία. — `call_save_conflict.dart:132`, `operator_save_conflict.dart:86`, `task_conflict_summary.dart:33`

  **Πού πάει:** ο τέταρτος φρουρός (Lansweeper) δεν πρόσθεσε τέταρτο αντίγραφο· χρησιμοποιεί κοινό βοηθό. — `conflict_actor_text.dart`

  **Δεν το βλέπει ο χρήστης:** τα τρία μηνύματα βγαίνουν σήμερα σωστά. Η ενοποίηση αφορά το τι θα γίνει την επόμενη φορά που θα αλλάξει η διατύπωση.

  **Ζητούμενο:** να περάσουν και τα τρία υπάρχοντα στον κοινό βοηθό, ώστε μια διόρθωση στη διατύπωση να γίνεται μία φορά.

- **Δ10 — Η βοηθητική συνάρτηση που δίνει το όνομα χρήστη για το Ιστορικό δέχεται όρισμα που αγνοεί.**
  *Εύρημα 16/09, κατά τη δουλειά για το «ποιος έκλεισε την εκκρεμότητα».*

  **Τι ισχύει:** η `AuditService.performingUser` δεχόταν κάποτε σύνδεση στη βάση· σήμερα απαντά από τη μνήμη και **δεν την κοιτά καν**. Το όρισμα έμεινε προαιρετικό ώστε να μη χρειαστεί μαζικό ξαναγράψιμο, και **33 σημεία κλήσης εξακολουθούν να το περνούν**. — `audit_service.dart:25`

  **Δεν το βλέπει ο χρήστης:** το όνομα που γράφεται στο Ιστορικό είναι σωστό. Η ζημιά είναι στην ανάγνωση του κώδικα.

  **Γιατί ενοχλεί:** όποιος διαβάσει ένα σημείο κλήσης συμπεραίνει ότι η απάντηση εξαρτάται από τη συγκεκριμένη συναλλαγή — δηλαδή το αντίθετο από ό,τι συμβαίνει.

  **Ζητούμενο:** να φύγει η παράμετρος και να καθαριστούν τα 33 σημεία κλήσης. Καθαρά μηχανικό — ο μεταγλωττιστής δείχνει κάθε σημείο.

- **Δ11 — Επτά αδρανή διπλόγραφα ονομάτων προσωπικών ρυθμίσεων.**
  *Υπόλοιπο της 17/09 — οι έξι ζωντανές παραβιάσεις διορθώθηκαν αυθημερόν.*

  **Τι μένει:** επτά σταθερές γράφουν για δεύτερη φορά το όνομα ενός προσωπικού κλειδιού: έξι του Gemini/Lansweeper στο `settings_repository.dart` και μία του Λεξικού στο `lexicon_list_filters_provider.dart`.

  **Δεν το βλέπει ο χρήστης:** καμία δεν χρησιμοποιείται στην εφαρμογή — μόνο σε ελέγχους, ή πουθενά.

  **Γιατί αξίζει:** είναι ακριβώς η παγίδα που γέννησε τις έξι που διορθώθηκαν — έτοιμο ωμό όνομα, έτοιμο για χρήση από τη λάθος πόρτα.

  **Ζητούμενο:** να σβηστούν οι νεκρές και οι δύο του Gemini να δείχνουν στον κατάλογο. Καθαρά μηχανικό. Η λίστα χρέους του `profile_settings_single_door_test.dart` τις κρατά — με το σβήσιμο αδειάζει και εκείνη.

- **Δ12 — Λογική που ζει μέσα σε widget αντί σε υπηρεσία (δύο επιβεβαιωμένες περιπτώσεις).**
  *Ευρήματα 25-26/07 — φρέσκιες αναφορές 13/08.*

  **Συμβόλαιο:** «Ενορχήστρωση και σύνθεση κειμένου προς τον χρήστη είναι λογική — ζει σε υπηρεσία, όχι σε `build`.»

  **Δεν το βλέπει ο χρήστης:** και οι δύο ροές δουλεύουν σωστά σήμερα.

  1. **Η οθόνη σφάλματος ξαναγράφει την ενορχήστρωση εναλλαγής βάσης.** Τρεις ροές (νέα βάση, επιλογέας, πρόσφατη διαδρομή) ξαναγράφουν η καθεμιά την ίδια τετράδα βημάτων — δείκτης προόδου, `setAndVerifyDatabasePath`, διάλογος αποτυχίας, φινάλε — ενώ ο κοινός εκτελεστής `runDatabasePathSwitch` υπάρχει και την επιβάλλει με τεσταρισμένα hooks. **Χαμηλό ρίσκο (επαλήθευση 04/08):** το φινάλε περνά κανονικά από την ενιαία ολοκλήρωση μέσω `onRetry` — αυτό που διπλογράφεται είναι μόνο η ενορχήστρωση διεπαφής. — `database_error_screen.dart:250-345`
  2. **Ο διάλογος τμημάτων συνθέτει μήνυμα μέσα στο widget.** Η διαγραφή τμήματος χτίζει το κείμενο του snackbar (ενικός/πληθυντικός + τμήμα μεταφορών) μέσα στο widget, ενώ η αδελφή ροή των υπαλλήλων έχει `userDeletionSummaryMessage` σε δική της υπηρεσία με unit τεστ. Ζητούμενο: εξαγωγή σε `department_deletion_messages.dart` κατά το ίδιο πρότυπο. — `departments_tab.dart:978-1006` / `user_deletion_messages.dart:74`

- **Δ13 — Δύο μέθοδοι-τέρατα περιμένουν διάσπαση.**
  *Ενημέρωση 13/08: η «Σύνθεση Φάση 2» της Ομάδας Α ΟΛΟΚΛΗΡΩΘΗΚΕ 28–29/07 — κανένα part/mixin-υπόλειμμα διάσπασης στο `lib/`.*

  **Δεν το βλέπει ο χρήστης:** και οι δύο δουλεύουν σωστά. Το κόστος το πληρώνει όποιος πάει να τις αλλάξει.

  1. **Ο υπολογισμός των στατιστικών του πίνακα ελέγχου** — ~500 γραμμές σε ΜΙΑ μέθοδο. — `calls_repository_dashboard.dart` (`getDashboardStatistics`)
  2. **Το δέντρο της οθόνης της Αναφοράς Lansweeper** (παρατήρηση 27/08, κατά την υλοποίηση της υποβολής ticket). Οι ρόλοι έχουν ήδη βγει — έξι συνεργάτες Σύνθεσης (καταχώρηση, γνώση, ΤΝ, περιηγητής, ρυθμίσεις, αποθήκευση στην κλήση)· **δεν** είναι αυτό το πρόβλημα. Το πρόβλημα είναι ένα `build` ~500 γραμμών με ένθεση που ξεπερνά τα 20 επίπεδα: στο βάθος της, μια γραμμή κώδικα χωράει τρεις λέξεις, και η προσθήκη δύο παραμέτρων στη φόρμα απαίτησε ανάγνωση 100 γραμμών για να βρεθεί το σημείο. Ζητούμενο: εξαγωγή των ενοτήτων του δέντρου σε δικά τους widgets (λίστα κλήσεων, στήλη φόρμας, μπάρα ενεργειών) — **όχι** νέα διάσπαση ρόλων. — `lansweeper_report_dialog.dart` (1284 γραμμές)

  **Ευκαιριακά, χωρίς δική τους σειρά:** η υπόλοιπη ΟΜΑΔΑ Β/Γ της διάσπασης γίνεται όποτε αγγίζουμε ούτως ή άλλως τα αρχεία. Η εκκαθάριση του `database_helper` ζει στη δική της ενότητα στο τέλος του αρχείου.

  Να προηγηθούν των μεγάλων έργων (ticket, μαζική ΤΝ, παράλληλη κλήση).

- **Δ14 — Ο Έλεγχος ακεραιότητας δεν έχει αποδειχθεί ότι πιάνει όσα υπόσχεται.**
  *Σημείωση Διευθυντή· απόφαση 13/08: είναι εργασία ποιότητας (συνεδρία με τον βοηθό), ΟΧΙ λειτουργία της εφαρμογής.*

  **Δεν το βλέπει ο χρήστης:** δεν αλλάζει καμία οθόνη — αλλάζει το πόσο εμπιστευόμαστε τα ευρήματα.

  **Ζητούμενο:** να επαληθευτεί συστηματικά ότι (1) κάθε τύπος ευρήματος του Ελέγχου ακεραιότητας αναπαράγεται στη δοκιμαστική βάση — υπάρχει ήδη debug seeder (`integrity_debug_seeder_service.dart`) — και (2) κάθε κανόνας επίλυσης διορθώνει σωστά. Παραδοτέο: τεστ ή αναφορά κάλυψης ανά τύπο ευρήματος.

  **Γιατί τώρα:** ο κανόνας «εξοπλισμός χωρίς τμήμα» έβγαλε 55 ψευδή ευρήματα και μηδέν αληθινά. Ένας κανόνας που λέει ψέματα κοστίζει περισσότερο από κανέναν κανόνα.

## Β. Προσθήκες (νέες λειτουργίες)

### Νοσοκομείο

- **Π1 — Εκτύπωση εκκρεμότητας.**

- **Π2 — Το ίδιο προφίλ ανοιχτό σε δύο θέσεις ταυτόχρονα.**
  *Ερώτημα Διευθυντή, αποσαφηνίστηκε 24/08: πρόκειται για την περίπτωση όπου διαλέγω προφίλ που κάποιος άλλος το έχει ήδη ανοιχτό σε άλλο μηχάνημα.*

  **Σήμερα δεν υπάρχει καμία διένεξη — υπάρχει μόνο πληροφορία.** Ο επιλογέας ταυτότητας δείχνει, κάτω από κάθε προφίλ, τους σταθμούς όπου είναι αυτή τη στιγμή ανοιχτό (γιατί «το ίδιο προφίλ μπορεί να δουλεύει από δύο θέσεις» ήταν συνειδητή παραδοχή). Δεν προειδοποιεί, δεν εμποδίζει, και δεν εξαιρεί κανέναν. — `operator_presence_summary.dart:36-48`, `selectable_profiles.dart`

  **Απόφαση που λείπει:** τι πρέπει να συμβαίνει.
  1. **Προειδοποίηση χωρίς φραγή** — «ο Βλάσης το έχει ανοιχτό στον ΣΤΑΘΜΟ-7· να συνεχίσω;». Η πληροφορία υπάρχει ήδη, λείπει η στιγμή που ρωτά.
  2. **Φραγή** — και τότε γεννιέται αμέσως το πρόβλημα των φαντασμάτων: μια εφαρμογή που κρασάρισε αφήνει πίσω της ίχνος «ανοιχτό», και ο άνθρωπος κλειδώνεται έξω από τον εαυτό του.
  3. **Εξαίρεση του διαχειριστή** — το ρητό ερώτημα του Διευθυντή («είναι σωστή πρακτική;»). Παρατήρηση: η εξαίρεση έχει νόημα ως **διέξοδος** από την περίπτωση 2 (κάποιος πρέπει να μπορεί να μπει όταν το μητρώο λέει ψέματα), όχι ως προνόμιο.

     **Και αυτό είναι μάλλον υπέρ της:** αφού αποφασίστηκε ότι τα προφίλ δεν αποκτούν κωδικούς (17/09/2026), η εξαίρεση είναι διαθέσιμη σε **όποιον** διαλέξει προφίλ διαχειριστή. Ως προνόμιο θα ήταν ψέμα· ως διέξοδος από κλείδωμα είναι ακριβώς ό,τι χρειάζεται — κανείς δεν μένει έξω επειδή μια εφαρμογή κόλλησε σε άλλο μηχάνημα.

  **Προϋπόθεση:** ό,τι επιλεγεί στηρίζεται στην πολιτική «μετά από πόση σιωπή θεωρείται νεκρή μια συνεδρία», που είναι ακριβώς το ανοιχτό σημείο του μητρώου συνεδριών. Οι δύο αποφάσεις είναι μία.

- **Π3 — Μετατροπή εκκρεμότητας σε ticket στο Lansweeper.**

  **Απόφαση που λείπει:** ολόκληρη η διαδικασία και ο τρόπος ενσωμάτωσης. Δένει με τον ανασχεδιασμό της Αναφοράς Lansweeper — αν η οθόνη ξαναχτιστεί, αυτή η έξοδος σχεδιάζεται μαζί της.

### Σπίτι

- **Π4 — Επαλήθευση αντιγράφων ασφαλείας.**
  *(Επαλήθευση 13/08: τίποτα από τα δύο σκέλη δεν υπάρχει ακόμη.)*

  1. **Δοκιμαστικό άνοιγμα μετά τη δημιουργία** κάθε backup, με έλεγχο ακεραιότητας — ώστε κατεστραμμένο αντίγραφο να μην ανακαλυφθεί τη στιγμή της ανάγκης.
  2. **Εμφανής ένδειξη «τελευταία εγγραφή: `<ημερομηνία>`»** ώστε να φαίνεται πόσο φρέσκο είναι το αρχείο που ανοίξαμε. *Σημείωση 17/07:* την ανάγκη την έδειξε το επεισόδιο «τμήμα-φάντασμα», όπου παλαιό αντίγραφο πέρασε για τρέχον.

- **Π5 — Η εκκαθάριση Ιστορικού θέλει διαβάθμιση ανά είδος εγγραφής, όχι ένα ψαλίδι στην ημερομηνία.**
  *Απόφαση Διευθυντή 17/09/2026, κατά τη συζήτηση για την υπογραφή χρήστη.*

  **Σήμερα:** η πολιτική έχει δύο μοχλούς — «σβήσε ό,τι είναι παλαιότερο από Ν ημέρες» και «κράτα το πολύ Ν γραμμές». Και οι δύο κόβουν **οριζόντια**: ό,τι πέφτει έξω από το όριο φεύγει, ανεξάρτητα από το τι είναι.

  **Γιατί δεν αρκεί:** οι εγγραφές του Ιστορικού δεν έχουν ίδια αξία στον χρόνο.
  1. **Οι δημιουργίες κλήσεων** πληθαίνουν καθημερινά και παλιώνουν γρήγορα — μετά από μήνες δεν τις ψάχνει κανείς.
  2. **Οι δημιουργίες οντοτήτων** (υπάλληλοι, τμήματα, εταιρείες, εξοπλισμός) είναι λίγες και **δεν παλιώνουν ποτέ**: είναι η μόνη απάντηση στο «ποιος και πότε το έφτιαξε», αφού οι πίνακες του Καταλόγου δεν κρατούν δική τους υπογραφή.

  **Ζητούμενο:** η πολιτική να κόβει **ανά είδος** — π.χ. «σβήσε τις δημιουργίες κλήσεων πάνω από Ν μήνες, κράτα για πάντα τις δημιουργίες οντοτήτων». Η ομαδοποίηση γίνεται πάνω στα υπάρχοντα πεδία «τύπος οντότητας» και «ενέργεια», που το φίλτρο του Ιστορικού ήδη χρησιμοποιεί.

  **Πλαίσιο — γιατί δεν είναι επείγον:** η αυτόματη εκκαθάριση είναι **σβηστή από προεπιλογή** και κανείς δεν την έχει ενεργοποιήσει. Η διαβάθμιση είναι **προϋπόθεση** για να ενεργοποιηθεί ποτέ με ασφάλεια, όχι διόρθωση κάποιου σημερινού προβλήματος. — `audit_retention_config.dart`, `audit_retention_runner.dart:36-48`

  Πριν από αυτήν προηγείται η διόρθωση του κειμένου που λέει «τοπικές ρυθμίσεις» στις Διορθώσεις: η οθόνη πρέπει να λέει την αλήθεια πριν ο διακόπτης γίνει ασφαλής.

- **Π6 — Χρειάζεται ρητή επιβεβαίωση όταν ένας υπάλληλος φεύγει από το νοσοκομείο;**
  *Ερώτημα Διευθυντή, ανοιχτό από 10/09/2026. Τα άλλα δύο σκέλη έκλεισαν 16/09.*

  **Τι επαληθεύτηκε ως ΚΛΕΙΣΤΟ — μη το ξαναζητήσεις:**
  1. **Τα αναγνωριστικά Lansweeper.** Η υπόδειξη κάτω από το πεδίο διαβάζει το **τμήμα που πληκτρολογείτε** και αλλάζει ζωντανά: μόλις γράψετε εταιρεία, λέει «το αναγνωριστικό δεν θα χρησιμοποιηθεί». — `user_form_dialog.dart:816-830`
  2. **Η αντίστροφη φορά.** Η καρτέλα λέει πλέον ότι θα ζητηθεί αναγνωριστικό και ότι όσο λείπει θα εμφανίζεται στον Έλεγχο δεδομένων. — `user_move_consequences.dart`
  3. **Ο εξοπλισμός προς εταιρεία.** Διορθώθηκε ζωντανό σφάλμα: το «Ακολουθεί» δεν προσφέρεται πια όταν ο προορισμός δεν κρατά μηχανήματα, σε **καμία** από τις τρεις ροές. — `asset_fate_on_department_change.dart:60-95`
  4. **Τα εσωτερικά τηλέφωνα.** Η αδελφή ροή του ίδιου σφάλματος, βρέθηκε και διορθώθηκε 16/09: το τετραψήφιο του κέντρου μας δεν ακολουθεί σε εταιρεία ή εξωτερική μονάδα, η ερώτηση αφορά μόνο τα προσωπικά δεκαψήφια, και όσα μένουν αναγγέλλονται ονομαστικά. — `phone_transfer_split.dart`

  **Τι λέει ήδη η καρτέλα σήμερα**, πριν πατηθεί η Αποθήκευση:
  1. Τη γραμμή για τα μηχανήματα που μένουν πίσω.
  2. Την υπόδειξη ότι το αναγνωριστικό παύει να ισχύει.
  3. Την αναγγελία για τα εσωτερικά τηλέφωνα που δεν ακολουθούν, ονομαστικά.
  4. Και ρωτά ξεχωριστά για όσα τηλέφωνα μπορούν όντως να ακολουθήσουν.

  **Απόφαση που λείπει:** χρειάζεται ξεχωριστή προειδοποίηση για τον **ίδιο τον άνθρωπο** («ο Αντώνης φεύγει από το νοσοκομείο, συνεχίζεις;») όταν μετακινείται ένας μόνος του; Να δοκιμαστεί στην πράξη αν τα τέσσερα παραπάνω αρκούν. Αν ναι, η καταχώρηση κλείνει χωρίς κώδικα.

- **Π7 — Ο ορθογραφικός έλεγχος του Λεξικού δεν ξέρει ότι το μοντέλο ΤΝ είναι πεσμένο.**
  *Εύρημα 25/08 κατά την απογραφή πυλών της έξυπνης υποβάθμισης ΤΝ· εξαιρέθηκε ρητά από εκείνη τη φάση με απόφαση Διευθυντή.*

  **Σενάριο:** το κύριο μοντέλο έχει εξαντλημένη ποσόστωση. Η «Πρόταση ΤΝ» της Αναφοράς Lansweeper το ξέρει και ξεκινά από το εφεδρικό. Ο ορθογραφικός έλεγχος όμως καλεί το ίδιο πεσμένο μοντέλο και περιμένει μισό λεπτό για να αποτύχει — κάθε φορά.

  **Τι λείπει:**
  1. **Δεν διαβάζει τη μνήμη υγείας μοντέλων.** Δοκιμάζει το κύριο ό,τι κι αν ξέρει ο υπολογιστής. — `spelling_lookup_gemini_service.dart:35-90`
  2. **Δεν έχει καθόλου εφεδρικό μοντέλο.** Όταν πέσει το κύριο, ο έλεγχος απλώς δεν δουλεύει.

  **Συμβόλαιο:** «κάθε κλήση προς την ΤΝ ξεκινά από το πρώτο μοντέλο που ο υπολογιστής δεν ξέρει ήδη πεσμένο»

  **Απόφαση που λείπει:** ο ορθογραφικός αποκτά δικό του εφεδρικό μοντέλο, ή μηνύει αμέσως «η ΤΝ δεν είναι διαθέσιμη τώρα»; Το δεύτερο είναι μικρή δουλειά.

- **Π8 — Πόσα έχω επιλέξει; Ένας κανόνας για κάθε λίστα με αναζήτηση και πολλαπλή επιλογή.**
  *Ερώτημα Διευθυντή 13/09/2026. Απόφαση: ένας κανόνας παντού, όχι διόρθωση μιας οθόνης.*

  **Το σενάριο:** αναζητάτε «αιμοδοσία», επιλέγετε· αναζητάτε «βιοχημικό», επιλέγετε· και ούτω καθεξής. Οι προηγούμενες επιλογές **εξαφανίζονται από την οθόνη**, γιατί η αναζήτηση τις φιλτράρει έξω. Δεν υπάρχει τρόπος να δείτε τι έχετε μαζέψει πριν πατήσετε την ενέργεια.

  **Πού ισχύει σήμερα** — έξι σημεία με το ίδιο μοτίβο:
  1. **Κατάλογος → Τμήματα** — `features/directory/screens/widgets/departments_tab.dart`
  2. **Κατάλογος → Υπάλληλοι** — `features/directory/screens/widgets/users_tab.dart`
  3. **Κατάλογος → Εξοπλισμός** — `features/directory/screens/widgets/equipment_tab.dart`
  4. **Κατάλογος → Κατηγορίες** — `features/directory/screens/widgets/categories_tab.dart`
  5. **Τμήματα σε φύλλο κάτοψης** — `features/directory/building_map/widgets/building_map_floor_departments_dialog.dart`
  6. **Διαχείριση Λεξικού** — `features/dictionary/screens/dictionary_manager_screen.dart`

  **Τι υπάρχει σήμερα:** στον διάλογο της κάτοψης το πλήθος φαίνεται **μόνο μέσα στο κουμπί** της ενέργειας («Αφαίρεση 4 από χάρτη»), και μόνο σε λειτουργία επεξεργασίας. Αλλού δεν φαίνεται καθόλου.

  **Απόφαση που λείπει:** μπορεί το ίδιο το chip «Επιλεγμένα: 4» να λειτουργεί ως **διακόπτης** που δείχνει μόνο τα επιλεγμένα; Ή αυτό συνηθίζεται για **αποεπιλογή** και θα μπερδεύει;

  **Ζητούμενο:** σχεδιασμός ενός μηχανισμού — ένδειξη πλήθους, τρόπος να δει κανείς τι έχει επιλέξει, και σαφής διάκριση από την αποεπιλογή — που μετά εφαρμόζεται και στα έξι σημεία.

- **Π9 — Το πρόσφατο ιστορικό δείχνει ολόκληρη την περιγραφή της κλήσης, που είναι μεγάλη.**
  *Ερώτημα Διευθυντή 13/09/2026.*

  **Τι ισχύει σήμερα:** η κάρτα «Τελευταίες Κλήσεις» δείχνει το πεδίο **Πρόβλημα** κομμένο στις δύο γραμμές με αποσιωπητικά. Δεν υπάρχει υπόδειξη με το πλήρες κείμενο, και η λύση δεν φαίνεται καθόλου. — `features/calls/screens/widgets/recent_calls_list.dart:237`

  **Τι ζητά ο Διευθυντής:** «όπου υπάρχει τίτλος, να μπαίνει ως **περίληψη** στο πρόσφατο ιστορικό» — το κύριο κείμενο της περιγραφής είναι πολύ μεγάλο για λίστα.

  **Απόφαση που λείπει:**
  1. **Από πού βγαίνει η περίληψη.** Η κλήση δεν έχει σήμερα πεδίο «Τίτλος»: τα πεδία είναι `issue` (Πρόβλημα) και `solution` (Λύση). Νέο πεδίο που συμπληρώνεται χειροκίνητα, ή αυτόματη περίληψη από την πρώτη γραμμή;
  2. **Πού πάει το υπόλοιπο.** Πρόβλημα και λύση ως υποδείξεις πάνω στη γραμμή.
  3. **Διακόπτης προεπισκόπησης.** Κουμπί ενεργοποίησης/απενεργοποίησης, για όποιον τον ενοχλεί.

- **Π10 — Τύποι οντότητας στο ιστορικό εφαρμογής.**
  *Ενημέρωση 13/08: οι τύποι πλέον λέγονται Εισαγωγή Δεδομένων, Συντήρηση, Αντίγραφο ασφαλείας και μαζικές ενέργειες (χρηστών/τμημάτων/εξοπλισμού).* — `application_audit_tab.dart:769`

  **Απόφαση που λείπει:** ανήκουν πράγματι αυτές οι μη-οντότητες στη λίστα τύπων του φίλτρου;

- **Π11 — Επεξεργαστής ιστορικού αλλαγών μέσα στην εφαρμογή (Unreleased).**
  *Αίτημα χρήστη 03/08, μαζί με την καθιέρωση του `changelog.json` ως μοναδικής πηγής αλήθειας.*

  **Πλαίσιο:** ο Διευθυντής διορθώνει περιστασιακά κακογραμμένο κείμενο εγγραφών· η χειροκίνητη επεξεργασία JSON είναι επικίνδυνη — ένα λάθος κόμμα σπάει το in-app ιστορικό ΚΑΙ τη Δημοσίευση. Οι ημερομηνίες δεν χρειάζονται επεξεργασία, σφραγίζονται αυτόματα.

  **Ζητούμενο:** υποτυπώδης επεξεργαστής στα «Σενάρια σφαλμάτων» ή στον διάλογο «Ιστορικό Αλλαγών» (debug): επεξεργασία/διαγραφή/μετακίνηση γραμμών του Unreleased ανά κατηγορία, με επικύρωση JSON πριν την αποθήκευση.

- **Π12 — Διαγνωστικά μέσα από την εφαρμογή: προβολή αρχείων καταγραφής ΚΑΙ ανάγνωση του Windows Event Log.**
  *Επανα-αίτημα χρήστη 28/07 («όταν κάτι στραβώσει στο μηχάνημα συναδέλφου, σήμερα δεν έχεις κανένα στοιχείο»)· απόφαση Διευθυντή 13/08: το σκέλος Windows Event Log ΜΕΝΕΙ — η εφαρμογή να δείχνει τι έχουν καταγράψει τα Windows για την «Καταγραφή Κλήσεων».*

  1. **Πυρήνας:** προβολή των `errors_YYYY-MM-DD.log` και των ιχνών τερματισμού μέσα από την εφαρμογή — τηλεδιάγνωση στο μηχάνημα συναδέλφου χωρίς εξερεύνηση φακέλων. Η αξία ανέβηκε με την παράδοση της Λάμπας.
  2. **Σφάλματα των Windows:** πάνελ που διαβάζει τις εγγραφές Application Error / Windows Error Reporting για το δικό μας εκτελέσιμο και τις δείχνει δίπλα στα δικά μας logs. **Τεχνικά εφικτό:** ερώτημα στο ημερολόγιο Application φιλτραρισμένο στο exe (π.χ. `wevtutil`/`Get-WinEvent` με XPath σε `EventData = call_logger.exe`)· η ανάγνωση του Application log δεν απαιτεί δικαιώματα διαχειριστή.
  3. **Ιστορικό:** το κυνήγι των APPCRASH 0xc0000005 ΕΚΛΕΙΣΕ — η ρίζα (κατάρρευση στο teardown) βρέθηκε και διορθώθηκε, δοκιμασμένο και στους συναδέλφους. Το πάνελ αφορά τα μελλοντικά άγνωστα, όχι εκείνη την ιστορία.

- **Π13 — Επεξεργασία/διαγραφή οντότητας ενώ συμμετέχει σε ανοιχτή κλήση.**

  **Απόφαση που λείπει:** απαγόρευση ή επιτρέπεται; Να αποφασιστεί πρώτα η πολιτική και μετά ο μηχανισμός.

  **Πραγματικό περιστατικό:** διαγραφή φαρμακείου ενώ έτρεχε κλήση σε Πρόβο — η κλήση καταγράφηκε αλλά δεν δόθηκε καμία ένδειξη· με τι μεταδεδομένα καταγράφηκε; Συσχέτιση με τη διαγραφή τμήματος.

  Δένει με τα κουμπιά που μένουν ενεργά σε διαγραμμένη οντότητα: ίδιο σπασμένο συμβόλαιο σε άλλη ροή — η οθόνη κρατά αναφορά σε κάτι που έχει πάψει να υπάρχει.

- **Π14 — Ανενεργός εξοπλισμός: τύπος «Χαλασμένο» ή κατάσταση;**
  *Ερώτημα Διευθυντή («είναι σωστή προσέγγιση ή ευκαιριακή τακτική που αργότερα θα μου δημιουργήσει προβλήματα;»)· απογραφή στον κώδικα 24/08.*

  **Απάντηση στο πρώτο σκέλος («πού χειρίζομαι τους τύπους εξοπλισμού;»):** στον Κατάλογο → Εξοπλισμός → ρυθμίσεις εξοπλισμού, πεδίο «Τύποι εξοπλισμού». — `equipment_settings_dialog.dart:113`

  **Γιατί ο τύπος ΔΕΝ μπορεί να κουβαλήσει ιδιότητες, όπως είναι σήμερα:**
  1. **Ο κατάλογος τύπων είναι μία γραμμή κειμένου με κόμματα**, με προεπιλογή «Υπολογιστής, Εκτυπωτής». Δεν είναι πίνακας, δεν έχει κωδικό, δεν έχει πεδία. — `settings_service_catalogs.dart:214-231`
  2. **Οι τύποι χρησιμοποιούνται μόνο ως λίστα προτάσεων** στη φόρμα εξοπλισμού και στη μαζική επεξεργασία. Κανένα σημείο του κώδικα δεν ρωτά «τι τύπος είναι αυτό» για να αλλάξει συμπεριφορά. — `equipment_form_dialog.dart:729`, `bulk_equipment_edit_dialog.dart:208`
  3. **Ο εξοπλισμός δεν έχει καθόλου έννοια κατάστασης.** Οι στήλες του είναι κωδικός, τύπος, σημειώσεις, εργαλείο, τμήμα, θέση, όνομα Lansweeper, και σημαία διαγραφής — τίποτα σαν «ενεργό/ανενεργό». — `database_foreign_keys.dart:91-103`

  **Η ουσία της ένστασης:** ο τύπος απαντά «**τι είναι**» (υπολογιστής, εκτυπωτής). Το «χαλασμένο» απαντά «**σε τι κατάσταση είναι**». Ένας χαλασμένος εκτυπωτής δεν παύει να είναι εκτυπωτής — αν το «Χαλασμένο» γίνει τύπος, χάνεται η πληροφορία τι μηχάνημα είναι, και τα στατιστικά ανά τύπο αρχίζουν να λένε ψέματα. Η υποψία του Διευθυντή είναι σωστή: είναι ευκαιριακή τακτική.

  **Απόφαση που λείπει:**
  1. **Ξεχωριστή κατάσταση εξοπλισμού** (ενεργός / χαλασμένος / αποσυρμένος) ως δικό της πεδίο, ώστε τύπος και κατάσταση να συνυπάρχουν.
  2. **Τι αλλάζει η κατάσταση** — απόκρυψη από λίστες προτάσεων, εξαίρεση από τους ελέγχους ακεραιότητας, ένδειξη στην καρτέλα, εξαίρεση από τα στατιστικά «πόσο συχνά χαλάει»;
  3. **Ό,τι επιλεγεί, ο κατάλογος τύπων μένει ελεύθερο κείμενο** ή γίνεται κανονικός πίνακας; Το δεύτερο είναι προϋπόθεση για οτιδήποτε κρεμαστεί ποτέ πάνω στον τύπο — και δένει με το Κτίριο τμήματος και τις Ομάδες τμημάτων, που ρωτούν το ίδιο πράγμα.

- **Π15 — Συντήρηση Βάσης Γνώσης με ΤΝ (υπόλοιπο).**
  *Τα Μέρη Α και Β ολοκληρώθηκαν 09/08 — μένει μόνο η μαζική οργάνωση.*

  **Τι επαληθεύτηκε ως κλειστό — μη το ξαναζητήσεις:**
  1. **Η λύση και η καθαρισμένη περιγραφή ζουν πάνω στην κλήση**, με προέλευση και χρονοσφραγίδα (`refined_source`/`refined_at`). *Ενημέρωση v43:* κάθε κλήση έχει πλέον **ΜΙΑ** Περιγραφή — το ξεχωριστό πεδίο «καθαρού» κειμένου καταργήθηκε· η προέλευση δείχνει αν πέρασε από ΤΝ (εικονίδιο δίπλα στην Περιγραφή).
  2. **Και οι δύο έξοδοι της φόρμας Lansweeper** γράφουν πίσω (υποβολή API και «Αντιγραφή & Άνοιγμα»), σε όλες τις κλήσεις του ticket.
  3. **Ευρετήριο αναζήτησης κλήσεων** — η κλήση βρίσκεται και από το παλιό «καθαρό» κείμενο των υπαρχουσών εγγραφών.
  4. **Πίνακας `knowledge_base`** — πλήρες σχήμα (σύμπτωμα, λύση, κατηγορία, ετικέτες, μετρητής χρήσης, κλήση προέλευσης, ευρετήριο).
  5. **Οθόνη Βάσης Γνώσης** — λίστα, αναζήτηση, φίλτρο κατηγορίας, δημιουργία/επεξεργασία/διαγραφή με ίχνος στο Ιστορικό.
  6. **«Αποθήκευση ως γνώση»** στη φόρμα Lansweeper, με ερώτηση όταν υπάρχει ήδη παρόμοιο άρθρο.
  7. **Placeholder `{Γνώση}`** στην προτροπή ΤΝ, με τα σχετικά άρθρα ως παραδείγματα.

  **Ζητούμενο (τι απομένει):** κουμπί **«Οργάνωση με ΤΝ»** στην οθόνη Βάσης Γνώσης.
  1. **Εντοπισμός διπλών** σε ολόκληρη τη βάση, με πρόταση συγχώνευσης — ο σημερινός έλεγχος πιάνει μόνο τη στιγμή της δημιουργίας.
  2. **Προτάσεις τίτλων και λέξεων-κλειδιών** για άρθρα που γράφτηκαν βιαστικά.
  3. **Πρόταση ενοτήτων/κατηγοριών** όταν μαζευτούν αρκετά άρθρα του ίδιου είδους.

  Όλα ως **προτάσεις προς έγκριση με κλικ**, ποτέ αυτόματη εφαρμογή: η εβδομαδιαία συντήρηση γίνεται μόνο αν κοστίζει πέντε λεπτά. Ίδιο μοτίβο με τη μαζική επεξεργασία λέξεων Λεξικού από ΤΝ — τα δύο αξίζει να σχεδιαστούν μαζί.

  **Όρια που μένουν ανοιχτά (επαλήθευση 13/08):**
  1. **Δεν υπάρχει υπόδειξη γνώσης την ώρα της κλήσης** — τα άρθρα φτάνουν στην ΤΝ, αλλά η φόρμα κλήσης δεν δείχνει «υπάρχει γνωστή λύση» ενώ πληκτρολογείτε (ο `KnowledgeMatcher` δεν χρησιμοποιείται από τη φόρμα).
  2. **Ο μετρητής χρήσης (`times_used`) ανεβαίνει μόνο από τη ροή της Αναφοράς** (`markUsed`) — όχι όταν ένα άρθρο απλώς τροφοδοτεί την προτροπή ΤΝ.

- **Π16 — Μαζική επεξεργασία λέξεων Λεξικού από ΤΝ.** Εικονίδιο επεξεργασίας προτροπής δίπλα στην «Ερώτηση ΤΝ»· επιλογή 100–200 λέξεων και μαζική διόρθωσή τους.

  **Απόφαση που λείπει:** ο μηχανισμός θέλει ξεχωριστό σχεδιασμό — μαζί με τη συντήρηση της Βάσης Γνώσης, που ρωτά το ίδιο πράγμα.

- **Π17 — Πλήρης ανασχεδιασμός της Αναφοράς Lansweeper.**
  *Απόφαση Διευθυντή 05/08: η λειτουργία είναι πλέον εργαλείο καθημερινής παραγωγής και ο σημερινός διάλογος δεν σηκώνει τον όγκο.*

  **Πλαίσιο:** κάθε μέρα μετά τις 13:00 περνούν όλες οι κλήσεις της ημέρας στο Lansweeper· η προϊσταμένη ελέγχει από εκεί τον φόρτο εργασίας. Η μετατροπή κλήσης σε αίτημα είναι από τις σημαντικότερες εργασίες της εφαρμογής.

  **Τι έκλεισε εν τω μεταξύ (επαλήθευση 13/08) — μη το ξαναζητήσεις:**
  1. **Δικό της χρονικό πλαίσιο** — προεπιλογή «Σήμερα», θυμάται το τελευταίο διάστημα του χρήστη. — `lansweeper_report_scope_provider.dart:17`
  2. **Συντόμευση από παντού** (Ctrl+Shift+L) με φρουρό διπλού ανοίγματος. — `app_shortcuts.dart`, `lansweeper_report_launcher.dart`
  3. **Ουρά εργασίας** — ο διάλογος δείχνει μόνο ό,τι μένει να καταχωρηθεί· το «τι έγινε» το απαντά το Ιστορικό.

  **Τι δεν αντέχει ακόμη:**
  1. **Όλα σε ένα modal** — καρτέλες κατάστασης, λίστα ανά καλούντα, φόρμα καταχώρησης, πρόταση ΤΝ και ρυθμίσεις σύνδεσης στριμώχνονται σε διάλογο πάνω από άλλη οθόνη.
  2. **Κλίμακα** — εκατοντάδες κλήσεις σήμερα, χιλιάδες αύριο· η λίστα κυλά μέσα σε πλαίσιο ύψους μισής οθόνης.
  3. **Μέγεθος κώδικα (μέτρηση 13/08):** ο διάλογος 1.217 γραμμές, η οικογένεια `lansweeper*` 52 αρχεία / 10.700+ γραμμές.

  **Ζητούμενα προς σχεδιασμό:**
  1. **Ιδιόκτητη οθόνη** αντί για modal, με δικό της χώρο και δική της πλοήγηση.
  2. **Ροή μαζικής καταχώρησης** — η καθημερινή δουλειά είναι «πέρασέ τις όλες», όχι «μία-μία».
  3. **Δέσιμο με τη Βάση Γνώσης** — βλ. το υπόλοιπο της συντήρησης Βάσης Γνώσης.
  4. **Οι έξοδοι της φόρμας** — μαζί με τον ανασχεδιασμό μπαίνει και η «Αποθήκευση κλήσης» χωρίς καταχώρηση στο Lansweeper: αν η οθόνη ξαναχτιστεί, αξίζει να ξαναχτιστεί με όλες τις εξόδους μαζί, μαζί με τη μετατροπή εκκρεμότητας σε ticket.

- **Π18 — Παράλληλη κλήση.**
  Δεύτερη ενεργή κλήση ως καρτέλα ή ελαχιστοποιημένη σε ιπτάμενο chip με τρέχον χρονόμετρο και επαναφορά με κλικ· η ανοιχτή κλήση κατεβαίνει κάτω· προειδοποίηση στις 3 ανοιχτές.

  **Απόφαση που λείπει:** μεγάλο έργο — θέλει σχεδιασμό κατάστασης πολλών φορμών πριν από οτιδήποτε άλλο.

## Συντήρηση κώδικα

*Περιοδική εργασία — η ενότητα ΔΕΝ αφαιρείται όταν εκτελεστεί.*

**Πότε τρέχει:** το πρώτο σκέλος σε κάθε `/sync` (υπόδειξη, όχι αυτόματη εκτέλεση)· τα υπόλοιπα πριν από ανέβασμα έκδοσης· η αναβάθμιση SDK κάθε τρίμηνο.

### 1. Μορφοποίηση κώδικα

**Πότε έτρεξε:** 21/08/2026, 30/08/2026 (πλήρες πέρασμα), 11/09/2026 (46 αρχεία σε δικό τους γύρο).

**Είναι το μόνο σκέλος που μαζεύει υπόλοιπο** — στη μέτρηση της 30/08 συγκεντρώνονταν ~10 αρχεία την ημέρα. Γι' αυτό ο έλεγχος μετακινήθηκε στο `/sync`: μορφοποιούνται **ονομαστικά τα αρχεία που άλλαξαν**, ποτέ ολόκληρος φάκελος (η σάρωση φακέλου πειράζει και αμορφοποίητα αρχεία τρίτων και κάνει το diff αδιάβαστο).

**Παγίδα που επιβεβαιώθηκε:** το `dart format` ισοπεδώνει CRLF σε LF. Δεν είναι πρόβλημα εδώ — με `core.autocrlf=true` το `git diff` βγαίνει ταυτόσημο με και χωρίς `--ignore-cr-at-eol`. Οι προειδοποιήσεις «LF will be replaced by CRLF» είναι θόρυβος.

### 2. Αναβάθμιση Flutter SDK και βιβλιοθηκών

**Πότε τρέχει:** κάθε ~3 μήνες. **Τελευταία: 31/08/2026** (SDK 3.47.2, Dart 3.13.2). **Επόμενη: τέλη Οκτωβρίου 2026.**

**Πάντα σε ξεχωριστό branch, ποτέ σε ώρα παράδοσης.** Κύκλος: `flutter upgrade` (stable, 1–2 βδομάδες μετά από μεγάλη έκδοση) → `flutter pub upgrade` → ψαλίδισμα overrides → `flutter analyze` + σουίτα → smoke test.

**Τρεις παγίδες που κοστίζουν ώρες αν ξεχαστούν:**
1. **Το golden test βγαίνει αναμενόμενα κόκκινο** μετά από άλμα μηχανής — το anti-aliasing αλλάζει στις στρογγυλεμένες γωνίες. Κοίτα την εικόνα διαφοράς **πριν** τρέξεις `--update-goldens`: αν η διαφορά είναι στο περιεχόμενο, δεν είναι η μηχανή.
2. **Τα νέα lint του SDK είναι δωρεάν εύρεση σφαλμάτων** — μην τα καταπίνεις. Το Dart 3.13 αποκάλυψε έτσι **7 πραγματικά σφάλματα**.
3. **Τα πακέτα που ανήκουν στο SDK** (`analyzer`, `test`, `meta`, …) δεν αναβαθμίζονται μόνα τους — έρχονται μαζί με το SDK, και τα `dependency_overrides` πάνω τους δουλεύουν εναντίον της τακτικής.

### 3. Έλεγχοι πριν από ανέβασμα

*Στη μέτρηση της 30/08 και τα τέσσερα βγήκαν **μηδενικά** — γι' αυτό δεν αξίζουν περιοδικότητα, μόνο πέρασμα πριν από έκδοση.*

1. **Αχρησιμοποίητες εξαρτήσεις** — τρία πακέτα δεν γίνονται ποτέ `import` αλλά **χρειάζονται**: το `sqlite3_flutter_libs` δίνει το native `sqlite3.dll`, το `cupertino_icons` ελέγχεται από τον φρουρό ακεραιότητας εκκίνησης, το `flutter_lints` μπαίνει από το `analysis_options.yaml`. — `startup_asset_integrity_service.dart:43`
2. **Ορφανά assets** — μόνο το `assets/.gitkeep`, που υπάρχει σκόπιμα. Οι φάκελοι που διαβάζονται δυναμικά μέσω `AssetManifest` (`splash/`, `avatars/`, `dictionaries/`, `floor_plans/`) εξαιρούνται εξ ορισμού.
3. **`TODO`/`FIXME`/`HACK` και σκέτο `print(`** στο `lib/`.
4. **`flutter analyze` + πλήρης σουίτα.** Δύο σουίτες ταυτόχρονα κλειδώνουν το `sqlite3.dll` — ο έλεγχος τρέχει μία φορά, σειριακά.

## Μηδενισμός έκδοσης βάσης

**Πότε τρέχει:** μόλις δημοσιευτεί η έκδοση 1 — δηλαδή όταν ολοκληρωθούν οι Διορθώσεις και οι Προσθήκες αυτού του αρχείου. Δεν έχει σειρά προτεραιότητας γιατί δεν μπορεί να ξεκινήσει νωρίτερα.

Εκκαθάριση του `lib/core/database/database_helper.dart`: περιέχει πολλές εκδόσεις βάσης και πολλά άχρηστα `IF`, αφού η δομή της βάσης σταθεροποιήθηκε. Στόχος ο μηδενισμός της έκδοσης βάσης σε v1.
