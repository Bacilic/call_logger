Το κεφάλαιο αυτό λειτουργεί ως οδηγός μελλοντικής υλοποίησης και σημειωματάριο του Διευθυντή Έργου. Υπάρχουν **δύο κωδικοί**: **Δ** = Διόρθωση (κάτι είναι ήδη λάθος), **Π** = Προσθήκη (κάτι δεν υπάρχει ακόμη). *(Οι κωδικοί «Ε», «Σ» και «Κ» καταργήθηκαν — δες παρακάτω.)*

> **Ο κωδικός δηλώνει ΘΕΣΗ ΠΡΟΤΕΡΑΙΟΤΗΤΑΣ, όχι σταθερή ταυτότητα.** Το Δ3 είναι «αυτό που πρέπει να λυθεί τρίτο», όχι πάντα το ίδιο θέμα. Σε κάθε αναδιάταξη οι κωδικοί ξαναδίνονται 1→Ν χωρίς κενά μέσα σε κάθε κατηγορία. Γι' αυτό οι καταχωρήσεις **παραπέμπουν η μία στην άλλη με περιγραφή, όχι με αριθμό** (ο αριθμός μετακινείται). Ροή εργασίας: ο Διευθυντής γράφει πρόχειρα στο «📝 Πρόχειρο» παρακάτω· ο βοηθός τα σουλουπώνει σε κωδικοποιημένες καταχωρήσεις.

> **Κάθε ενότητα ξεκινά με το ΝΟΣΟΚΟΜΕΙΟ.** Εκεί μπαίνει ό,τι δεν μπορεί να **επαληθευτεί** στο σπίτι: δίκτυο, Lansweeper, διακομιστές και εκτυπωτές, κοινόχρηστη βάση με συναδέλφους, πραγματικός εξοπλισμός, βάση Λάμπας. Ο κώδικας μπορεί κάλλιστα να γραφτεί σπίτι — η δοκιμή γίνεται επί τόπου. Ακολουθεί το **Σπίτι** με όλα τα υπόλοιπα. Έτσι ο χρόνος στη βάρδια πάει μόνο σε ό,τι απαιτεί το πραγματικό περιβάλλον. **Η αρίθμηση είναι συνεχής σε όλη την ενότητα**: το Νοσοκομείο παίρνει τους πρώτους αριθμούς, το Σπίτι συνεχίζει από εκεί.

> **Δύο σημάδια μέσα στις καταχωρήσεις, αντί για ξεχωριστές ενότητες** (17/09/2026):
> 1. **«Απόφαση που λείπει:»** — η καταχώρηση δεν μπορεί να ξεκινήσει σήμερα, γιατί δεν έχει αποφασιστεί ακόμη *τι* θέλουμε. Το `/fix` ανοίγει πρώτα συζήτηση. **Καμία τέτοια καταχώρηση δεν παίρνει τη θέση 1** — ο πρώτος αριθμός κάθε κατηγορίας πρέπει να είναι κάτι εκτελέσιμο σήμερα.
> 2. **«Δεν το βλέπει ο χρήστης:»** — διόρθωση στον κώδικα, όχι στην οθόνη. Τίποτα δεν χαλάει σήμερα για όποιον χρησιμοποιεί την εφαρμογή· διορθώνεται για να μη γεννήσει αυριανό σφάλμα. Τέτοιες καταχωρήσεις είναι ιδανικές για **πληρωμή εν κινήσει**: όταν μια άλλη δουλειά ανοίγει το ίδιο αρχείο, γίνονται μαζί.

> **Δεν υπάρχουν υποενότητες προτεραιότητας** («Υψηλή», «Μεσαία», «Χαμηλή») — καταργήθηκαν 06/09/2026. Τη σειρά τη λέει η αρίθμηση, οπότε ένα θέμα που γίνεται πιο επείγον αλλάζει αριθμό, όχι κεφαλίδα.

> **Η σειρά μέσα σε κάθε κατηγορία είναι κανόνας, όχι αίσθημα.**
> - **Διορθώσεις:** σιωπηλή απώλεια ή αλλοίωση δεδομένων → κατάρρευση ή μπλοκάρισμα → λάθος ορατό αποτέλεσμα → η οθόνη λέει ψέματα → ενόχληση → «δεν το βλέπει ο χρήστης».
> - **Προσθήκες:** ό,τι ξεκλειδώνει άλλα → καθημερινή χρήση → περιστασιακή → μεγάλα έργα.

> **Στο τέλος του αρχείου ζει ό,τι ΔΕΝ παίρνει αριθμό προτεραιότητας:** είτε επειδή επαναλαμβάνεται και δεν αφαιρείται ποτέ (Συντήρηση κώδικα), είτε επειδή περιμένει γεγονός που δεν ελέγχουμε πότε θα συμβεί (Μηδενισμός έκδοσης βάσης). Καθεμιά δηλώνει ρητά **πότε τρέχει**.

> Κεντρική ιδέα: Ένα σφάλμα που βρίσκεις είναι σύμπτωμα, όχι ο στόχος. Αντί να το μπαλώσω, το χρησιμοποιώ ως σημείο εισόδου για μια οριοθετημένη (στοχευμένη, όχι καθολική) ιχνηλάτηση.

> Έτσι φτιάχτηκε η Ικανότητα Fix. Σύνταξη: `/fix` όνομα σφάλματος

> **Αναδόμηση 24/09/2026 — το δίκτυο δοκιμών του σπιτιού μετακίνησε τέσσερα θέματα.** Με απόφαση του Διευθυντή, ό,τι αναπαράγεται με Popinio/PICINIO **δεν** είναι πια Νοσοκομείο: η νεκρή σύνδεση, τα δύο αντίγραφα, η σιωπηλή αναβάθμιση και ο έλεγχος των 12 δευτερολέπτων έφυγαν από τη βάρδια. Στο Νοσοκομείο μένει μόνο ό,τι θέλει **ζωντανό Lansweeper ή πραγματικό συνάδελφο**. **Καμία πρόχειρη σημείωση δεν αποδείχθηκε λανθασμένη** — και οι τέσσερις τηλεγραφικές εντοπίστηκαν στον κώδικα, δύο με έκπληξη μεγέθους: ο φρουρός «ένας διάλογος αρχείου» **υπάρχει ήδη** και τον παρακάμπτουν **δέκα** σημεία (όχι μόνο το Λεξικό), και ο τίτλος της Αναφοράς **γράφεται σωστά** στη βάση — αυτό που λείπει είναι η **ανάγνωσή** του πίσω στη φόρμα. **Έμεινε αναπάντητο:** το λευκό παράθυρο, που περιμένει επανάληψη με κλειστή την εφαρμογή ανάπτυξης. **Γνωστή ένταση κανόνων:** η μόνη Προσθήκη Νοσοκομείου έχει «Απόφαση που λείπει» και αναγκαστικά παίρνει το Π1 — η διαίρεση κέρδισε τον κανόνα της θέσης 1.

> **Αναδόμηση 17/09/2026 — τέσσερις ενότητες έγιναν δύο, και το Πρόχειρο άδειασε.** Οι «Προς συζήτηση» και οι «Προτάσεις Claude» καταργήθηκαν: ήταν αποθήκες αναβολής, όχι είδη δουλειάς. Ό,τι έχαναν έγινε **γραμμή μέσα στην καταχώρηση** — «Απόφαση που λείπει» και «Δεν το βλέπει ο χρήστης». **Η ερώτηση για τα δικαιώματα αποδείχθηκε το σοβαρότερο εύρημα της ημέρας** και μπήκε πρώτη: η αλλαγή δεν φτάνει ποτέ στον άλλο σταθμό, ούτε όταν **αφαιρείς** πρόσβαση. **Μία ιδέα εγκαταλείφθηκε από τον Διευθυντή:** ο έλεγχος του ιστορικού εκδόσεων από δεύτερη ΤΝ — το changelog είναι σωρευτικό ιστορικό, δεν λέει τι ισχύει σήμερα. **Η σημείωση που είχε μείνει αναπάντητη** (η λέξη «Επώνυμο» που κρύβεται) λύθηκε αυθημερόν με στιγμιότυπο: έφταιγε η κύλιση του διαλόγου, όχι η ετικέτα.

> **Αναδόμηση 13/09/2026 — οι Διορθώσεις ξαναγέμισαν από τον έλεγχο πεδίου των αντιγράφων.** Πέντε από τις έξι αφορούσαν την επαναφορά από αντίγραφο, με κοινή ρίζα: ο κριτής επαναφοράς δεν κοιτά καθόλου την έκδοση του αρχείου. **Μία σημείωση αποδείχθηκε λανθασμένη και σβήστηκε:** ο έλεγχος επικοινωνίας πριν από την επανεκκίνηση διακομιστή **υπάρχει ήδη** — ο διάλογος σαρώνει συνεδρίες μόλις ανοίξει και δείχνει σφάλμα αν ο διακομιστής δεν απαντά. Η ένδειξη πλήθους επιλεγμένων αποδείχθηκε **έξι οθόνες** με το ίδιο μοτίβο, όχι μία.

## Πρόχειρο (νέες σημειώσεις εδώ — προς σουλούπωμα) 📝

- **Τεστ εκκρεμοτήτων που αποτυγχάνει μόνο όταν τρέχει με παρέα.**
  *Εύρημα 24/09 κατά την ιχνηλάτηση της «Άμεσης Καταχώρησης». Δεν το προκάλεσε η δουλειά εκείνου του γύρου — εμφανίστηκε και χωρίς τις αλλαγές της.*

  **Τι συμβαίνει:** το `ολοκλήρωση μαρκάρει την εργασία ως closed στη βάση` περνά όταν τρέχει μόνο του, και σκάει όταν τρέχει ολόκληρος ο φάκελος `test/features/tasks/`. Δεν σκάει κάθε φορά — σε τρεις εκτελέσεις του φακέλου απέτυχε στις δύο. — `tasks_screen_split_characterization_test.dart`

  **Γιατί μετράει:** παραβιάζει τον κανόνα «ένα τεστ = μία συμπεριφορά, ανεξάρτητο από τη σειρά εκτέλεσης». Ένα τεστ που σκάει τυχαία εκπαιδεύει το μάτι να αγνοεί τις κόκκινες γραμμές — και την επόμενη φορά που θα είναι αληθινή, θα περάσει απαρατήρητη.

  **Ζητούμενο:** να βρεθεί τι κρατά κατάσταση ανάμεσα στα τεστ του φακέλου (βάση, ρυθμίσεις, χρονιστές) και να μηδενίζεται στο στήσιμο.

*(γράψε εδώ τις επόμενες σημειώσεις σου)*

### ⏳ Εκκρεμεί διευκρίνιση

- **Λευκό παράθυρο που δεν αλλάζει μέγεθος — δεν ξέρουμε ακόμη αν φταίει η εφαρμογή.**
  *Παρατήρηση 23/09, στο δίκτυο δοκιμών του σπιτιού. Μένει εδώ και δεν παίρνει αριθμό: δεν έχει αποδειχθεί καν ότι είναι σφάλμα του έργου.*

  **Τι φάνηκε:**
  1. **Κανονική έκδοση με προσωρινό προφίλ `nettest`** — το παράθυρο έμεινε λευκό στο αρχικό μέγεθος (691×480) και **αρνήθηκε** να μεγιστοποιηθεί, ενώ η διεργασία απαντούσε και δεν υπήρχε κρυφός διάλογος. Το ημερολόγιο εκκίνησης έφτασε κανονικά ως την οθόνη σφάλματος (η βάση ζητούσε συγκατάθεση για αναβάθμιση σχήματος).
  2. **Και με το κανονικό προφίλ `dev`** — σε εκκίνηση που το ημερολόγιο έγραψε «ΕΤΟΙΜΗ», οι λήψεις οθόνης έδειχναν λευκό, ενώ μία ώρα νωρίτερα, στο ίδιο μηχάνημα, οι λήψεις έδειχναν κανονικά την εφαρμογή.

  **Κοινό στοιχείο των δύο:** έτρεχε ταυτόχρονα στον ίδιο υπολογιστή η εφαρμογή ανάπτυξης (debug). Όταν δεν έτρεχε, το παράθυρο εμφανιζόταν κανονικά.

  **Ανοιχτή ερώτηση:** φταίει η εφαρμογή (π.χ. δύο αντίγραφα στον ίδιο υπολογιστή, ή οθόνη σφάλματος που δεν ζωγραφίζεται σε νέο προφίλ), ή ο τρόπος που την ξεκινούσα και τη φωτογράφιζα;

  **Πρώτο βήμα:** επανάληψη με την εφαρμογή ανάπτυξης **κλειστή**, και με τα μάτια του χρήστη, όχι με λήψη οθόνης. Δένει με τα δύο αντίγραφα που βλέπουν το ένα το άλλο ως κατάρρευση — αν αποδειχθεί ότι φταίει η εφαρμογή, είναι πιθανότατα η ίδια οικογένεια.

## Α. Διορθώσεις

### Νοσοκομείο

### Σπίτι

- **Δ4 — Μετά από στιγμιαία διακοπή, η σύνδεση με τη βάση μένει νεκρή για πάντα — και ο φύλακας δεν το βλέπει.**
  *Εύρημα 23/09, στο δίκτυο δοκιμών του σπιτιού — μετρημένο στη βάση και στο κοινό ημερολόγιο σφαλμάτων. Πιθανότατα η αιτία του «η εφαρμογή μου χάνει το δίκτυο» στη δουλειά.*

  **Σενάριο:** ο Popinio δουλεύει στην κοινόχρηστη βάση. Στις 22:49 γίνεται στιγμιαία διακοπή πρόσβασης.

  **Τι μετρήθηκε:**
  1. **Η σύνδεση δεν συνήλθε ποτέ** — από τις 22:49 κάθε εγγραφή του Popinio αποτυγχάνει με «disk I/O error (code 1802)», κάθε λεπτό, για πάνω από 20 λεπτά. Το σημάδι παρουσίας του στη βάση πάγωσε στις 22:48:20. — `operator_presence_repository.dart:80`, `operator_presence_heartbeat.dart:170`
  2. **Χτύπησε και τους δύο σταθμούς** — και η εφαρμογή του PICINIO, από τις 22:49, έπαιρνε κάθε λεπτό «η βάση δεν απάντησε σε 18 δευτερόλεπτα» στο σημάδι παρουσίας της. Σταμάτησε **μόνο** όταν η εφαρμογή ξεκίνησε ξανά (23:05) — δηλαδή με **καινούργια** σύνδεση.
  3. **Όλα τα άλλα δουλεύουν** — την ίδια ώρα το αρχείο ίχνους του Popinio ανανεωνόταν κανονικά και εγγραφή από άλλο πρόγραμμα πέρασε σε 71 ms. Νεκρή είναι μόνο η **ήδη ανοιχτή σύνδεση** κάθε εφαρμογής· μια νέα δουλεύει αμέσως.
  4. **Ο φύλακας λέει «όλα καλά»** — ελέγχει το αρχείο και ανοίγει **καινούργια** σύνδεση για το κλείδωμα· και τα δύο πετυχαίνουν. Η σύνδεση που χρησιμοποιεί η εφαρμογή για όλα δεν ελέγχεται ποτέ. — `database_reachability.dart`
  5. **Αντιφάσκει με παλιότερη μέτρηση** — 08/09 η σύνδεση συνήλθε μόνη της μετά από αποσύνδεση καλωδίου. Εδώ ο κωδικός είναι 1802 (αποτυχία ανάγνωσης στοιχείων αρχείου), δηλαδή πιθανότατα χάθηκε η «λαβή» του αρχείου στον διακομιστή — άλλη μορφή αποτυχίας.

  **Συμβόλαιο:** «Ο φύλακας κρίνει τη σύνδεση που χρησιμοποιεί η εφαρμογή, όχι μια καινούργια.»

  **Ζητούμενο:**
  1. **Ανίχνευση** — επαναλαμβανόμενο σφάλμα I/O στη δική μας σύνδεση να ανάβει την κόκκινη λωρίδα.
  2. **Ανάκαμψη** — επανασύνδεση όταν ο φάκελος απαντά αλλά η σύνδεση όχι. Προσοχή στο μάθημα 04/09: η βιαστική επανασύνδεση είχε κολλήσει την εφαρμογή — πρώτα «τι κρατά την παλιά σύνδεση;».
  3. **Αναπαραγωγή** — να βρεθεί τρόπος να προκληθεί ξανά κατά βούληση (π.χ. αποσύνδεση Wi-Fi του Popinio για λίγα δευτερόλεπτα).

- **Δ5 — Η κοινή βάση αναβαθμίζεται σιωπηλά από τον πρώτο σταθμό που παίρνει νέα έκδοση.**
  *Εύρημα 23/09, στο δίκτυο δοκιμών του σπιτιού (Popinio ανοιχτός στη βάση), κατά τη διόρθωση του φρουρού «ποιος άλλος κρατά τη βάση».*

  **Σενάριο:** δημοσιεύεται έκδοση με αλλαγή σχήματος βάσης. Ο πρώτος σταθμός που ενημερώνεται ανοίγει την κοινόχρηστη βάση — την ίδια που άνοιγε πάντα. Οι συνάδελφοι δουλεύουν ακόμη με την παλιά εφαρμογή.

  **Τι συμβαίνει σήμερα:**
  1. **Καμία ερώτηση, κανένας φρουρός** — η συγκατάθεση για «μόνιμη αναβάθμιση» ζητείται μόνο όταν η βάση **δεν** είναι η τελευταία που άνοιξε ο σταθμός. Για τη συνηθισμένη βάση η αναβάθμιση γίνεται αυτόματα στο άνοιγμα. — `database_helper.dart:733-737`
  2. **Μετρημένο:** βάση σχήματος 62 με τον Popinio ζωντανό πάνω της → ο PICINIO την αναβάθμισε σε 66 μέσα σε 8 δευτερόλεπτα της εκκίνησης, χωρίς κανένα μήνυμα.
  3. **Τι παθαίνουν οι συνάδελφοι:** όποιος ανοίξει μετά με την παλιά εφαρμογή απορρίπτεται («νεότερο σχήμα»). Όποιος την έχει ήδη ανοιχτή συνεχίζει να γράφει με την παλιά εφαρμογή πάνω σε νέο σχήμα — **δεν έχει ελεγχθεί** τι γίνεται τότε.

  **Απόφαση που λείπει:**
  1. Πρέπει ο φρουρός «η βάση είναι ανοιχτή αλλού» να ρωτά και σε αυτή την αυτόματη αναβάθμιση;
  2. Ή είναι αποδεκτό, επειδή όλοι οι σταθμοί ενημερώνονται σύντομα από τον ίδιο φάκελο ενημερώσεων;
  3. Και τι γίνεται με όποιον έχει ήδη ανοιχτή την παλιά εφαρμογή τη στιγμή της αναβάθμισης;

- **Δ6 — Δύο αντίγραφα της εφαρμογής στον ίδιο υπολογιστή «βλέπουν» το ένα το άλλο ως κατάρρευση.**
  *Εύρημα 23/09, στο δίκτυο δοκιμών του σπιτιού — επιβεβαιωμένο στο ημερολόγιο σφαλμάτων.*

  **Σενάριο:** στον PICINIO τρέχει η εφαρμογή ανάπτυξης (ξεκίνησε 22:05) και ανοίγει δίπλα της η κανονική έκδοση, στην ίδια κοινόχρηστη βάση.

  **Τι πάει στραβά:**
  1. **Ψεύτικη κατάρρευση** — η δεύτερη εφαρμογή βρίσκει το ίχνος «τρέχω τώρα» της πρώτης και γράφει ΚΡΙΣΙΜΟ «Η προηγούμενη εκτέλεση δεν τερμάτισε ομαλά (ξεκίνησε 22:05…)» — ενώ η εκτέλεση των 22:05 τρέχει ακόμη. Συνέβη δύο φορές (22:22 και 22:27). — `crash_log_service.dart:209-231`
  2. **Χαμένη ανίχνευση πραγματικής κατάρρευσης** — τα δύο αντίγραφα μοιράζονται το ίδιο αρχείο ίχνους (ένα ανά σταθμό). Όταν το ένα κλείνει ομαλά το σβήνει, και μια κατάρρευση του άλλου μετά από αυτό δεν αφήνει ίχνος. — `crash_log_service.dart:36-37`

  **Συμβόλαιο:** «Το ίχνος "τρέχω τώρα" ανήκει σε μία εκτέλεση, όχι σε έναν υπολογιστή.»

  **Ζητούμενο:** το ίχνος να ξεχωρίζει και την εκτέλεση (π.χ. στο όνομα του αρχείου), ώστε κάθε αντίγραφο να βλέπει και να σβήνει μόνο το δικό του. Προσοχή: τα ίχνη διαβάζει πλέον και ο φρουρός «ποιος άλλος κρατά τη βάση» (`readSessionLivenessMarks`) — η αλλαγή ονόματος πρέπει να τον ακολουθήσει.

  **Δένει με** το λευκό παράθυρο που περιμένει διευκρίνιση: αν αποδειχθεί ότι φταίει η εφαρμογή, είναι πιθανότατα η ίδια οικογένεια.

- **Δ7 — Το κλείσιμο δεν προλαβαίνει το αντίγραφο εξόδου όταν η βάση ζει στο δίκτυο.**
  *Εύρημα 21/09, μετρημένο στο ίχνος κλεισίματος του PC3569.*

  **Τι συνέβη:** το κλείσιμο χτύπησε το όριο ασφαλείας των 20 δευτερολέπτων στη μέση του βήματος «Αντίγραφο ασφαλείας εξόδου» και η εφαρμογή τερματίστηκε βίαια. Τα δύο τελευταία βήματα (κλείσιμο σύνδεσης, κλείσιμο ημερολογίου) δεν έτρεξαν, και η επόμενη εκκίνηση το αναφέρει ως μη ομαλό κλείσιμο.

  **Πού πήγε ο χρόνος — δύο σκέλη:**
  1. **Το αντίγραφο ξεκινά με 10 δευτερόλεπτα στη διάθεσή του** — τα πρώτα τρία βήματα έφαγαν τα μισά από τα 20. Αντιγραφή 15 MB πάνω από δίκτυο, συν εγγραφή στο ιστορικό και στις ρυθμίσεις αντιγράφων, δεν χωράει. — `shutdown_coordinator.dart:78`, `database_exit_backup.dart:68-99`
  2. **Το ίχνος του κλεισίματος τρώει τον χρόνο που υποτίθεται ότι μετρά** — κάθε γραμμή είναι ξεχωριστή **σύγχρονη** εγγραφή με `flush: true` σε δικτυακό αρχείο. Στο συγκεκριμένο ίχνος, η πραγματική δουλειά των τριών πρώτων βημάτων ήταν 3,8 δευτερόλεπτα αλλά το ρολόι έδειξε 10. — `shutdown_trace_service.dart:302-311`

  **Δεύτερη μέτρηση, 21/09 στις 10:35 — πέρασε ΟΡΙΑΚΑ.** Το ίδιο κλείσιμο ολοκληρώθηκε ομαλά και τα έξι βήματα πέρασαν, αλλά το αντίγραφο εξόδου κράτησε **15,96 από τα 20 δευτερόλεπτα** — το 80% του ορίου για ένα βήμα. Δεν είναι σφάλμα που χτυπά πάντα· είναι όριο που περνιέται ή χάνεται ανάλογα με το πόσο απασχολημένο είναι το δίκτυο εκείνη τη στιγμή. Γι' αυτό το ίδιο βήμα διακόπηκε στις 08:38 και πέρασε στις 10:35.

  **Συμβόλαιο:** «Το όριο ασφαλείας μετρά τη δουλειά του κλεισίματος, όχι την καταγραφή της.»

  **Απόφαση που λείπει — τρεις δρόμοι, όχι απαραίτητα αποκλειστικοί:**
  1. Το αντίγραφο εξόδου να παίρνει δικό του παράθυρο χρόνου αντί να μοιράζεται το συνολικό.
  2. Το ίχνος να γράφεται τοπικά και να ανεβαίνει μία φορά στο τέλος.
  3. Το αντίγραφο να μην γίνεται στο κλείσιμο όταν η βάση είναι δικτυακή.

  **Τι ΔΕΝ χάθηκε — μην το ξαναψάξεις:** το αντίγραφο `2026-09-21_08-38_call_logger.db` γράφτηκε **ακέραιο** (3695 σελίδες δηλωμένες, 3695 παρούσες), και η βάση έμεινε χωρίς υπολείμματα (κανένα -journal / -wal). Η διακοπή έπεσε στην καταγραφή μετά την αντιγραφή, όχι στην ίδια την αντιγραφή.

- **Δ8 — Ο έλεγχος «έγραψε άλλος;» ξαναφορτώνει τα πάντα κάθε λεπτό, επειδή η παρουσία μετράει ως αλλαγή δεδομένων.**
  *Σημείωση Διευθυντή 23/09, με τα γεγονότα του κώδικα από κάτω.*

  **Η αρχική ιδέα:** ο σταθμός που κάνει αλλαγή να ειδοποιεί τους άλλους, αντί να ρωτούν όλοι κάθε 12 δευτερόλεπτα — «σε πραγματικές συνθήκες η βάση μένει ώρες χωρίς εγγραφή».

  **Διόρθωση παρανόησης:** κάθε 12 δευτερόλεπτα **δεν** παίρνεται στιγμιότυπο της βάσης. Γίνεται ένα μικροσκοπικό ερώτημα («άλλαξε ο μετρητής αλλαγών;») που δεν διαβάζει δεδομένα. Το στιγμιότυπο γίνεται μόνο στον έλεγχο ακεραιότητας και στο αντίγραφο ασφαλείας. — `shared_database_refresh.dart:23`

  **Τι κοστίζει πραγματικά σήμερα:**
  1. **Το ερώτημα των 12 δευτερολέπτων** — ελαφρύ· δεν έχει μετρηθεί ως πρόβλημα.
  2. **Το σημάδι «είμαι εδώ» κάθε λεπτό** — είναι **εγγραφή**. Άρα η βάση δεν μένει ποτέ «ώρες χωρίς εγγραφή»: με δύο σταθμούς ο μετρητής αλλάζει κάθε ~30 δευτερόλεπτα.
  3. **Η συνέπεια** — ο κάθε σταθμός βλέπει «άλλαξε» και ξαναφορτώνει όλες τις λίστες (εκκρεμότητες, τμήματα, κατάλογο, κλήσεις) κάθε λεπτό, **χωρίς να έχει αλλάξει τίποτα ουσιαστικό**. Μετρημένο ~1 δευτερόλεπτο ανάγνωσης από το δίκτυο ανά φορά, στη βάση των 13 MB. — `operator_presence_heartbeat.dart`, `shared_database_refresh.dart:39-60`

  **Γιατί η «ειδοποίηση» απορρίφθηκε:** στα Windows δεν υπάρχει αξιόπιστος τρόπος ένας σταθμός να «σπρώξει» μήνυμα στους άλλους μέσω κοινόχρηστου φακέλου χωρίς διακομιστή· οι ειδοποιήσεις αλλαγών φακέλου μέσω δικτύου χάνονται σιωπηλά σε διακοπές. Μια ειδοποίηση που χάθηκε ισοδυναμεί με μπαγιάτικη οθόνη χωρίς να το ξέρει κανείς — τρύπα στον φρουρό. **Η ερώτηση ανά 12 δευτερόλεπτα μένει** ως το αξιόπιστο δίχτυ.

  **Τι ευσταθεί αντί γι' αυτήν:** να πάψει το σημάδι παρουσίας να μετράει ως «αλλαγή δεδομένων» — π.χ. να ζει εκτός βάσης (το αρχείο ίχνους υπάρχει ήδη) ή οι σταθμοί να αγνοούν αλλαγές που είναι μόνο παρουσία. Τότε οι λίστες ξαναφορτώνονται **μόνο** όταν κάποιος γράψει πραγματικά.

  **Απόφαση που λείπει:**
  1. Αποδεκτή η αναδιατύπωση (ίδιος έλεγχος, αλλά χωρίς ψεύτικες «αλλαγές» από την παρουσία);
  2. Η παρουσία στη βάση τροφοδοτεί και την ανάθεση και το «ποιος είναι συνδεδεμένος» — τι χάνεται αν φύγει από εκεί;
  3. Εναλλακτικά: μπορεί να φορτώνεται **μόνο** η αλλαγή που συνέβη αντί για όλες τις λίστες; Είναι πιο φθηνό, ή δεν είναι εφικτό τεχνικά;

- **Δ9 — Ο τίτλος γράφεται στην κλήση αλλά δεν επιστρέφει ποτέ στη φόρμα.**
  *Σημείωση Διευθυντή 24/09 («δεν αποθηκεύει τον τίτλο»)· ιχνηλατήθηκε στον κώδικα — η εγγραφή γίνεται, η **ανάγνωση** λείπει.*

  **Σενάριο:** στην Αναφορά Lansweeper ο χειριστής γράφει δικό του τίτλο και πατά «Αποθήκευση στην κλήση». Γυρίζει αργότερα στην ίδια κλήση: ο τίτλος του **δεν είναι εκεί** — στη θέση του ο αυτόματος.

  **Τι πάει στραβά — δύο βήματα που αλληλοτροφοδοτούνται:**
  1. **Η προσυμπλήρωση αγνοεί τον αποθηκευμένο τίτλο.** Η `prefillTitle` επιστρέφει **πάντα** τον αυτόματο τίτλο από κατηγορία και αριθμό κλήσης· το `call.title` δεν διαβάζεται ποτέ. Η περιγραφή και η λύση φορτώνονται κανονικά από τις κλήσεις — ο τίτλος είναι η μόνη εξαίρεση, μέσα στην ίδια συνάρτηση. — `lansweeper_ai_presenter.dart:114-115`, `lansweeper_report_ai.dart:40-55`
  2. **Η δεύτερη αποθήκευση τον σβήνει από την εικόνα.** Επειδή το πεδίο δείχνει πλέον τον αυτόματο τίτλο, η `callTitleToPersist` κρίνει «ίδιος με τον αυτόματο» και επιστρέφει `null`· το `title` δεν μπαίνει καν στα πεδία που γράφονται. Ο παλιός τίτλος μένει στη βάση, αόρατος και ανενημέρωτος. — `lansweeper_sync_service.dart:720-727`, `calls_lansweeper_repository.dart:295-300`

  **Τι ΔΕΝ φταίει — μην το ξαναψάξεις:** η εγγραφή στη βάση είναι σωστή. Όταν ο τίτλος διαφέρει από τον αυτόματο, γράφεται κανονικά στη στήλη `title` μαζί με περιγραφή και λύση.

  **Συμβόλαιο:** «Ό,τι γράφει η φόρμα στην κλήση, η φόρμα το ξαναδιαβάζει.»

  **Ζητούμενο:** η προσυμπλήρωση να δείχνει τον αποθηκευμένο τίτλο της κλήσης όταν υπάρχει, και τον αυτόματο μόνο όταν δεν υπάρχει — όπως ήδη κάνουν η περιγραφή και η λύση.

- **Δ10 — Στον χάρτη κτιρίου, το τμήμα που σχεδιάζεις δεν δείχνει το όνομά του.**
  *Σημείωση Διευθυντή 24/09· αιτία εντοπισμένη στον κώδικα.*

  **Σενάριο:** σχεδιάζεις νέο γραφείο στην κάτοψη. Το σχήμα φαίνεται, το όνομα όχι. Για να ρυθμίσεις τη θέση και το μέγεθος της ετικέτας πρέπει πρώτα να πατήσεις αποθήκευση και μετά να ξαναμπείς σε επεξεργασία — δύο βήματα για κάτι που θα έπρεπε να φαίνεται από την πρώτη στιγμή.

  **Τι πάει στραβά:** η ετικέτα διαβάζει τη γεωμετρία του προσχεδίου **μόνο σε λειτουργία επεξεργασίας** (`toolMode == MapToolMode.edit`). Σε λειτουργία σχεδίασης (`draw`) πέφτει πίσω στα αποθηκευμένα `mapX/mapY/mapWidth/mapHeight` του τμήματος — που για ένα σχήμα υπό σχεδίαση είναι ακόμη κενά, οπότε η συνάρτηση επιστρέφει `null` και δεν ζωγραφίζεται καμία ετικέτα. — `building_map_label_layout.dart:344-355`

  **Ζητούμενο:** η ετικέτα να ακολουθεί το προσχέδιο και στη σχεδίαση, όχι μόνο στην επεξεργασία — ώστε το όνομα να φαίνεται από τη στιγμή που ορίζεται το σχήμα.

- **Δ11 — Ο διάλογος επιλογής αρχείου ανοίγει όσες φορές τον πατήσεις· ο φρουρός υπάρχει και τον παρακάμπτουν δέκα σημεία.**
  *Σημείωση Διευθυντή 24/09 για το Λεξικό· η σάρωση έδειξε ότι το θέμα είναι πολύ πλατύτερο.*

  **Σενάριο:** στο Λεξικό πατάς «επιλογή αρχείου». Ο διάλογος των Windows αργεί να φανεί, ξαναπατάς, και ανοίγουν δύο — ή πέντε. Στην επιλογή βάσης αυτό δεν συμβαίνει.

  **Η λύση υπάρχει ήδη και δουλεύει.** Ο `FilePickerSession` κρατά μία ενεργή συνεδρία και, στο δεύτερο κλικ, **εστιάζει τον ήδη ανοιχτό διάλογο** αντί να ανοίξει δεύτερο. — `file_picker_session.dart`

  **Το πραγματικό μέγεθος:** εννέα σημεία τον χρησιμοποιούν, **δέκα τον παρακάμπτουν** — δηλαδή λίγο περισσότερα από τα μισά είναι απροστάτευτα:
  1. **Λεξικό — τρία αρχεία**, αυτά που ανέφερε ο Διευθυντής. — `dictionary_manager_screen.dart:677`, `core_lexicon_setup_dialog.dart:80`, `dictionary_settings_dialog.dart:284` και `:341`
  2. **Χάρτης κτιρίου — δύο σημεία.** — `building_map_controller.dart`, `building_map_sheet_export_save_path.dart`
  3. **Εξαγωγές και εκτυπώσεις — τρία σημεία.** — `dashboard_export_launcher.dart`, `task_print_launcher.dart`, `profile_settings_export.dart`
  4. **Λάμπα και εργαλεία απομακρυσμένης σύνδεσης — δύο σημεία.** — `lamp_resolution_progress_dialog.dart`, `remote_tool_form_dialog.dart`

  **Συμβόλαιο:** «Κάθε άνοιγμα διαλόγου αρχείου περνά από τον ίδιο φρουρό — κανένα σημείο δεν τον παρακάμπτει.»

  **Ζητούμενο:** τα δέκα σημεία να τυλιχτούν στον υπάρχοντα φρουρό. Καμία νέα λογική· η ίδια συνάρτηση που ήδη προστατεύει την επιλογή βάσης. Αξίζει επίσης **έλεγχος αρχιτεκτονικής** που να μη δέχεται νέα απευθείας κλήση του επιλογέα — αλλιώς η λίστα θα ξαναγεμίσει.

## Β. Προσθήκες (νέες λειτουργίες)

### Νοσοκομείο

- **Π1 — Το ίδιο προφίλ ανοιχτό σε δύο θέσεις ταυτόχρονα.**
  *Ερώτημα Διευθυντή, αποσαφηνίστηκε 24/08: πρόκειται για την περίπτωση όπου διαλέγω προφίλ που κάποιος άλλος το έχει ήδη ανοιχτό σε άλλο μηχάνημα.*

  **Σήμερα δεν υπάρχει καμία διένεξη — υπάρχει μόνο πληροφορία.** Ο επιλογέας ταυτότητας δείχνει, κάτω από κάθε προφίλ, τους σταθμούς όπου είναι αυτή τη στιγμή ανοιχτό (γιατί «το ίδιο προφίλ μπορεί να δουλεύει από δύο θέσεις» ήταν συνειδητή παραδοχή). Δεν προειδοποιεί, δεν εμποδίζει, και δεν εξαιρεί κανέναν. — `operator_presence_summary.dart:36-48`, `selectable_profiles.dart`

  **Απόφαση που λείπει:** τι πρέπει να συμβαίνει.
  1. **Προειδοποίηση χωρίς φραγή** — «ο Βλάσης το έχει ανοιχτό στον ΣΤΑΘΜΟ-7· να συνεχίσω;». Η πληροφορία υπάρχει ήδη, λείπει η στιγμή που ρωτά.
  2. **Φραγή** — και τότε γεννιέται αμέσως το πρόβλημα των φαντασμάτων: μια εφαρμογή που κρασάρισε αφήνει πίσω της ίχνος «ανοιχτό», και ο άνθρωπος κλειδώνεται έξω από τον εαυτό του.
  3. **Εξαίρεση του διαχειριστή** — το ρητό ερώτημα του Διευθυντή («είναι σωστή πρακτική;»). Παρατήρηση: η εξαίρεση έχει νόημα ως **διέξοδος** από την περίπτωση 2 (κάποιος πρέπει να μπορεί να μπει όταν το μητρώο λέει ψέματα), όχι ως προνόμιο.

     **Και αυτό είναι μάλλον υπέρ της:** αφού αποφασίστηκε ότι τα προφίλ δεν αποκτούν κωδικούς (17/09/2026), η εξαίρεση είναι διαθέσιμη σε **όποιον** διαλέξει προφίλ διαχειριστή. Ως προνόμιο θα ήταν ψέμα· ως διέξοδος από κλείδωμα είναι ακριβώς ό,τι χρειάζεται — κανείς δεν μένει έξω επειδή μια εφαρμογή κόλλησε σε άλλο μηχάνημα.

  **Προϋπόθεση:** ό,τι επιλεγεί στηρίζεται στην πολιτική «μετά από πόση σιωπή θεωρείται νεκρή μια συνεδρία», που είναι ακριβώς το ανοιχτό σημείο του μητρώου συνεδριών. Οι δύο αποφάσεις είναι μία.

  **Σημείωση αρίθμησης:** παίρνει τη θέση 1 παρά το «Απόφαση που λείπει», επειδή είναι η **μόνη** Προσθήκη του Νοσοκομείου και η διαίρεση Νοσοκομείο/Σπίτι προηγείται. Το πρώτο **εκτελέσιμο σήμερα** είναι ο επεξεργαστής ιστορικού αλλαγών, αμέσως παρακάτω.

### Σπίτι

- **Π2 — Επεξεργαστής ιστορικού αλλαγών μέσα στην εφαρμογή (Unreleased).**
  *Αίτημα χρήστη 03/08, μαζί με την καθιέρωση του `changelog.json` ως μοναδικής πηγής αλήθειας.*

  **Πλαίσιο:** ο Διευθυντής διορθώνει περιστασιακά κακογραμμένο κείμενο εγγραφών· η χειροκίνητη επεξεργασία JSON είναι επικίνδυνη — ένα λάθος κόμμα σπάει το in-app ιστορικό ΚΑΙ τη Δημοσίευση. Οι ημερομηνίες δεν χρειάζονται επεξεργασία, σφραγίζονται αυτόματα.

  **Ζητούμενο:** υποτυπώδης επεξεργαστής στα «Σενάρια σφαλμάτων» ή στον διάλογο «Ιστορικό Αλλαγών» (debug): επεξεργασία, διαγραφή και μετακίνηση γραμμών του Unreleased ανά κατηγορία, με επικύρωση JSON πριν την αποθήκευση.

- **Π3 — Διαγνωστικά μέσα από την εφαρμογή: προβολή αρχείων καταγραφής ΚΑΙ ανάγνωση του Windows Event Log.**
  *Επανα-αίτημα χρήστη 28/07 («όταν κάτι στραβώσει στο μηχάνημα συναδέλφου, σήμερα δεν έχεις κανένα στοιχείο»)· απόφαση Διευθυντή 13/08: το σκέλος Windows Event Log ΜΕΝΕΙ — η εφαρμογή να δείχνει τι έχουν καταγράψει τα Windows για την «Καταγραφή Κλήσεων».*

  1. **Πυρήνας:** προβολή των `errors_YYYY-MM-DD.log` και των ιχνών τερματισμού μέσα από την εφαρμογή — τηλεδιάγνωση στο μηχάνημα συναδέλφου χωρίς εξερεύνηση φακέλων. Η αξία ανέβηκε με την παράδοση της Λάμπας.
  2. **Σφάλματα των Windows:** πάνελ που διαβάζει τις εγγραφές Application Error / Windows Error Reporting για το δικό μας εκτελέσιμο και τις δείχνει δίπλα στα δικά μας logs. **Τεχνικά εφικτό:** ερώτημα στο ημερολόγιο Application φιλτραρισμένο στο exe (π.χ. `wevtutil`/`Get-WinEvent` με XPath σε `EventData = call_logger.exe`)· η ανάγνωση του Application log δεν απαιτεί δικαιώματα διαχειριστή.
  3. **Ιστορικό:** το κυνήγι των APPCRASH 0xc0000005 ΕΚΛΕΙΣΕ — η ρίζα (κατάρρευση στο teardown) βρέθηκε και διορθώθηκε, δοκιμασμένο και στους συναδέλφους. Το πάνελ αφορά τα μελλοντικά άγνωστα, όχι εκείνη την ιστορία.

  **Δένει με** τα δύο αντίγραφα που μοιράζονται αρχείο ίχνους: χωρίς αυτή τη διόρθωση, το πάνελ θα δείχνει ψεύτικες καταρρεύσεις.

- **Π4 — Τύποι οντότητας στο ιστορικό εφαρμογής.**
  *Ενημέρωση 13/08: οι τύποι πλέον λέγονται Εισαγωγή Δεδομένων, Συντήρηση, Αντίγραφο ασφαλείας και μαζικές ενέργειες (χρηστών, τμημάτων, εξοπλισμού).* — `application_audit_tab.dart:769`

  **Απόφαση που λείπει:** ανήκουν πράγματι αυτές οι μη-οντότητες στη λίστα τύπων του φίλτρου;

- **Π5 — Ανενεργός εξοπλισμός: τύπος «Χαλασμένο» ή κατάσταση;**
  *Ερώτημα Διευθυντή («είναι σωστή προσέγγιση ή ευκαιριακή τακτική που αργότερα θα μου δημιουργήσει προβλήματα;»)· απογραφή στον κώδικα 24/08.*

  **Απάντηση στο πρώτο σκέλος («πού χειρίζομαι τους τύπους εξοπλισμού;»):** στον Κατάλογο → Εξοπλισμός → ρυθμίσεις εξοπλισμού, πεδίο «Τύποι εξοπλισμού». — `equipment_settings_dialog.dart:113`

  **Γιατί ο τύπος ΔΕΝ μπορεί να κουβαλήσει ιδιότητες, όπως είναι σήμερα:**
  1. **Ο κατάλογος τύπων είναι μία γραμμή κειμένου με κόμματα**, με προεπιλογή «Υπολογιστής, Εκτυπωτής». Δεν είναι πίνακας, δεν έχει κωδικό, δεν έχει πεδία. — `settings_service_catalogs.dart:214-231`
  2. **Οι τύποι χρησιμοποιούνται μόνο ως λίστα προτάσεων** στη φόρμα εξοπλισμού και στη μαζική επεξεργασία. Κανένα σημείο του κώδικα δεν ρωτά «τι τύπος είναι αυτό» για να αλλάξει συμπεριφορά. — `equipment_form_dialog.dart:729`, `bulk_equipment_edit_dialog.dart:208`
  3. **Ο εξοπλισμός δεν έχει καθόλου έννοια κατάστασης.** Οι στήλες του είναι κωδικός, τύπος, σημειώσεις, εργαλείο, τμήμα, θέση, όνομα Lansweeper, και σημαία διαγραφής — τίποτα σαν «ενεργό/ανενεργό». — `database_foreign_keys.dart:91-103`

  **Η ουσία της ένστασης:** ο τύπος απαντά «**τι είναι**» (υπολογιστής, εκτυπωτής). Το «χαλασμένο» απαντά «**σε τι κατάσταση είναι**». Ένας χαλασμένος εκτυπωτής δεν παύει να είναι εκτυπωτής — αν το «Χαλασμένο» γίνει τύπος, χάνεται η πληροφορία τι μηχάνημα είναι, και τα στατιστικά ανά τύπο αρχίζουν να λένε ψέματα. Η υποψία του Διευθυντή είναι σωστή: είναι ευκαιριακή τακτική.

  **Απόφαση που λείπει:**
  1. **Ξεχωριστή κατάσταση εξοπλισμού** (ενεργός / χαλασμένος / αποσυρμένος) ως δικό της πεδίο, ώστε τύπος και κατάσταση να συνυπάρχουν.
  2. **Τι αλλάζει η κατάσταση** — απόκρυψη από λίστες προτάσεων, εξαίρεση από τους ελέγχους ακεραιότητας, ένδειξη στην καρτέλα, εξαίρεση από τα στατιστικά «πόσο συχνά χαλάει»;
  3. **Ό,τι επιλεγεί, ο κατάλογος τύπων μένει ελεύθερο κείμενο** ή γίνεται κανονικός πίνακας; Το δεύτερο είναι προϋπόθεση για οτιδήποτε κρεμαστεί ποτέ πάνω στον τύπο — και δένει με το Κτίριο τμήματος και τις Ομάδες τμημάτων, που ρωτούν το ίδιο πράγμα.

- **Π6 — Συντήρηση Βάσης Γνώσης με ΤΝ (υπόλοιπο).**
  *Τα Μέρη Α και Β ολοκληρώθηκαν 09/08 — μένει μόνο η μαζική οργάνωση.*

  **Τι επαληθεύτηκε ως κλειστό — μη το ξαναζητήσεις:**
  1. **Η λύση και η καθαρισμένη περιγραφή ζουν πάνω στην κλήση**, με προέλευση και χρονοσφραγίδα (`refined_source`/`refined_at`). *Ενημέρωση v43:* κάθε κλήση έχει πλέον **ΜΙΑ** Περιγραφή — το ξεχωριστό πεδίο «καθαρού» κειμένου καταργήθηκε· η προέλευση δείχνει αν πέρασε από ΤΝ (εικονίδιο δίπλα στην Περιγραφή).
  2. **Και οι δύο έξοδοι της φόρμας Lansweeper** γράφουν πίσω (υποβολή API και «Αντιγραφή & Άνοιγμα»), σε όλες τις κλήσεις του ticket.
  3. **Ευρετήριο αναζήτησης κλήσεων** — η κλήση βρίσκεται και από το παλιό «καθαρό» κείμενο των υπαρχουσών εγγραφών.
  4. **Πίνακας `knowledge_base`** — πλήρες σχήμα (σύμπτωμα, λύση, κατηγορία, ετικέτες, μετρητής χρήσης, κλήση προέλευσης, ευρετήριο).
  5. **Οθόνη Βάσης Γνώσης** — λίστα, αναζήτηση, φίλτρο κατηγορίας, δημιουργία/επεξεργασία/διαγραφή με ίχνος στο Ιστορικό.
  6. **«Αποθήκευση ως γνώση»** στη φόρμα Lansweeper, με ερώτηση όταν υπάρχει ήδη παρόμοιο άρθρο.
  7. **Placeholder `{Γνώση}`** στην προτροπή ΤΝ, με τα σχετικά άρθρα ως παραδείγματα.

  **Ζητούμενο (τι απομένει):** κουμπί **«Οργάνωση με ΤΝ»** στην οθόνη Βάσης Γνώσης.
  1. **Εντοπισμός διπλών** σε ολόκληρη τη βάση, με πρόταση συγχώνευσης — ο σημερινός έλεγχος πιάνει μόνο τη στιγμή της δημιουργίας.
  2. **Προτάσεις τίτλων και λέξεων-κλειδιών** για άρθρα που γράφτηκαν βιαστικά.
  3. **Πρόταση ενοτήτων/κατηγοριών** όταν μαζευτούν αρκετά άρθρα του ίδιου είδους.

  Όλα ως **προτάσεις προς έγκριση με κλικ**, ποτέ αυτόματη εφαρμογή: η εβδομαδιαία συντήρηση γίνεται μόνο αν κοστίζει πέντε λεπτά. Ίδιο μοτίβο με τη μαζική επεξεργασία λέξεων Λεξικού από ΤΝ — τα δύο αξίζει να σχεδιαστούν μαζί.

  **Όρια που μένουν ανοιχτά (επαλήθευση 13/08):**
  1. **Δεν υπάρχει υπόδειξη γνώσης την ώρα της κλήσης** — τα άρθρα φτάνουν στην ΤΝ, αλλά η φόρμα κλήσης δεν δείχνει «υπάρχει γνωστή λύση» ενώ πληκτρολογείτε (ο `KnowledgeMatcher` δεν χρησιμοποιείται από τη φόρμα).
  2. **Ο μετρητής χρήσης (`times_used`) ανεβαίνει μόνο από τη ροή της Αναφοράς** (`markUsed`) — όχι όταν ένα άρθρο απλώς τροφοδοτεί την προτροπή ΤΝ.

- **Π7 — Μαζική επεξεργασία λέξεων Λεξικού από ΤΝ.**
  Εικονίδιο επεξεργασίας προτροπής δίπλα στην «Ερώτηση ΤΝ»· επιλογή 100–200 λέξεων και μαζική διόρθωσή τους.

  **Απόφαση που λείπει:** ο μηχανισμός θέλει ξεχωριστό σχεδιασμό — μαζί με τη συντήρηση της Βάσης Γνώσης, που ρωτά το ίδιο πράγμα.

- **Π8 — Παράλληλη κλήση.**
  Δεύτερη ενεργή κλήση ως καρτέλα ή ελαχιστοποιημένη σε ιπτάμενο chip με τρέχον χρονόμετρο και επαναφορά με κλικ· η ανοιχτή κλήση κατεβαίνει κάτω· προειδοποίηση στις 3 ανοιχτές.

  **Απόφαση που λείπει:** μεγάλο έργο — θέλει σχεδιασμό κατάστασης πολλών φορμών πριν από οτιδήποτε άλλο. Καρτέλα με χρονόμετρο στην αρχική κλήση;

## Συντήρηση κώδικα

*Περιοδική εργασία — η ενότητα ΔΕΝ αφαιρείται όταν εκτελεστεί.*

**Πότε τρέχει:** το πρώτο σκέλος σε κάθε `/sync` (υπόδειξη, όχι αυτόματη εκτέλεση)· τα υπόλοιπα πριν από ανέβασμα έκδοσης· η αναβάθμιση SDK κάθε τρίμηνο.

### 1. Μορφοποίηση κώδικα

**Πότε έτρεξε:** 21/08/2026, 30/08/2026 (πλήρες πέρασμα), 11/09/2026 (46 αρχεία σε δικό τους γύρο).

**Είναι το μόνο σκέλος που μαζεύει υπόλοιπο** — στη μέτρηση της 30/08 συγκεντρώνονταν ~10 αρχεία την ημέρα. Γι' αυτό ο έλεγχος μετακινήθηκε στο `/sync`: μορφοποιούνται **ονομαστικά τα αρχεία που άλλαξαν**, ποτέ ολόκληρος φάκελος (η σάρωση φακέλου πειράζει και αμορφοποίητα αρχεία τρίτων και κάνει το diff αδιάβαστο).

**Παγίδα που επιβεβαιώθηκε:** το `dart format` ισοπεδώνει CRLF σε LF. Δεν είναι πρόβλημα εδώ — με `core.autocrlf=true` το `git diff` βγαίνει ταυτόσημο με και χωρίς `--ignore-cr-at-eol`. Οι προειδοποιήσεις «LF will be replaced by CRLF» είναι θόρυβος.

### 2. Αναβάθμιση Flutter SDK και βιβλιοθηκών

**Πότε τρέχει:** κάθε ~3 μήνες. **Τελευταία: 31/08/2026** (SDK 3.47.2, Dart 3.13.2). **Επόμενη: τέλη Οκτωβρίου 2026.**

**Πάντα σε ξεχωριστό branch, ποτέ σε ώρα παράδοσης.** Κύκλος: `flutter upgrade` (stable, 1–2 βδομάδες μετά από μεγάλη έκδοση) → `flutter pub upgrade` → ψαλίδισμα overrides → `flutter analyze` + σουίτα → smoke test.

**Τρεις παγίδες που κοστίζουν ώρες αν ξεχαστούν:**
1. **Το golden test βγαίνει αναμενόμενα κόκκινο** μετά από άλμα μηχανής — το anti-aliasing αλλάζει στις στρογγυλεμένες γωνίες. Κοίτα την εικόνα διαφοράς **πριν** τρέξεις `--update-goldens`: αν η διαφορά είναι στο περιεχόμενο, δεν είναι η μηχανή.
2. **Τα νέα lint του SDK είναι δωρεάν εύρεση σφαλμάτων** — μην τα καταπίνεις. Το Dart 3.13 αποκάλυψε έτσι **7 πραγματικά σφάλματα**.
3. **Τα πακέτα που ανήκουν στο SDK** (`analyzer`, `test`, `meta`, …) δεν αναβαθμίζονται μόνα τους — έρχονται μαζί με το SDK, και τα `dependency_overrides` πάνω τους δουλεύουν εναντίον της τακτικής.

### 3. Έλεγχοι πριν από ανέβασμα

*Στη μέτρηση της 30/08 και τα τέσσερα βγήκαν **μηδενικά** — γι' αυτό δεν αξίζουν περιοδικότητα, μόνο πέρασμα πριν από έκδοση.*

1. **Αχρησιμοποίητες εξαρτήσεις** — τρία πακέτα δεν γίνονται ποτέ `import` αλλά **χρειάζονται**: το `sqlite3_flutter_libs` δίνει το native `sqlite3.dll`, το `cupertino_icons` ελέγχεται από τον φρουρό ακεραιότητας εκκίνησης, το `flutter_lints` μπαίνει από το `analysis_options.yaml`. — `startup_asset_integrity_service.dart:43`
2. **Ορφανά assets** — μόνο το `assets/.gitkeep`, που υπάρχει σκόπιμα. Οι φάκελοι που διαβάζονται δυναμικά μέσω `AssetManifest` (`splash/`, `avatars/`, `dictionaries/`, `floor_plans/`) εξαιρούνται εξ ορισμού.
3. **`TODO`/`FIXME`/`HACK` και σκέτο `print(`** στο `lib/`.
4. **`flutter analyze` + πλήρης σουίτα.** Δύο σουίτες ταυτόχρονα κλειδώνουν το `sqlite3.dll` — ο έλεγχος τρέχει μία φορά, σειριακά.

## Μηδενισμός έκδοσης βάσης

**Πότε τρέχει:** μόλις δημοσιευτεί η έκδοση 1 — δηλαδή όταν ολοκληρωθούν οι Διορθώσεις και οι Προσθήκες αυτού του αρχείου. Δεν έχει σειρά προτεραιότητας γιατί δεν μπορεί να ξεκινήσει νωρίτερα.

Εκκαθάριση του `lib/core/database/database_helper.dart`: περιέχει πολλές εκδόσεις βάσης και πολλά άχρηστα `IF`, αφού η δομή της βάσης σταθεροποιήθηκε. Στόχος ο μηδενισμός της έκδοσης βάσης σε v1.
