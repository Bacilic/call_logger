# Βάση Δεδωμένων

Εκαθάριση του lib\core\database\database_helper.dart μετά την ολοκλήρωση της εφαρμογής. Περιέχει πολλές εκδόσεις βάσης και πολλά άχριστα IF αφού η δομή της βάσης σταθεροποιήθηκε

# Διωρθώσεις
- Να προστεθεί λειτουργία μετακίνησης εικόνας κατά την σχεδίαση του ορόφου. Στο ισόγιο δεν μπορώ να δω όλο τον όροφο και πρέπει να τον μετακινήσω ώστε να εστιάσω πχ στα μαγειρια

- Να υλοποιηθεί η λειτουργία ανά ομάδα στα τμήματα. Δηλαδή. Εργαστήρια, κληνικές, διοίκηση, τεχνική υπερεσία, κλπ

- Σε νέο εξοπλησμο να δούμε το πεδιο τοποθεσία όταν συμπληρώνω τμήμα.

- Στην κάρτα του υπαλλήλου στην οθόνη κατάλογος να φαίρουμε και τον εξοπλισμό. Σκέψεις για μόνο προβλή ή και επεξεργασία;

- Στην ολοκλήρωση εκκρεμότητας να δείχνει και το χρόνο ολοκλήρωσης (έχουμε διαδικασία). Ένα χρόνος από τη δημιουργία της εκκρεμότητας και άλλος από την τελευταία αναβολή
- Το κείμενο στην ολοκλήρωση εκκρεμότητας να μην επεκτείνεται προς τα δεξιά αλλά να αναδιπλώνεται
Ορθογραφικός έλεγχος στις σημειώσεις (λόγος) αναβολής εκκρεμότητας
- Διαδικασία-τρόπος ώστε μία εκκρεμότητα να μετατρέπεται σε ticket στο Lansweeper
- Στην αναφορά κλήσεων στην κάρτα των κορυφαίων όταν το πρώτο είναι Άγνωστο να μην δείχνει παύλα (αλλά) “Χωρίς Τμήμα”, επίσης να εμφανίζει και την μπάρα με υπόδειξη όπως τα άλλα τμήματα: ”Χωρίς Τμήμα: 9 κλήσεις”
- Αξιοποίηση των σημειώσεων στις κλήσεις, όταν ετοιμάζονται για lansweeper. Πολύ καλό ιστορικό
- Η αποθήκευση της προτροπής να μην βγαίνει ως snacbar πίσω από 2 διαλόγους! αλλά στο ίδιο επίπεδο με το διάλογο της προτροπής
- Στην Αναφορά lansweeper να υπάρχει μετρητής ανά κατηγορία
- Κάπου συντόμευση που να οδηγή στην Αναφορά lansweeper
- Αναδιαμόρφωση: Τα αρχεία και οι κλάσεις-συναρτήσεις που χρησιμοποιούν το όνομα gemini (πχ gemini_prompt_template_field_test.dart) να γίνουν ποιο γενικές: Τεχνητή Νοημοσύνη (αργότερα μπορεί να μπούν και άλλα api)

## Άξονες Ελέγχου Ποιότητας

Οι παρακάτω άξονες είναι προτροπές ελέγχου που εκτελούνται **επαναληπτικά** σε διαφορετικές φάσεις της ανάπτυξης — όχι μία φορά. Οι έλεγχοι εξετάζουν τον **κώδικα**, όχι τα δεδομένα (εκτός από τον Άξονα 6), οπότε ο όγκος της βάσης δεν παίζει ρόλο. Κάτω από κάθε τίτλο σημειώνεται η κατάλληλη χρονική στιγμή εκτέλεσης.

### Άξονας 1 — Διαρροές Κατάστασης (State Leaks)

> **Πότε:** Μετά από κάθε νέο feature που γράφει στη βάση (νέος provider ή νέα mutation), και οπωσδήποτε μία φορά πριν την καθημερινή χρήση στη δουλειά. Εκτελέστηκε ήδη μία φορά (πριν την αναδόμηση του καταλόγου, Ιούλιος 2026) — αξίζει επανάληψη επειδή η αναδόμηση άλλαξε τα repositories που καλούν οι providers.

```
Αναλύεις τους Riverpod providers της εφαρμογής call_logger για πιθανές διαρροές κατάστασης.

1. Εξέτασε κάθε αρχείο provider που περιέχει εγγραφή στη βάση δεδομένων (insert/update/delete).
   Τα αρχεία που σε ενδιαφέρουν βρίσκονται στα:
   @call_entry_provider.dart @smart_entity_selector_provider.dart @call_header_provider.dart
   @tasks_provider.dart @task_service_provider.dart
   @department_directory_provider.dart @equipment_directory_provider.dart
   @category_directory_provider.dart @directory_provider.dart

2. Για κάθε StateNotifier / AsyncNotifier που εκτελεί mutation:
   - Καταγράφεις ποιους άλλους providers επηρεάζει λογικά η αλλαγή αυτή.
   - Ελέγχεις αν μετά την εγγραφή υπάρχει ρητή κλήση ref.invalidate / ref.refresh
     στους εξαρτημένους providers.
   - Σημειώνεις περιπτώσεις όπου ο εξαρτημένος provider δεν ενημερώνεται
     και το UI μπορεί να εμφανίζει ξεπερασμένα δεδομένα.

3. Ελέγχεις επίσης:
   - Providers που χρησιμοποιούν ref.watch σε άλλον provider αλλά δεν
     αντιδρούν όταν αυτός αλλάζει (π.χ. επειδή η εξάρτηση είναι σε
     παλιό snapshot και όχι στο reactive stream).
   - AsyncNotifier που κάνουν early return χωρίς να ενημερώνουν το state
     σε περίπτωση σφάλματος.

4. Παρέχεις έξοδο αυστηρά στη μορφή:
   [Αρχείο] → [Μέθοδος/Provider] → [Πρόβλημα] → [Πιθανή επίπτωση στο UI]

Οι παραπάνω οδηγίες λειτουργούν ως υπόδειξη — κάνε τους δικούς σου ελέγχους στον κώδικα για παραλήψεις και σφάλματα.
```

---

### Άξονας 2 — Συμβάσεις Βάσης (SQL Contract Violations)

> **Πότε:** ΤΩΡΑ — αμέσως μετά την αναδόμηση του καταλόγου σε αυτόνομα repositories, ως «τεστ παραλαβής» της αναδόμησης. Επανάληψη μετά από κάθε αλλαγή στο σχήμα της βάσης ή προσθήκη νέου repository.
>
> **Κατάσταση 2/7/2026:** Η Φάση 1 ολοκληρώθηκε — τα σκέλη 1 και 3 καλύπτονται πλέον μόνιμα από αυτόματα τεστ: `test/architecture/sql_isolation_test.dart`, `test/core/database/schema_model_consistency_test.dart`, `test/core/database/model_null_safety_test.dart`. Στον Πράκτορα μένει μόνο η κρίση του σκέλους 2 (πού απαιτείται φίλτρο) και των transactions.

```
Αναλύεις τη βάση δεδομένων και τα repository αρχεία της εφαρμογής call_logger
για παραβιάσεις συμβάσεων SQL και λανθασμένη διαχείριση δεδομένων.

1. Σάρωσε όλα τα αρχεία ΕΚΤΟΣ του φακέλου core/database/ για άμεσες
   κλήσεις σε DatabaseHelper ή sqflite API (π.χ. db.query, db.insert,
   db.rawQuery). Αναφέρεις κάθε εύρημα ως παραβίαση του κανόνα
   «SQL μόνο στα Repositories».
   Αρχεία ελέγχου: @calls_screen.dart @tasks_screen.dart @directory_screen.dart
   @history_screen.dart @lamp_screen.dart @lamp_migration_service.dart

2. Στα Repository αρχεία ελέγχεις:
   @calls_repository.dart @user_repository.dart @phone_repository.dart
   @department_repository.dart @equipment_repository.dart @category_repository.dart
   @building_map_repository.dart @settings_repository.dart
   @remote_tools_repository.dart @dictionary_repository.dart
   καθώς και τα υποστηρικτικά: @directory_support.dart @omnisearch_service.dart @integrity_service.dart

   Για κάθε SELECT ερώτημα:
   - Φιλτράρεται ρητά η στήλη is_deleted = 0 όπου απαιτείται;
     (χρησιμοποιείται με συνέπεια το notDeletedClause του DirectorySupport
     ή υπάρχουν σημεία με χειρόγραφο — και ίσως ξεχασμένο — φίλτρο;)
   - Φιλτράρεται ρητά η στήλη lansweeper_state όπου η λογική το απαιτεί;
   - Στήλες που επιτρέπουν NULL (π.χ. caller_id, equipment_id, department_id)
     — χειρίζεται το Dart model σωστά την περίπτωση null χωρίς να κρασάρει;
   - Εγγραφές που αγγίζουν πολλούς πίνακες μαζί (π.χ. upsert + audit_log
     + search index) — εκτελούνται μέσα σε transaction ώστε να μην μείνει
     η βάση μισοενημερωμένη αν κάτι αποτύχει στη μέση;

3. Ελέγχεις τη συνέπεια μεταξύ σχήματος και model:
   Συγκρίνεις τις στήλες του πίνακα calls (όπως ορίζονται στο @database_v1_schema.dart)
   με τα πεδία του @call_model.dart — υπάρχουν στήλες που υπάρχουν στο σχήμα
   αλλά δεν αντιστοιχούν σε πεδίο model ή αντίστροφα;
   Επαναλαμβάνεις για tasks / @task.dart και equipment / @equipment_model.dart

4. Παρέχεις έξοδο αυστηρά στη μορφή:
   [Αρχείο] → [Μέθοδος/Ερώτημα] → [Πρόβλημα] → [Πιθανή επίπτωση]

Οι παραπάνω οδηγίες λειτουργούν ως υπόδειξη — κάνε τους δικούς σου ελέγχους στον κώδικα για παραλήψεις και σφάλματα.
```

---

### Άξονας 3 — Ασύγχρονες Συνθήκες Ανταγωνισμού (Async Race Conditions)

> **Πότε:** Τώρα ή πολύ σύντομα — τα προβλήματα συγχρονισμού γίνονται όλο και πιο δύσκολα στον εντοπισμό όσο μεγαλώνει ο κώδικας. Επανάληψη όταν προστίθεται νέα async ροή (νέος διάλογος με αποθήκευση, νέο service) και οπωσδήποτε πριν την καθημερινή χρήση.

```
Αναλύεις τον ασύγχρονο κώδικα της εφαρμογής call_logger για συνθήκες
ανταγωνισμού και μη ασφαλείς async μεθόδους.

1. Εξέτασε τα παρακάτω αρχεία που περιέχουν σύνθετη async λογική:
   @call_entry_provider.dart @smart_entity_selector_provider.dart
   @task_service.dart @database_backup_service.dart
   @lamp_migration_service.dart @lansweeper_sync_service.dart
   @database_maintenance_service.dart

2. Για κάθε async μέθοδο που τροποποιεί κατάσταση μετά από await:
   - Υπάρχει guard μεταβλητή (π.χ. _isLoading, _isSaving) που εμποδίζει
     διπλή ταυτόχρονη εκτέλεση;
   - Αν η μέθοδος καλείται δύο φορές γρήγορα (π.χ. διπλό tap σε κουμπί),
     τι συμβαίνει στην κατάσταση;
   - Μετά από κάθε await, ελέγχεται αν το context/ref είναι ακόμα έγκυρο
     πριν την ανάθεση νέου state;

3. Αναζήτησε συγκεκριμένα αυτά τα patterns:
   - setState() ή state = ... μετά από await χωρίς mounted check σε StatefulWidget.
   - Future.wait() ή παράλληλα awaits που τροποποιούν τον ίδιο πίνακα SQLite.
   - Timer ή Future.delayed που δεν ακυρώνονται στο dispose().
   - showDialog / Navigator push μετά από await χωρίς context.mounted check.

4. Στο κλείσιμο παραθύρου (AppShortcuts, window close flow):
   @app_shortcuts.dart @database_exit_backup.dart @database_helper.dart
   Ελέγχεις αν υπάρχει πιθανότητα η ροή κλεισίματος να εκτελεστεί δύο φορές
   ταυτόχρονα ή να διακοπεί στη μέση λόγω exception χωρίς να κλείσει
   η σύνδεση με τη βάση.

5. Παρέχεις έξοδο αυστηρά στη μορφή:
   [Αρχείο] → [Μέθοδος] → [Σενάριο ανταγωνισμού] → [Πιθανή επίπτωση]

Οι παραπάνω οδηγίες λειτουργούν ως υπόδειξη — κάνε τους δικούς σου ελέγχους στον κώδικα για παραλήψεις και σφάλματα.
```

---

### Άξονας 4 — Διαχείριση Πόρων (Resource Management)

> **Πότε:** Όταν σταθεροποιηθεί το UI — π.χ. αφού κλείσουν οι εκκρεμότητες της λίστας «Διορθώσεις» παραπάνω. Αν τρέξει σε οθόνες που αλλάζουν ακόμα συχνά, τα ευρήματα ξεπερνιούνται γρήγορα. Οι διαρροές πόρων φαίνονται μόνο σε πολύωρη χρήση, οπότε το όριο είναι πριν τη «σοβαρή» καθημερινή λειτουργία. Επανάληψη μετά από κάθε νέα οθόνη ή διάλογο με πολλά πεδία.
>
> **Κατάσταση 2/7/2026 (Φάση 2 ολοκληρωμένη):** Το leak_tracker είναι εγκατεστημένο (`test/flutter_test_config.dart`) και ελέγχει αυτόματα όλα τα widget τεστ — κάθε νέα διαρροή θα κοκκινίζει το αντίστοιχο τεστ. Οι 5 ρίζες διαρροών στο lib/ διορθώθηκαν, οι εξαιρέσεις (singletons, ImageCache) είναι τεκμηριωμένες στο config, και όλοι οι φάκελοι τεστ είναι πράσινοι. Πλήρες ιστορικό στο `docs/Αναφορά_Διαρροών.md`.
>
> **Τυφλή γωνία για τον Πράκτορα:** η εξαίρεση της κλάσης `Image` (ImageCache) κρύβει και τυχόν δικές μας διαρροές `ui.Image` — το σημείο 4 της προτροπής (εικόνες χάρτη ορόφων / CustomPainter) ΔΕΝ καλύπτεται από τον αυτόματο φύλακα και παραμένει υποχρεωτικό αντικείμενο του ελέγχου Πράκτορα.

```
Αναλύεις τη διαχείριση πόρων (resources) στην εφαρμογή call_logger
για διαρροές μνήμης και μη απελευθερωμένους πόρους.

1. Εξέτασε όλα τα StatefulWidget αρχεία στα features:
   @calls_screen.dart @call_header_form.dart @smart_entity_selector_widget.dart
   @smart_entity_selector_caller_field.dart @smart_entity_selector_equipment_field.dart
   @smart_entity_selector_phone_field.dart @smart_entity_selector_department_field.dart
   @tasks_screen.dart @task_form_dialog.dart @task_close_dialog.dart
   @directory_screen.dart @user_form_dialog.dart @equipment_form_dialog.dart
   @department_form_dialog.dart @building_map_dialog.dart

2. Για κάθε StatefulWidget καταγράφεις:
   - Κάθε FocusNode που δημιουργείται — έχει αντίστοιχο .dispose() στο dispose();
   - Κάθε TextEditingController — έχει αντίστοιχο .dispose();
   - Κάθε ScrollController / PageController / TabController — έχει .dispose();
   - Κάθε AnimationController — έχει .dispose();
   - Κάθε StreamSubscription — έχει .cancel();

3. Στα ConsumerStatefulWidget (Riverpod):
   - Υπάρχουν ref.listen() κλήσεις χωρίς αντίστοιχη ακύρωση;
   - Δημιουργούνται objects μέσα στο build() που θα έπρεπε να είναι
     στο initState() (π.χ. FocusNode, Controller);

4. Στο building map (CustomPainter / Canvas):
   @building_map_sheet_painter.dart @building_map_sheet_viewport.dart
   @building_map_controller.dart
   - Εικόνες (ui.Image) που φορτώνονται — αποδεσμεύονται με .dispose()
     όταν αλλάζει ο όροφος ή κλείνει το dialog;
   - Υπάρχουν listeners στο GestureDetector ή ChangeNotifier που δεν
     αφαιρούνται;

5. Παρέχεις έξοδο αυστηρά στη μορφή:
   [Αρχείο] → [Κλάση] → [Πόρος που δεν αποδεσμεύεται] → [Πιθανή επίπτωση]

Οι παραπάνω οδηγίες λειτουργούν ως υπόδειξη — κάνε τους δικούς σου ελέγχους στον κώδικα για παραλήψεις και σφάλματα.
```

---

### Άξονας 5 — Κενά Επαγγελματικής Λογικής (Business Logic Gaps)

> **Πότε:** Ανά ροή, όταν η ροή «κλειδώνει» λειτουργικά. Τα κομμάτια 3 (LAMP) και 4 (backup) μπορούν να τρέξουν σύντομα γιατί είναι ολοκληρωμένα· τα κομμάτια 1–2 (κλήσεις, εκκρεμότητες) αφού κλείσουν οι σχετικές εκκρεμότητες της λίστας «Διορθώσεις» (σημειώσεις αναβολής, χρόνος ολοκλήρωσης). Ολόκληρος ο άξονας ξανά πριν βασιστεί η δουλειά στην εφαρμογή.

```
Αναλύεις την επαγγελματική λογική (business logic) της εφαρμογής call_logger
για σιωπηλές αποτυχίες και μη χειριζόμενες οριακές περιπτώσεις.

1. Ροή δημιουργίας/αποθήκευσης κλήσης:
   @call_entry_provider.dart @call_header_provider.dart @calls_repository.dart
   Ακολούθησε τη ροή από το «Αποθήκευση» κουμπί μέχρι την εγγραφή στη βάση.
   - Τι γίνεται αν ο χρήστης πατήσει «Αποθήκευση» ενώ δεν έχει επιλεγεί
     καλών (caller_id = null) και caller_text είναι κενό;
   - Τι γίνεται αν η κατηγορία δεν υπάρχει πλέον στη βάση (διαγράφηκε
     εν τω μεταξύ);
   - Αν η αποθήκευση αποτύχει (exception), ενημερώνεται ο χρήστης ή
     χάνεται σιωπηλά η κλήση;

2. Ροή διαχείρισης εκκρεμοτήτων (tasks):
   @task_service.dart @tasks_provider.dart @task_form_dialog.dart
   - Τι γίνεται αν μια εκκρεμότητα συνδέεται με κλήση που έχει διαγραφεί
     (is_deleted = 1); Εμφανίζεται στη λίστα; Μπορεί να κλείσει κανονικά;
   - Το snooze — αν η ώρα του συστήματος είναι λανθασμένη ή αλλάξει
     (π.χ. θερινή ώρα), τι συμβαίνει με τις προθεσμίες;
   - Αν δύο χρήστες στο κοινό δίκτυο κλείσουν την ίδια εκκρεμότητα
     ταυτόχρονα, τι καταγράφεται στο audit;

3. Ροή μεταφοράς LAMP:
   @lamp_migration_service.dart @lamp_transfer_wizard_dialog.dart
   @old_equipment_repository.dart
   - Αν μια οντότητα (π.χ. χρήστης) υπάρχει ήδη στην κύρια βάση με το
     ίδιο όνομα, τι γίνεται; Δημιουργείται διπλότυπο ή απορρίπτεται;
   - Αν η μεταφορά διακοπεί στη μέση (π.χ. κλείσιμο παραθύρου), η βάση
     παραμένει σε συνεπή κατάσταση; Υπάρχει transaction rollback;
   - Καταγράφονται οι μεταφερθείσες οντότητες στο audit_log;

4. Ροή backup και συντήρησης βάσης:
   @database_backup_service.dart @database_maintenance_service.dart
   @database_exit_backup.dart
   - Αν ο χώρος του δίσκου εξαντληθεί κατά το backup, τι γίνεται με
     το ημιτελές αρχείο; Καθαρίζεται;
   - Αν το backup on exit αποτύχει, εμποδίζεται το κλείσιμο της εφαρμογής
     ή αγνοείται το σφάλμα;
   - Το retention (διαγραφή παλιών backup) — αν αποτύχει, επηρεάζεται
     το κυρίως backup;

5. Παρέχεις έξοδο αυστηρά στη μορφή:
   [Αρχείο] → [Ροή/Σενάριο] → [Οριακή περίπτωση] → [Τι συμβαίνει σήμερα] → [Πιθανή επίπτωση]

Οι παραπάνω οδηγίες λειτουργούν ως υπόδειξη — κάνε τους δικούς σου ελέγχους στον κώδικα για παραλήψεις και σφάλματα.
```

---

### Άξονας 6 — Ακεραιότητα Σχέσεων & Ορφανές Εγγραφές (Referential Integrity)

> **Πότε:** Αντίθετα με τους Άξονες 1–5, εδώ έχει σημασία και το περιεχόμενο της βάσης, όχι μόνο ο κώδικας. Πρώτη εκτέλεση: τώρα, για το σκέλος του κώδικα (ροές διαγραφής). Επανάληψη: μετά από κάθε μεταφορά LAMP και περιοδικά (π.χ. ανά μήνα) όταν η εφαρμογή χρησιμοποιείται με πραγματικά δεδομένα.
>
> **Κατάσταση 2/7/2026 (Φάση 3 ολοκληρωμένη):** Ο σαρωτής ορφανών εγγραφών υπάρχει και είναι πλήρης — 19-βηματικό διαγνωστικό (`database_integrity_diagnostics.dart`) με ευρήματα, επιδιορθώσεις και audit. Καλύπτονται όλες οι σχέσεις του σχήματος, μαζί με τις 3 που προστέθηκαν (εξοπλισμός→τμήμα, τμήμα→όροφος, τηλέφωνο→τμήμα). Σύμβαση: soft-deleted γονέας = ιστορική αλήθεια (όχι εύρημα)· εύρημα μόνο η αναφορά σε εγγραφή που λείπει εντελώς. Το σημείο 4 της προτροπής (περιοδικός σαρωτής) ΕΧΕΙ υλοποιηθεί — ο περιοδικός έλεγχος σε πραγματικά δεδομένα γίνεται από την οθόνη ακεραιότητας της εφαρμογής. Στον Πράκτορα μένει μόνο το σκέλος 2 για το UI (τι βλέπει ο χρήστης όταν ο γονέας είναι διαγραμμένος) και το undo/restore.

```
Αναλύεις το σχήμα της βάσης και τη λογική διαγραφών της εφαρμογής call_logger
για κινδύνους ορφανών εγγραφών και ασυνεπών σχέσεων.
ΚΡΙΣΙΜΟ ΠΛΑΙΣΙΟ: στο @database_v1_schema.dart οι σχέσεις μεταξύ πινάκων
ΔΕΝ προστατεύονται με FOREIGN KEY (μόνη εξαίρεση τα remote_tools) —
η συνέπεια στηρίζεται αποκλειστικά στη λογική της εφαρμογής.

1. Χαρτογράφησε όλες τις λογικές σχέσεις μεταξύ πινάκων με βάση το
   @database_v1_schema.dart (π.χ. calls.caller_id → users,
   calls.department_id → departments, calls.equipment_id → equipment,
   tasks → calls, equipment → departments, phones → users/departments,
   τοποθετήσεις στον χάρτη ορόφων → τμήματα/χρήστες).

2. Για κάθε σχέση ελέγχεις τη ροή soft delete (is_deleted = 1):
   - Όταν «διαγράφεται» γονέας (χρήστης, τμήμα, εξοπλισμός, κατηγορία),
     τι συμβαίνει στις εγγραφές που δείχνουν σε αυτόν; Ενημερώνονται,
     καθαρίζονται, ή μένουν να δείχνουν σε διαγραμμένη εγγραφή;
   - Στα σημεία του UI που εμφανίζεται η σχέση, τι βλέπει ο χρήστης
     όταν ο γονέας είναι διαγραμμένος; (κενό, παύλα, ή κρασάρισμα;)
   - Στην επαναφορά (undo) μιας διαγραμμένης οντότητας, επανέρχονται
     και οι σχέσεις της ή έχουν χαθεί οριστικά;
   Αρχεία: @user_repository.dart @department_repository.dart
   @equipment_repository.dart @phone_repository.dart @category_repository.dart
   @building_map_repository.dart @integrity_service.dart

3. Σύγκρινε τα ευρήματα με όσα ήδη καλύπτει το @integrity_service.dart —
   ποιες σχέσεις ελέγχονται ήδη και ποιες μένουν ακάλυπτες;

4. Πρότεινε (χωρίς να υλοποιήσεις ακόμα) έναν περιοδικό έλεγχο δεδομένων
   στο integrity_service: SQL ερωτήματα που εντοπίζουν ορφανές εγγραφές
   στην πραγματική βάση (π.χ. κλήσεις με caller_id που δεν υπάρχει ή
   δείχνει σε is_deleted = 1) και αναφέρουν πλήθος ανά σχέση.

5. Παρέχεις έξοδο αυστηρά στη μορφή:
   [Σχέση] → [Σενάριο] → [Τι συμβαίνει σήμερα] → [Πιθανή επίπτωση]

Οι παραπάνω οδηγίες λειτουργούν ως υπόδειξη — κάνε τους δικούς σου ελέγχους στον κώδικα για παραλήψεις και σφάλματα.
```
